window.LESSONS = {"title": "Technical Lessons Learnt — Next.js / TypeScript / Python / FastAPI / PostgreSQL / MongoDB", "schema_version": 1, "as_of": "2026-09-20", "content_hash": "9a666ef399e1eea4", "how_to_use": ["Each lesson is stack-level, not application-level: symptom → root cause → resolution → prevention → detection.", "severity uses the repolens vocabulary (critical/high/medium/low/info) and ranks the blast radius when the trap is hit, not how often.", "detection[].kind: regex (grep-able pattern), ast (needs a parser), sql (run against the live catalog), shell, ci, test, lint, config, review (human judgement). Regexes are starting points — tune per repo; every one has false positives.", "evidence cites commits/docs in the source repository; sources are external references. An evidence-free lesson came from research, not an incident.", "Rule packs: docs/lessons/rules/ (one Markdown file per category). Regenerate with python3 docs/lessons/export_lessons_learnt.py --out docs/lessons/rules [--category <id>] [--min-severity high].", "content_hash is sha256 of json.dumps(lessons, sort_keys=True, separators=(',',':'), ensure_ascii=False), first 16 hex."], "severity_levels": ["critical", "high", "medium", "low", "info"], "categories": [{"id": "nextjs-react", "title": "Next.js & React", "group": "frontend", "summary": "Rendering, hydration, build output, App Router, Next 16 changes.", "count": 28}, {"id": "auth-session", "title": "Auth & Session (frontend)", "group": "frontend", "summary": "NextAuth/session loading, token storage, impersonation.", "count": 4}, {"id": "api-contract", "title": "API Contract (client ↔ server)", "group": "frontend", "summary": "Axios base URLs, error envelopes, payload shapes that fail silently.", "count": 13}, {"id": "typescript-js", "title": "TypeScript & JavaScript", "group": "frontend", "summary": "Language traps: dates, sorting, numbers, truthiness, types that lie.", "count": 12}, {"id": "frontend-testing", "title": "Frontend Testing", "group": "frontend", "summary": "Jest, React Testing Library, Radix, Playwright.", "count": 12}, {"id": "js-dependencies", "title": "JS Dependencies & Tooling", "group": "frontend", "summary": "Yarn/npm, lockfiles, CVEs, upgrades.", "count": 8}, {"id": "accessibility", "title": "Accessibility (WCAG)", "group": "frontend", "summary": "ARIA roles that promise more than the markup delivers, live regions, contrast across themes, keyboard navigation.", "count": 4}, {"id": "fastapi-pydantic", "title": "FastAPI & Pydantic", "group": "python", "summary": "Routing, dependencies, response models, validation, error handling.", "count": 20}, {"id": "python-core", "title": "Python Core", "group": "python", "summary": "Language and stdlib traps: datetimes, rounding, defaults, imports, excepts.", "count": 24}, {"id": "asyncio", "title": "asyncio", "group": "python", "summary": "Tasks, gather, cancellation, event loops, blocking calls.", "count": 7}, {"id": "python-testing", "title": "Python Testing", "group": "python", "summary": "pytest, mocks, fixtures, async tests, isolation.", "count": 16}, {"id": "postgres-rls", "title": "PostgreSQL: RLS & Tenancy", "group": "database", "summary": "Row-level security, tenant context, pooling, views.", "count": 10}, {"id": "postgres-schema", "title": "PostgreSQL: Schema, Types & Privileges", "group": "database", "summary": "Constraints, NULLs, types, ownership, timeouts.", "count": 15}, {"id": "alembic", "title": "Alembic Migrations", "group": "database", "summary": "Revision discipline, heads, env.py, locks, autogenerate.", "count": 9}, {"id": "sqlalchemy-asyncpg", "title": "SQLAlchemy & asyncpg", "group": "database", "summary": "Bind params, transactions, pools, async ORM, poolers.", "count": 8}, {"id": "mongodb", "title": "MongoDB & PyMongo", "group": "database", "summary": "Indexes, types, filters, upserts, pagination, injection.", "count": 16}, {"id": "dual-store", "title": "Multi-store Consistency & Data Repair", "group": "database", "summary": "Two stores with no shared transaction; shadow/parity; repair scripts.", "count": 8}, {"id": "security-authz", "title": "Security & Authorisation", "group": "cross-cutting", "summary": "BOLA, mass assignment, effective roles, claims, credentials.", "count": 8}, {"id": "gates-ratchets", "title": "CI Gates, Audits & Ratchets", "group": "tooling", "summary": "How checks die, oracles, baselines, what green actually means.", "count": 17}, {"id": "generated-artefacts-git", "title": "Generated Artefacts & Git", "group": "tooling", "summary": "Merge drivers, determinism, scripted edits, worktrees.", "count": 9}, {"id": "github-deps", "title": "GitHub & Dependency Automation", "group": "tooling", "summary": "gh CLI, token scopes, Dependabot, review bots.", "count": 5}, {"id": "deploy-ops", "title": "Deploy & Operations", "group": "tooling", "summary": "Schedulers, systemd, preflights, live builds.", "count": 7}, {"id": "secrets-test-isolation", "title": "Secrets & Test Isolation", "group": "tooling", "summary": "Secrets in templates, tests reaching production, test-data flags.", "count": 5}, {"id": "docs-knowledge", "title": "Docs, Knowledge & Agent Practice", "group": "tooling", "summary": "Readings vs facts, doc gates, working with coding agents.", "count": 4}], "lessons": [{"id": "NX-001", "category": "nextjs-react", "title": "A statement line with no semicolon merges with a following line that starts with `(`, `[` or a backtick after minification", "stacks": ["nextjs", "typescript", "nextauth", "minification"], "severity": "critical", "symptom": "Login worked in `next dev` and failed in the production build with `JWTSessionError: TypeError: t.role is not a function`.", "root_cause": "Automatic semicolon insertion does not insert a semicolon before a line that begins with `(`. `(token as any).role` followed by a line starting `(session.user as any).data` was parsed as a call: `t.role(e.user)`. The dev server did not minify, so it never showed up there.", "resolution": "Added explicit semicolons to every statement in the NextAuth `jwt()`/`session()` callbacks. Introduced intermediate variables (`const tok = token as any;`) so no line starts with `(`. Left a warning comment in the file.", "prevention": "Enforce `semi: always` and `no-unexpected-multiline` in ESLint for all TS/JS. Verify auth flows against a production build, never only against `next dev`.", "detection": [{"kind": "lint", "recipe": "ESLint `semi: ['error','always']` + `no-unexpected-multiline: 'error'`"}, {"kind": "regex", "recipe": "a line not ending in [;{},(\\[] followed by a line matching ^\\s*[\\(\\[`]"}], "evidence": ["fd4df225b (2026-02-21)", "frontend/src/auth.ts"], "sources": []}, {"id": "NX-002", "category": "nextjs-react", "title": "A role guard that runs before the session resolves redirects authorised users", "stacks": ["react", "nextauth", "client-components"], "severity": "high", "symptom": "Admins were bounced to `/dashboard`. The Playwright specs covering admin pages failed about one run in two.", "root_cause": "On the first render `user` is null, so `isAdmin()` is false and a `useEffect` or render guard redirects before the session has loaded.", "resolution": "Every guard checks `if (loading) return;` (or renders null) BEFORE any role predicate, both in the effect and in the render guard.", "prevention": "Guard helpers should return a tri-state (`loading | allowed | denied`) so a boolean check cannot be written before loading completes.", "detection": [{"kind": "ast", "recipe": "a useEffect or early return that calls router.replace/push inside a branch on is[A-Z]\\w*\\( or user?.role, where the scope destructures useAuth() without reading `loading` first"}], "evidence": ["2c1a0934e (2026-07-02)", "CLAUDE.md §Auth Loading Guard"], "sources": []}, {"id": "NX-003", "category": "nextjs-react", "title": "Reading browser storage in a `useState` initialiser causes a hydration mismatch", "stacks": ["react", "ssr", "nextjs"], "severity": "medium", "symptom": "React error #418 (hydration mismatch) on first load.", "root_cause": "`useState(() => localStorage.getItem(...))` returns null during the server render and a value in the browser, so the first client render differs from the server HTML.", "resolution": "Initialise the state to a server-safe value and read storage in a mount `useEffect` (or render behind a `mounted` flag).", "prevention": "Treat every access to `window`, `document`, `navigator` or storage during render as server-unsafe. Non-deterministic values (`Date.now()`, `Math.random()`, `toLocaleString()` without a fixed `timeZone`) cause the same class of mismatch.", "detection": [{"kind": "ast", "recipe": "useState( initialiser referencing localStorage|sessionStorage|window.|document.|navigator."}, {"kind": "regex", "recipe": "toLocale(Date|Time)?String\\(\\)|Math\\.random\\(\\)|Date\\.now\\(\\) inside a component render body"}], "evidence": ["8fd208c77, 1ac3c8182 (2026-04-21)"], "sources": ["https://react.dev/reference/react-dom/client/hydrateRoot#handling-different-client-and-server-content"]}, {"id": "NX-004", "category": "nextjs-react", "title": "An effect whose condition is always true and which sets its own dependency loops forever", "stacks": ["react", "hooks"], "severity": "high", "symptom": "A POST fired about once per second and hammered every downstream endpoint.", "root_cause": "The effect compared two identifiers of different kinds (a human-readable number against a UUID), so the condition was always true. It then called `setToken`, and `token` was in the effect's dependency list, so the effect re-ran.", "resolution": "Compare like-for-like identifiers, make the condition idempotent, and never set state that the same effect depends on unless it converges.", "prevention": "Add a test asserting the request count per mount (e.g. exactly one POST). Review every effect that calls a setter for one of its own dependencies.", "detection": [{"kind": "ast", "recipe": "useEffect whose deps include X and whose body (directly or via a called callback) calls setX under a condition"}, {"kind": "test", "recipe": "count network calls per mount with a mocked client; assert an upper bound"}], "evidence": ["b9bc1a0cd, 9d6e0b1be (2026-06-28)"], "sources": []}, {"id": "NX-005", "category": "nextjs-react", "title": "Unstable references in a dependency array cause render loops", "stacks": ["react", "hooks", "eslint"], "severity": "high", "symptom": "Adding `router` from `useRouter()` to an effect's dependencies, to satisfy `exhaustive-deps`, looped under the test harness and turned 12 tests red.", "root_cause": "An object that is re-created on every render (a mocked router, an inline object or array literal, an inline `accessors` config) changes identity on every render, so the effect re-fires, sets state, re-renders, and repeats.", "resolution": "Hoist constant configuration to module scope, memoise derived objects, and keep unstable hook results out of the dependency list or read them through a ref.", "prevention": "When satisfying `exhaustive-deps`, check that each added dependency is referentially stable.", "detection": [{"kind": "ast", "recipe": "deps array containing a useRouter()/useNavigate() result or an object/array literal created in render, where the effect calls a state setter"}], "evidence": ["de93f0f38 (2026-08-20)", "tasks/lessons.md"], "sources": []}, {"id": "NX-006", "category": "nextjs-react", "title": "StrictMode runs effects twice in development; non-idempotent effects fire twice", "stacks": ["react", "strictmode"], "severity": "medium", "symptom": "Duplicate POSTs and duplicate analytics events in development. Combined with RSC prefetch, a burst of guaranteed 403s looked like a redirect loop.", "root_cause": "In development, React 18/19 StrictMode mounts, unmounts and re-mounts every component, so an effect with no cleanup runs twice. Effects that fire fetches the user is not entitled to make produce 403s on every mount.", "resolution": "Mutations belong in event handlers or server actions, not effects. Fetch effects return an AbortController cleanup. Gate scoped fetches on claims already present in the session.", "prevention": "Lint: flag `useEffect` bodies that call `.post/.put/.delete`.", "detection": [{"kind": "regex", "recipe": "useEffect\\([^)]*=>\\s*\\{[^}]*\\.(post|put|patch|delete)\\("}], "evidence": ["de27aeff1 (2026-05-03)", "7a6e4c2e7"], "sources": ["https://react.dev/reference/react/StrictMode#fixing-bugs-found-by-double-rendering-in-development"]}, {"id": "NX-007", "category": "nextjs-react", "title": "A one-shot URL parameter that is never consumed re-fires its action", "stacks": ["nextjs", "router"], "severity": "medium", "symptom": "`?new=true` survived every `router.replace()` and re-opened the dialog after each navigation.", "root_cause": "The effect read the parameter and opened the dialog, but nothing removed the parameter from the URL, so every re-render re-triggered it.", "resolution": "Consume the parameter: after acting on it, `router.replace` to the same path without it.", "prevention": "Pair every `searchParams.get(X)` that triggers an action with a code path that deletes X.", "detection": [{"kind": "ast", "recipe": "effect reading searchParams.get(X) that triggers setOpen(true)/an action, with no code path that removes X from the URL"}], "evidence": ["de93f0f38"], "sources": []}, {"id": "NX-008", "category": "nextjs-react", "title": "Invalid DOM nesting breaks layout and hydration", "stacks": ["react", "html"], "severity": "low", "symptom": "Tables rendered misaligned, and hydration warnings appeared for `<div>` inside `<p>` and `<tr>` inside `<tr>`.", "root_cause": "Browsers repair invalid nesting while parsing the HTML, so the DOM no longer matches React's virtual tree.", "resolution": "Use fragments with sibling `<tr>` elements, and `<span>` inside `<p>`.", "prevention": "Run a JSX nesting validator (React's validateDOMNesting rules) in lint or tests.", "detection": [{"kind": "ast", "recipe": "JSX <div>|<p>|<table> inside <p>; <tr> directly inside <tr>"}], "evidence": ["ae822ed69 (2026-04-26)", "785a102bc (2026-08-25)"], "sources": []}, {"id": "NX-009", "category": "nextjs-react", "title": "A `<Link>` wrapper breaks the child's `h-full`", "stacks": ["nextjs", "tailwind"], "severity": "low", "symptom": "Card grids rendered cards of unequal height, and the link-cards were missing their footer.", "root_cause": "`next/link` renders an `<a>` element with no height of its own, so `h-full` on its child resolves against an auto-height parent.", "resolution": "Put `h-full` (or `block h-full`) on the `Link` itself.", "prevention": "Lint for a `<Link>` whose only child carries `h-full` while the Link lacks it.", "detection": [{"kind": "ast", "recipe": "<Link className=...> without h-full whose single child has className containing h-full"}], "evidence": ["3d8ae5253, 314ec5960"], "sources": []}, {"id": "NX-010", "category": "nextjs-react", "title": "`NEXT_PUBLIC_*` is inlined at build time, and a Create React App prefix is `undefined` in Next.js", "stacks": ["nextjs", "env"], "severity": "high", "symptom": "Eight files silently read `undefined` from `process.env.REACT_APP_*`. After changing a public env value, restarting the server did nothing.", "root_cause": "Next.js text-replaces `process.env.NEXT_PUBLIC_X` into the client bundle during `next build`. Other prefixes are not exposed to the client. Dynamic access (`process.env[name]`) is never inlined.", "resolution": "Renamed the variables to `NEXT_PUBLIC_*` and documented that a change needs a rebuild, not a restart.", "prevention": "Never put secrets under `NEXT_PUBLIC_`. Server-only values are read on the server at request time.", "detection": [{"kind": "regex", "recipe": "process\\.env\\.REACT_APP_"}, {"kind": "regex", "recipe": "NEXT_PUBLIC_\\w*(SECRET|KEY|TOKEN|PASSWORD)"}, {"kind": "ast", "recipe": "client component ('use client' or pages/) reading process.env.X where X lacks the NEXT_PUBLIC_ prefix"}], "evidence": ["4e7c1e86b (2026-04-18)"], "sources": ["https://nextjs.org/docs/app/guides/environment-variables#bundling-environment-variables-for-the-browser"]}, {"id": "NX-011", "category": "nextjs-react", "title": "Next.js 16: request APIs are Promises only", "stacks": ["nextjs"], "severity": "high", "symptom": "Code written for Next 14 reads `params.id` synchronously, which fails at type-check or returns a Promise at runtime.", "root_cause": "Next 16 removed the Next 15 sync compatibility shim. `params`, `searchParams`, `cookies()`, `headers()` and `draftMode()` must be awaited, and the `upgrade` codemod does not run the async-API migration.", "resolution": "Run `npx @next/codemod@canary next-async-request-api .`, then type pages with `PageProps<'/route'>` from `next typegen`.", "prevention": "Run `tsc --noEmit` in CI.", "detection": [{"kind": "regex", "recipe": "(params|searchParams)\\.\\w+|cookies\\(\\)\\.(get|set)|headers\\(\\)\\.get  (flag when not preceded by await)"}], "evidence": [], "sources": ["https://nextjs.org/docs/app/guides/upgrading/version-16#async-request-apis-breaking-change"]}, {"id": "NX-012", "category": "nextjs-react", "title": "Next.js 16: `middleware` becomes `proxy`, and proxy is not an authorization layer", "stacks": ["nextjs", "security"], "severity": "critical", "symptom": "Auth decisions made only in middleware are bypassable. CVE-2025-29927: a forged `x-middleware-subrequest` header skipped middleware entirely on unpatched 11.1–15.2.2.", "root_cause": "Middleware/proxy matches paths and does not cover every entry point: server actions, route handlers and data fetches. Next 16 renames `middleware.ts` to `proxy.ts`, which runs on Node only.", "resolution": "Authorise inside the data-access layer, every route handler and every server action. Strip `x-middleware-subrequest` at the reverse proxy.", "prevention": "Rename `middleware.ts` with the codemod, and audit any auth logic that exists only in proxy.", "detection": [{"kind": "shell", "recipe": "ls src/middleware.* middleware.*; grep -n skipMiddleware next.config.*"}, {"kind": "config", "recipe": "nginx: proxy_set_header x-middleware-subrequest \"\";"}], "evidence": [], "sources": ["https://nextjs.org/blog/cve-2025-29927", "https://nextjs.org/docs/app/guides/upgrading/version-16#middleware-to-proxy"]}, {"id": "NX-013", "category": "nextjs-react", "title": "Server Actions are public POST endpoints", "stacks": ["nextjs", "react19", "security"], "severity": "critical", "symptom": "A page-level auth check does not protect the actions defined on that page. Their return values are serialised to the client.", "root_cause": "Every exported `'use server'` function that is referenced from anywhere is reachable by a direct POST, whatever the page's own guard does.", "resolution": "In every action: authenticate, check ownership (IDOR), validate arguments (zod), and return a minimal DTO. Mark the DAL with `import 'server-only'`.", "prevention": "When there are multiple instances, set `NEXT_SERVER_ACTIONS_ENCRYPTION_KEY`, and set `serverActions.allowedOrigins` behind a proxy.", "detection": [{"kind": "shell", "recipe": "rg -l \"^['\\\"]use server\" | xargs rg -L \"auth\\(|getCurrentUser\""}], "evidence": [], "sources": ["https://nextjs.org/docs/app/guides/data-security#mutating-data"]}, {"id": "NX-014", "category": "nextjs-react", "title": "The server/client boundary follows the import graph", "stacks": ["nextjs", "security"], "severity": "high", "symptom": "A module that reads a non-public env var, imported by a client component, produced `undefined` in the browser. Passing a DB row as a prop shipped every column to the client.", "root_cause": "Anything imported by a `'use client'` module is bundled for the browser, and props crossing the boundary are serialised in full.", "resolution": "Mark DAL and secret modules with `import 'server-only'`, and pass DTOs, never rows.", "prevention": "Optionally enable `experimental.taint` for sensitive objects.", "detection": [{"kind": "shell", "recipe": "rg -L \"server-only\" $(rg -l \"process\\.env\\.(?!NEXT_PUBLIC)\" src/lib)"}], "evidence": [], "sources": ["https://nextjs.org/docs/app/guides/data-security#preventing-client-side-execution-of-server-only-code"]}, {"id": "NX-015", "category": "nextjs-react", "title": "`tsc --noEmit` passes while `next build` fails", "stacks": ["nextjs", "turbopack", "typescript", "allowJs"], "severity": "high", "symptom": "The build failed on `the name Button is defined multiple times` in a `.jsx` file, and on an imported package that was not installed. Type-checking was green.", "root_cause": "`tsc` with `allowJs` does not report duplicate import bindings or unresolved modules in JS the way the bundler does. Only `next build` exercises the real module graph.", "resolution": "Rule: no change is 'verified' until a clean `next build` has passed.", "prevention": "Enable `import/no-duplicates` and `import/no-unresolved` for JS/JSX, and run `next build` as a CI job.", "detection": [{"kind": "lint", "recipe": "eslint-plugin-import: no-duplicates, no-unresolved on **/*.{js,jsx}"}, {"kind": "ci", "recipe": "a job that runs `next build` from a clean checkout"}], "evidence": ["b371e1b15 (2026-08-25)", "tasks/lessons.md"], "sources": []}, {"id": "NX-016", "category": "nextjs-react", "title": "A warm `.next`, `.turbopack` or `tsbuildinfo` cache masks build regressions", "stacks": ["nextjs", "ci"], "severity": "high", "symptom": "A dependency bump built green locally and broke the clean production build.", "root_cause": "Incremental caches reuse results computed against the old dependency tree.", "resolution": "Run `rm -rf .next .turbopack tsconfig.tsbuildinfo` before every verification build.", "prevention": "CI verification jobs must not restore `.next/cache`.", "detection": [{"kind": "ci", "recipe": "a build step not preceded by a cache clean, or a cache restore of .next in a verification job"}], "evidence": ["3ca08b858 (2026-08-10)"], "sources": []}, {"id": "NX-017", "category": "nextjs-react", "title": "Stale `.next/types` route validators reference deleted routes", "stacks": ["nextjs"], "severity": "low", "symptom": "Type errors pointed at pages that had been deleted.", "root_cause": "Next generates route-type validators under `.next/types` and does not prune them when routes disappear.", "resolution": "A `prebuild` script removes `.next/types` and `.next/dev/types` (not `.next/cache`).", "prevention": "Clean generated type directories whenever routes are removed.", "detection": [{"kind": "config", "recipe": "package.json has a prebuild step that clears .next/types"}], "evidence": ["0eaf9408f (2026-08-07)", "frontend/scripts/clean-next-types.js"], "sources": []}, {"id": "NX-018", "category": "nextjs-react", "title": "Rebuilding `.next` under a live `next start`, or two servers sharing one `.next`, causes 500s", "stacks": ["nextjs", "deploy"], "severity": "high", "symptom": "Nine routes returned `ERRORED_DOCUMENT_REQUEST (500)` and clients saw `ChunkLoadError`. A deploy was killed by a concurrent build.", "root_cause": "The running server lazy-loads chunks from `.next`. Replacing or deleting them underneath it, running two `next build`s at once, or pruning `node_modules` while live chunks still import a removed package all break it.", "resolution": "Build and restart atomically, never share a `.next` between processes, provide a `--clean` deploy flag, and never `rm -rf .next` in the serving directory while the service runs.", "prevention": "A `.next` owned by root after a sudo build leaves the service unable to read it, so check ownership after every build. If a different file is missing on each retry, suspect a concurrent writer (`ps` for `next build`).", "detection": [{"kind": "shell", "recipe": "deploy scripts that rm -rf .next or run next build in the serving dir before stopping the service; multiple units with the same WorkingDirectory"}], "evidence": ["0bfa6dc1a", "docs/deployment/INSTALL_AND_DEPLOY.md"], "sources": []}, {"id": "NX-019", "category": "nextjs-react", "title": "A stray `.d.ts` next to a `.jsx` page breaks Turbopack resolution", "stacks": ["nextjs", "turbopack", "typescript"], "severity": "medium", "symptom": "`Can't resolve 'next/dist/compiled/process'`.", "root_cause": "A declaration file with the same basename as a page module confused the resolver.", "resolution": "Deleted the stray sidecars.", "prevention": "Flag any `.d.ts` that shares a basename with a `.js`/`.jsx` file under `pages/` or `app/`.", "detection": [{"kind": "shell", "recipe": "for f in $(find src -name '*.d.ts'); do b=${f%.d.ts}; ls $b.js $b.jsx 2>/dev/null; done"}], "evidence": ["0006e9928 (2026-08-10)"], "sources": []}, {"id": "NX-020", "category": "nextjs-react", "title": "Security headers set on the wrong tier", "stacks": ["nextjs", "nginx", "security"], "severity": "high", "symptom": "The CSP was attached only to API JSON responses, never to the HTML pages. `frame-ancestors 'none'` conflicted with `X-Frame-Options: SAMEORIGIN`. A rate-limit location regex matched nothing.", "root_cause": "The headers were configured on the backend tier, which does not serve the documents the browser renders.", "resolution": "Moved the CSP into `next.config` `headers()` as Report-Only first, aligned the frame directives, and added `frame-src blob:` for PDF iframes.", "prevention": "Assert on the headers of an HTML page response, not an API one.", "detection": [{"kind": "config", "recipe": "next.config.* headers() lacks Content-Security-Policy*; frame-ancestors 'none' alongside X-Frame-Options SAMEORIGIN; Report-Only without report-to"}], "evidence": ["bf5b901a5 (2026-09-07)"], "sources": []}, {"id": "NX-021", "category": "nextjs-react", "title": "Root-layout `robots: {index: true}` is inherited by authenticated routes", "stacks": ["nextjs", "seo"], "severity": "medium", "symptom": "Private pages were indexable. A `'use client'` layout cannot export `metadata`, so it could not override the setting.", "root_cause": "App Router metadata cascades from the root layout.", "resolution": "Send an `X-Robots-Tag: noindex` header from `next.config`, built from one private-route list, with a test that walks the `app/` directories.", "prevention": "Keep `robots.txt` and the private-route list derived from the same source.", "detection": [{"kind": "ast", "recipe": "root layout metadata robots.index true + authenticated segments under 'use client' layouts"}], "evidence": ["b6d170731 (2026-09-02)"], "sources": []}, {"id": "NX-022", "category": "nextjs-react", "title": "Everything under `public/` is served to the whole internet with no auth", "stacks": ["nextjs", "security"], "severity": "critical", "symptom": "24 MB of sensitive documents sat under `public/`, including three copies of one plan: a PDF, JPEGs, and a base64 copy embedded in an innocently named `.html` file.", "root_cause": "Static files bypass every auth layer, and nothing checked what was committed there.", "resolution": "Moved the records behind an authenticated asset endpoint. A test refuses identifying tokens in public paths, guide pages over 1 MB, and large images that have no stated reason.", "prevention": "Use a SIZE rule as well as a name rule: the third copy was found by asking which files were large, not by grepping a name.", "detection": [{"kind": "shell", "recipe": "find public -size +1M; grep -l 'data:image/[a-z]*;base64' public/**/*.html"}, {"kind": "test", "recipe": "assert no file in public/ exceeds N MB unless listed with a written reason"}], "evidence": ["317e7e58d (2026-09-08)"], "sources": []}, {"id": "NX-023", "category": "nextjs-react", "title": "Seeded navigation points at routes that don't exist", "stacks": ["nextjs", "app-router"], "severity": "medium", "symptom": "12 sidebar links returned 404. The route audit passed because it only read hrefs hardcoded in components.", "root_cause": "The nav config lives in database seeds, not code, so a code-only audit never sees it.", "resolution": "Collected every href from code AND seeds and resolved each one against `app/**/page.tsx` and `public/`.", "prevention": "Also flag nav items that belong to no group.", "detection": [{"kind": "test", "recipe": "enumerate hrefs from nav sources incl. seed data; each must resolve to an app route or public file"}], "evidence": ["407de29d6 (2026-08-10)", "docs/fixes/nav_stale_route_404s_2026-08-10.md"], "sources": []}, {"id": "NX-024", "category": "nextjs-react", "title": "HTML `<title>` is RCDATA, so injected markup renders as literal text", "stacks": ["html"], "severity": "low", "symptom": "A tab title displayed `<span data-x>` literally.", "root_cause": "The contents of `<title>` are raw text, not parsed as markup.", "resolution": "Put plain text in titles.", "prevention": "Regex check on title contents.", "detection": [{"kind": "regex", "recipe": "<title>[^<]*<[a-z]"}], "evidence": ["49ca3e814 (2026-09-07)"], "sources": []}, {"id": "NX-025", "category": "nextjs-react", "title": "Next.js 16 removals: `next lint`, runtime config, and a custom webpack config fail silently or loudly", "stacks": ["nextjs"], "severity": "medium", "symptom": "CI that relied on `next build` running lint now lints nothing. `getConfig()` throws. A custom or plugin-injected `webpack` config fails the build under Turbopack, which is now the default.", "root_cause": "Next 16 removed `next lint`, the `eslint` key, `serverRuntimeConfig` and `publicRuntimeConfig`, and moved `experimental.turbopack` to top-level `turbopack`.", "resolution": "Run `eslint` explicitly in CI, use env vars for configuration, and migrate aliases to `turbopack.resolveAlias` (or build with `--webpack`).", "prevention": "Re-read the upgrade guide on every major bump. Also: `revalidateTag` needs a second argument (use `updateTag` for read-your-writes), and parallel-route slots need a `default.js`.", "detection": [{"kind": "regex", "recipe": "getConfig\\(|RuntimeConfig|next lint|experimental\\.turbopack|revalidateTag\\(\\s*[^,)]+\\)"}], "evidence": [], "sources": ["https://nextjs.org/docs/app/guides/upgrading/version-16"]}, {"id": "NX-026", "category": "nextjs-react", "title": "next/image defaults tightened; a local-IP allowance is an SSRF risk", "stacks": ["nextjs", "security"], "severity": "medium", "symptom": "Images returned 400 inside a VPC, and `domains` produced deprecation warnings.", "root_cause": "Next 16 blocks private/local IPs unless `dangerouslyAllowLocalIP` is set, defaults `qualities` to `[75]`, and deprecates `images.domains`.", "resolution": "Use explicit `remotePatterns` (protocol plus hostname) and avoid `**` wildcards.", "prevention": "Review next.config image settings on upgrade.", "detection": [{"kind": "regex", "recipe": "domains:|dangerouslyAllowLocalIP|hostname:\\s*['\\\"]\\*\\*['\\\"]"}], "evidence": [], "sources": ["https://nextjs.org/docs/app/guides/upgrading/version-16#nextimage-changes"]}, {"id": "NX-027", "category": "nextjs-react", "title": "Tailwind v4: CSS-first config, and classes built at runtime are never generated", "stacks": ["tailwind"], "severity": "medium", "symptom": "Styles vanished for `` `bg-${color}-500` ``. Borders, rings and shadows changed visually after the upgrade. Removing a UI library whose classes were generated at runtime dropped its styles.", "root_cause": "Tailwind scans source text for literal class names. v4 reads `@theme` in CSS, not `tailwind.config.js` (unless `@config` is used), respects `.gitignore` when detecting sources, and changed several defaults (border colour, ring width, shadow scale names).", "resolution": "Use full literal class maps, use `@source` for packages that are gitignored or live in `node_modules`, and review the visual defaults after upgrading.", "prevention": "Lint for template-literal class names.", "detection": [{"kind": "regex", "recipe": "className=\\{`[^`]*\\$\\{"}], "evidence": ["8d7419b26"], "sources": ["https://tailwindcss.com/docs/upgrade-guide", "https://tailwindcss.com/docs/detecting-classes-in-source-files#dynamic-class-names"]}, {"id": "NX-028", "category": "nextjs-react", "title": "A nav or UI state keyed off `onClick` only misses link-driven affordances", "stacks": ["react", "accessibility"], "severity": "low", "symptom": "Cards that navigated via `href` were missing the footer/affordance that `onClick` cards had.", "root_cause": "Rendering logic checked `onClick` to decide whether something was actionable.", "resolution": "Derive 'actionable' from either `href` or `onClick`.", "prevention": "Review component props that gate affordances.", "detection": [{"kind": "review", "recipe": "components deciding interactivity from a single prop"}], "evidence": ["314ec5960"], "sources": []}, {"id": "AU-001", "category": "auth-session", "title": "`Bearer ${token}` sends the literal string `Bearer null`", "stacks": ["javascript", "axios", "auth"], "severity": "high", "symptom": "Every sub-request on one page returned 401. Backend tests could not see it.", "root_cause": "The token came from `localStorage.getItem('token')`, which returned null because the app keeps its token in React state. A template literal turns null into a truthy header string, which bypassed the shared interceptor's `if (token)` guard.", "resolution": "Use the authenticated axios instance from context. A source-scan test forbids hand-built Authorization headers.", "prevention": "One authenticated client per app, and no raw `axios.get` with manual auth headers in components.", "detection": [{"kind": "regex", "recipe": "`Bearer \\$\\{[^}]+\\}` where the identifier comes from (local|session)Storage\\.getItem"}, {"kind": "regex", "recipe": "axios\\.(get|post|put|patch|delete)\\([^)]*Authorization"}], "evidence": ["122871ac9 (2026-09-03)", "tests/frontend/unit/authenticated-client-usage.test.ts", "6c950ddfd"], "sources": []}, {"id": "AU-002", "category": "auth-session", "title": "A hardcoded fallback for the auth signing secret makes sessions forgeable", "stacks": ["nextauth", "security"], "severity": "critical", "symptom": "When `AUTH_SECRET` was unset, sessions were signed with the literal `'secret'`.", "root_cause": "`process.env.AUTH_SECRET || 'secret'` fails open.", "resolution": "Fail fast at startup when the secret is missing.", "prevention": "Secrets never have literal fallbacks, in any language.", "detection": [{"kind": "regex", "recipe": "(AUTH|NEXTAUTH|JWT)_SECRET\\s*(\\|\\||\\?\\?|or)\\s*['\\\"]"}], "evidence": ["37ef3c596 (2026-04-18)"], "sources": []}, {"id": "AU-003", "category": "auth-session", "title": "A catch-all in credentials `authorize()` hides rate limits and outages as 'wrong password'", "stacks": ["nextauth"], "severity": "medium", "symptom": "A 429 from the backend looked identical to invalid credentials (`CredentialsSignin`).", "root_cause": "`authorize()` had `catch { return null }`.", "resolution": "Re-throw on 429 and 5xx, and map NextAuth error codes to distinct user messages.", "prevention": "Never collapse different failure classes into one return value.", "detection": [{"kind": "ast", "recipe": "authorize() whose catch block only returns null without inspecting status"}], "evidence": ["22079ce54 (2026-04-18)"], "sources": []}, {"id": "AU-004", "category": "auth-session", "title": "Logout via `router.push` keeps the session cookie; navigation after `signIn` loses it", "stacks": ["nextauth"], "severity": "medium", "symptom": "Users 'logged out' but were still authenticated. After login, the next page saw no session.", "root_cause": "Client-side navigation neither clears nor waits for the cookie. NextAuth v5 reads `AUTH_URL`, not `NEXTAUTH_URL`, and needs `AUTH_TRUST_HOST=true` behind a proxy.", "resolution": "`await signOut({callbackUrl})`; use a full `window.location.href` navigation after `signIn`; set the v5 env names.", "prevention": "Keep JWT session payloads minimal: they live in a cookie readable by the client (~4 KB, chunked) and cannot be revoked before they expire.", "detection": [{"kind": "ast", "recipe": "logout handler calling router.push('/'|'/login') without an awaited signOut"}, {"kind": "regex", "recipe": "getServerSession|NEXTAUTH_URL"}], "evidence": ["fd4df225b", "aaf731348 (2026-07-02)"], "sources": ["https://authjs.dev/getting-started/migrating-to-v5"]}, {"id": "AC-001", "category": "api-contract", "title": "A double `/api/api` prefix on the axios base URL", "stacks": ["axios"], "severity": "high", "symptom": "33 call sites in 9 files returned 404 in production.", "root_cause": "The instance's `baseURL` already ended in `/api`, and callers passed `'/api/…'` as well.", "resolution": "Removed the prefix at every call site and documented the rule next to the client.", "prevention": "Flag any path whose first segment equals the last segment of the instance's `baseURL`.", "detection": [{"kind": "regex", "recipe": "\\bapi\\.(get|post|put|patch|delete)\\(\\s*[`'\\\"]/api/"}], "evidence": ["2addb9a70, 70a5eb335, 5b444aa30 (2026-04-25)"], "sources": []}, {"id": "AC-002", "category": "api-contract", "title": "A global error-envelope rewrap breaks every client that reads `detail.code`", "stacks": ["axios", "fastapi"], "severity": "high", "symptom": "Specific error messages became unreachable and users saw generic toasts, including for the login 'pending approval' case.", "root_cause": "A server-side global exception handler rewrapped structured `detail` into `{error: {code, message, metadata}}`, so `err.response.data.detail.code` was `undefined` and the UI silently degraded.", "resolution": "Added one tolerant reader, `getApiErrorDetail()`, that accepts both shapes, and migrated callers to it.", "prevention": "An error-shape change is an API contract change: grep every consumer.", "detection": [{"kind": "lint", "recipe": "ESLint no-restricted-syntax on MemberExpression response.data.detail.(code|message) outside the owner module"}, {"kind": "regex", "recipe": "response\\??\\.data\\??\\.detail\\??\\.(code|message)"}], "evidence": ["17c7881ba, cb62106a6 (2026-08-27)"], "sources": []}, {"id": "AC-003", "category": "api-contract", "title": "A FastAPI 422 `detail` is an array, and `typeof [] === 'object'`", "stacks": ["javascript", "fastapi"], "severity": "medium", "symptom": "Validation errors rendered as empty messages.", "root_cause": "The reader branched on `typeof detail === 'object'` and destructured named keys from an array.", "resolution": "Check `Array.isArray` first and join the `msg` fields.", "prevention": "Handle every documented error shape in the one error reader.", "detection": [{"kind": "ast", "recipe": "typeof x === 'object' branch that destructures named keys with no Array.isArray(x) check before it"}], "evidence": ["aa2e58923 (2026-08-28)"], "sources": []}, {"id": "AC-004", "category": "api-contract", "title": "`responseType: 'blob'` also applies to the error body", "stacks": ["axios"], "severity": "medium", "symptom": "A failed download showed `[object Blob]` or a generic message.", "root_cause": "axios decodes an error response with the same `responseType`, so the JSON error arrives as a Blob.", "resolution": "Added an async reader: `await blob.text()`, then `JSON.parse`.", "prevention": "Every blob request's catch block must decode the Blob.", "detection": [{"kind": "ast", "recipe": "call with responseType 'blob' whose catch does not call .text()/the async reader"}], "evidence": ["4eff813d5, 5eb0efa57 (2026-09-04)"], "sources": []}, {"id": "AC-005", "category": "api-contract", "title": "FormData sent through an instance whose default Content-Type is JSON is JSON-stringified", "stacks": ["axios"], "severity": "high", "symptom": "Every upload returned 422 because the request had no file part.", "root_cause": "axios `transformRequest` sees a JSON content type and runs `JSON.stringify(formDataToJSON(data))`.", "resolution": "Don't hardcode `Content-Type: application/json` on the shared instance, or override it per multipart call.", "prevention": "Test uploads through the real client instance.", "detection": [{"kind": "ast", "recipe": "api.(post|put)(url, <FormData>) on an instance created with headers Content-Type application/json and no per-call override"}], "evidence": ["5eb0efa57"], "sources": []}, {"id": "AC-006", "category": "api-contract", "title": "Classifying 403s by status code alone collapses different refusals", "stacks": ["axios", "api-design"], "severity": "medium", "symptom": "'Feature disabled', 'outside your appointment' and 'role not allowed' all rendered as one generic 403. Removing the interceptor branch kept the suite green.", "root_cause": "The client keyed off `status === 403` instead of the backend's typed error code.", "resolution": "One `classifyApiError()` owner; the interceptor attaches the classification to every failure, with static tests asserting the interceptor still calls it.", "prevention": "Pages never compare error-code literals themselves.", "detection": [{"kind": "regex", "recipe": "status\\s*===\\s*403 (outside the classifier)"}], "evidence": ["ac6f11f39 (2026-08-29)"], "sources": []}, {"id": "AC-007", "category": "api-contract", "title": "A page reads a key the API never emits, and renders nothing", "stacks": ["typescript", "react", "api-contract"], "severity": "high", "symptom": "One tab was empty for every tenant. A countdown never rendered. No error anywhere.", "root_cause": "The component read `data.capital_outlook`, which only an older engine emitted, and `voting_closes_at`, where the writer uses `voting_deadline`. An undefined key and a legitimately empty state look identical. TypeScript cannot catch it when the response is typed `any` or lives in `.jsx`.", "resolution": "Extracted the keys each JSX dereferences and asserted that the backend emits each one. Aligned key sets across implementations.", "prevention": "Generate client types from the OpenAPI schema, or contract-test recorded fixtures.", "detection": [{"kind": "ast", "recipe": "collect data?.X / props.x.y reads per component, diff against OpenAPI response schema keys"}], "evidence": ["360fdb1d7 (2026-09-05)", "71c59b20f (2026-09-06)", "3f600c11b (2026-09-12)", "4170d67c7"], "sources": []}, {"id": "AC-008", "category": "api-contract", "title": "A request body carries fields the server silently drops, or omits a required one", "stacks": ["api-contract", "pydantic"], "severity": "high", "symptom": "Voting always returned 422 because `lot_id` was missing. `estimated_cost` was discarded by `extra='ignore'`, so the user's value was never stored.", "root_cause": "Client payloads and server request models drifted. The server ignored unknown keys instead of rejecting them.", "resolution": "Fixed the payloads, and use `extra='forbid'` on request models so a typo fails loudly.", "prevention": "Compare literal request bodies against the OpenAPI request schema.", "detection": [{"kind": "ast", "recipe": "api.post(url, {literal keys}) compared against the OpenAPI requestBody: unknown keys and missing required keys"}], "evidence": ["71c59b20f"], "sources": []}, {"id": "AC-009", "category": "api-contract", "title": "A root-relative asset URL from the API resolves against the frontend origin", "stacks": ["nextjs", "html"], "severity": "medium", "symptom": "`<img src='/api/settings/logo/…'>` 404'd silently. `src=\"\"` made some browsers re-request the current page.", "root_cause": "A backend-relative path is resolved by the browser against the Next.js origin.", "resolution": "One URL-resolver helper that prefixes the backend URL and returns null for absent values.", "prevention": "Never render `src=\"\"`.", "detection": [{"kind": "regex", "recipe": "src=\\\"\\\"|src=\\{[^}]*\\|\\|\\s*''\\}"}], "evidence": ["4eff813d5"], "sources": []}, {"id": "AC-010", "category": "api-contract", "title": "Missing data rendered as a confident zero or verdict", "stacks": ["react", "typescript", "ux"], "severity": "high", "symptom": "An unmeasured score rendered red on one screen and `Healthy` on another (`?? 100`). A fallback rate of `?? 0.035` was shown as fact. `amount || 0` appeared at 456 sites.", "root_cause": "`??`/`||` defaults turn 'unknown' into a number, and `undefined >= 80` is false.", "resolution": "The scoring helpers return an explicit `'unmeasured'` member, and missing values render as an em dash.", "prevention": "Zero and missing are different states in every layer: API, formatter and UI.", "detection": [{"kind": "regex", "recipe": "\\?\\?\\s*\\d|\\|\\|\\s*0\\b  (on numeric display fields)"}, {"kind": "ast", "recipe": ">=|<= comparisons on possibly-undefined props"}], "evidence": ["feefe5259 (2026-09-09)", "21771129c", "de843f769"], "sources": []}, {"id": "AC-011", "category": "api-contract", "title": "`.catch(() => {})` plus a loading fallback means a failure looks like loading forever", "stacks": ["react"], "severity": "medium", "symptom": "A page sat on 'Loading…' permanently when the request failed.", "root_cause": "The error was swallowed, and render only distinguished data from no data.", "resolution": "Model three states (loading, error, data) and render the error.", "prevention": "Ban empty catch handlers in UI code.", "detection": [{"kind": "regex", "recipe": "\\.catch\\(\\s*\\(\\)\\s*=>\\s*\\{\\s*\\}\\s*\\)|catch\\s*(\\([^)]*\\))?\\s*\\{\\s*\\}"}], "evidence": ["4764b8394 (2026-09-02)"], "sources": []}, {"id": "AC-012", "category": "api-contract", "title": "Hardcoded 'now': the year, relative labels, currency formatting", "stacks": ["javascript"], "severity": "medium", "symptom": "`CURRENT_YEAR = 2026`, a literal '3.5 yrs ago', `` `$${(v/1000).toFixed(0)}k` `` on 12 chart axes, and an off-by-one financial year.", "root_cause": "Values that change over time or by locale were written as literals.", "resolution": "Derive dates from the clock, and route every money display through one formatter owner.", "prevention": "Money detectors must match the AST (see GT-008), not single lines.", "detection": [{"kind": "regex", "recipe": "\\b(19|20)\\d{2}\\b assigned to a year-like identifier"}, {"kind": "regex", "recipe": "[`'\\\"]\\$\\$\\{|>\\s*\\$\\$\\{"}, {"kind": "regex", "recipe": "Intl\\.NumberFormat\\('en-[A-Z]{2}' (outside the owner)"}], "evidence": ["21771129c", "6cb1e8cec", "9ccab84da", "6f6a4ab7f", "retirement_calculator_au (2026-09-20): the un-escaped '>${' arm matched 20 chart/label interpolations and no dates"], "sources": []}, {"id": "AC-013", "category": "api-contract", "title": "A field name that states neither its unit nor its time base", "stacks": ["javascript", "api-contract"], "severity": "high", "symptom": "`estateAtLifespan` held nominal dollars when one code path wrote it and inflation-adjusted present-value dollars when another did, and both wrote the same key into the same persisted payload. Comparing it against a present-value target overstated it by the plan's whole inflation factor. Separately, a balance measured at the planning age was discounted over the years-to-retirement instead, overstating it 1.9x.", "root_cause": "The name said WHEN the value is measured but not WHAT it is measured in, and no single function owned the conversion. Two producers of \"the same\" field disagreed, and nothing compared them.", "resolution": "Put both the unit and the time base in the name, convert in exactly one place, and derive a discount horizon from the two ages rather than reusing whichever span variable is already in scope.", "prevention": "Two quantities that cannot be added are not the same field. Name them apart, and keep the conversion at one boundary.", "detection": [{"kind": "review", "recipe": "grep every writer of a shared payload key and confirm they agree on units"}, {"kind": "test", "recipe": "assert a known input yields the documented unit on every path that writes the field"}], "evidence": ["retirement_calculator_au (2026-09-20 audit): reverse planner and the forward-projection bridge wrote estateAtLifespan in different units"], "sources": []}, {"id": "TS-001", "category": "typescript-js", "title": "A wrong prop name in `.jsx` gives the child `undefined`, and gated UI disappears", "stacks": ["jsx", "typescript", "allowJs"], "severity": "high", "symptom": "Every admin block rendered nothing: the caller passed `canEdit` where the prop is `canAdmin`.", "root_cause": "`.jsx` callers are not type-checked against the props a `.tsx` component declares.", "resolution": "Fixed the prop, and converted the callers to `.tsx`.", "prevention": "Enable `checkJs`, or convert callers of typed components to TypeScript.", "detection": [{"kind": "ast", "recipe": "JSX attributes on a component with a TS prop type that are not declared props"}], "evidence": ["d6f2bb915 (2026-09-03)"], "sources": []}, {"id": "TS-002", "category": "typescript-js", "title": "`// @ts-nocheck` hid a ReferenceError", "stacks": ["typescript"], "severity": "high", "symptom": "Both export buttons threw at runtime. JSX referenced state that was never declared.", "root_cause": "The file opted out of type checking, so undeclared identifiers compiled.", "resolution": "Removed `@ts-nocheck` and declared the state.", "prevention": "Ratchet the count of `@ts-nocheck`/`@ts-ignore`, and run ESLint `no-undef` on JS/JSX.", "detection": [{"kind": "regex", "recipe": "@ts-nocheck|@ts-ignore"}], "evidence": ["5eb0efa57", "4170d67c7 (2026-06-09)"], "sources": []}, {"id": "TS-003", "category": "typescript-js", "title": "`any`, `as` and `JSON.parse` defeat the type system", "stacks": ["typescript"], "severity": "high", "symptom": "Type-correct code crashes on data of the wrong shape.", "root_cause": "`JSON.parse`, `res.json()` and axios data are `any` (or a lie via a generic), and `as` asserts without checking.", "resolution": "Validate at the boundary (zod/valibot) and narrow from `unknown`.", "prevention": "`strict`, `noUncheckedIndexedAccess`, typescript-eslint `no-explicit-any` and `no-unsafe-*`.", "detection": [{"kind": "regex", "recipe": ":\\s*any\\b|as any|as unknown as"}, {"kind": "config", "recipe": "tsconfig strict: true"}], "evidence": [], "sources": ["https://typescript-eslint.io/rules/no-unsafe-assignment/", "https://www.typescriptlang.org/tsconfig/#noUncheckedIndexedAccess"]}, {"id": "TS-004", "category": "typescript-js", "title": "Floating-point money", "stacks": ["javascript"], "severity": "critical", "symptom": "`0.1+0.2 !== 0.3`; `(1.005).toFixed(2) === '1.00'`; sums of dollar floats drift by cents.", "root_cause": "IEEE-754 binary floats cannot represent most decimal fractions exactly.", "resolution": "Carry integer cents (or a decimal library) end to end, and format only at display.", "prevention": "Name money variables `*_cents` and convert once at the boundary.", "detection": [{"kind": "regex", "recipe": "parseFloat\\(.*(amount|price|total|balance|salary|cost|income)"}, {"kind": "ast", "recipe": "arithmetic on a money-typed binding, not a formatting call on one"}], "evidence": ["retirement_calculator_au (2026-09-20): every one of 20 'toFixed(2)' matches was display formatting; none was money arithmetic"], "sources": ["https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number/toFixed"]}, {"id": "TS-005", "category": "typescript-js", "title": "`new Date('YYYY-MM-DD')` parses as UTC, but `'YYYY-MM-DDTHH:mm'` parses as local", "stacks": ["javascript", "time"], "severity": "high", "symptom": "Due dates showed the previous day for users in negative-offset time zones.", "root_cause": "Per the ECMAScript spec, a date-only ISO string is UTC midnight while a date-time string without an offset is local time.", "resolution": "Treat date-only values as strings or construct them with `new Date(y, m-1, d)`, and always pass `timeZone` to `Intl.DateTimeFormat`.", "prevention": "Keep business dates as plain `date` values end to end.", "detection": [{"kind": "regex", "recipe": "new Date\\(['\\\"`]?\\d{4}-\\d{2}-\\d{2}['\\\"`]?\\)|new Date\\(\\w+\\.(due_date|date)\\)"}], "evidence": [], "sources": ["https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/Date#date_time_string_format"]}, {"id": "TS-006", "category": "typescript-js", "title": "`Array.prototype.sort()` is lexicographic and mutates in place", "stacks": ["javascript"], "severity": "medium", "symptom": "`[10,9,1].sort()` returns `[1,10,9]`, and `'TH10'` sorts before `'TH2'`.", "root_cause": "The default comparator compares strings.", "resolution": "Use `(a,b)=>a-b` or `Intl.Collator(undefined,{numeric:true}).compare`, and `toSorted()` to avoid mutating the input.", "prevention": "Mirror of the database lesson MG-003.", "detection": [{"kind": "regex", "recipe": "\\.sort\\(\\)"}], "evidence": ["retirement_calculator_au (2026-09-20): Object.keys(versions).sort().reverse() ranked '2026.1' above '2.4.0'"], "sources": ["https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/sort"]}, {"id": "TS-007", "category": "typescript-js", "title": "`async` inside `forEach` is not awaited; floating promises crash Node", "stacks": ["javascript", "node"], "severity": "high", "symptom": "Work ran out of order, and errors surfaced as unhandled rejections that killed the process.", "root_cause": "`forEach` ignores returned promises. Since Node 15 an unhandled rejection throws by default.", "resolution": "Use `for…of` with `await`, or `await Promise.all(arr.map(f))` / `allSettled`.", "prevention": "typescript-eslint `no-misused-promises` and `no-floating-promises`.", "detection": [{"kind": "regex", "recipe": "forEach\\(async"}, {"kind": "lint", "recipe": "@typescript-eslint/no-floating-promises: error"}], "evidence": [], "sources": ["https://typescript-eslint.io/rules/no-misused-promises/", "https://nodejs.org/api/cli.html#--unhandled-rejectionsmode"]}, {"id": "TS-008", "category": "typescript-js", "title": "`||` versus `??`, and `==`", "stacks": ["javascript"], "severity": "medium", "symptom": "A genuine zero balance was replaced by the default value.", "root_cause": "`x || d` also replaces `0`, `''` and `false`. `==` coerces.", "resolution": "Use `??` for defaults and `===` everywhere.", "prevention": "ESLint `eqeqeq` and `prefer-nullish-coalescing`.", "detection": [{"kind": "regex", "recipe": "(amount|balance|count|total|cents)\\s*\\|\\|"}], "evidence": [], "sources": ["https://eslint.org/docs/latest/rules/eqeqeq"]}, {"id": "TS-009", "category": "typescript-js", "title": "Shallow spread copies, and a lossy `JSON` round-trip", "stacks": ["javascript"], "severity": "low", "symptom": "Nested state mutated through a 'copy'. Dates became strings.", "root_cause": "Spread is shallow. `JSON.parse(JSON.stringify(x))` drops `undefined`, stringifies dates, loses Map/Set and throws on BigInt.", "resolution": "Use `structuredClone()`.", "prevention": "Grep for the JSON round-trip idiom.", "detection": [{"kind": "regex", "recipe": "JSON\\.parse\\(JSON\\.stringify"}], "evidence": [], "sources": ["https://developer.mozilla.org/en-US/docs/Web/API/Window/structuredClone"]}, {"id": "TS-010", "category": "typescript-js", "title": "Integers above 2^53 lose precision in JSON", "stacks": ["javascript", "api-contract"], "severity": "medium", "symptom": "A `bigint` identifier or amount from the database came back altered in the browser.", "root_cause": "JSON numbers become IEEE doubles, and `Number.MAX_SAFE_INTEGER` is 2^53-1.", "resolution": "Serialise big integers as strings in the API.", "prevention": "Review response models whose int fields map to BIGINT columns.", "detection": [{"kind": "review", "recipe": "int fields mapped to BIGINT in API responses"}], "evidence": [], "sources": ["https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number/MAX_SAFE_INTEGER"]}, {"id": "TS-011", "category": "typescript-js", "title": "`Number.isFinite(Number(x))` accepts null, '' and [] as zero", "stacks": ["javascript", "typescript"], "severity": "high", "symptom": "A confidence that had not been measured yet rendered as a measured `0%` — the worst possible reading of a missing value. The guard looked like a validity check.", "root_cause": "`Number(null)`, `Number('')`, `Number(' ')`, `Number([])` and `Number(false)` are all `0`, and `0` is finite. `Number.isFinite` answers \"is this a finite number\", never \"did the caller supply one\".", "resolution": "Test the input rather than its coercion: `typeof x === 'number' && Number.isFinite(x)`, or reject nullish and empty explicitly before coercing. Render a missing value as \"not assessed\", not as zero.", "prevention": "\"Has a value been supplied\" and \"is the value in range\" are two questions; a parse helper that answers only the second needs a nullish guard in front of it.", "detection": [{"kind": "regex", "recipe": "Number\\.isFinite\\(\\s*(Number|parseFloat|parseInt)\\("}, {"kind": "review", "recipe": "a coerce-then-range-check helper reached by a value that may be null"}], "evidence": ["retirement_calculator_au (2026-09-20 audit): parseConfidence(null) returned 0, which reached the comparison table as '0%'"], "sources": ["https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number/isFinite"]}, {"id": "TS-012", "category": "typescript-js", "title": "A bounds check that is always true makes the rest of the condition dead", "stacks": ["javascript", "typescript"], "severity": "high", "symptom": "`const passes = target <= 1 || (measured !== undefined ? measured >= target : true)`. A probability is always `<= 1`, so the right-hand side never ran and the user's confidence requirement was ignored on every code path. It had shipped, with tests — all of which asserted the `true` result.", "root_cause": "The guard was written against the wrong bound. The author meant \"no target was set\" (`<= 0`) and wrote \"this is a probability\" (`<= 1`).", "resolution": "Correct the bound, and separate \"the condition does not apply\" from \"the condition passed\" so the caller can tell which happened.", "prevention": "For every short-circuit guard, write the test that makes the right-hand side matter. A branch no test can make false is dead code, and a suite that only asserts the passing result cannot see it.", "detection": [{"kind": "regex", "recipe": "<=\\s*1(\\.0)?\\s*\\|\\||>=\\s*0(\\.0)?\\s*\\|\\|"}, {"kind": "test", "recipe": "per short-circuit guard, one case making the left operand false and asserting on the right"}], "evidence": ["retirement_calculator_au (2026-09-20 audit): evaluateEngineGoal's passesConfidence returned true for a 99% target against a measured 1%"], "sources": []}, {"id": "FT-001", "category": "frontend-testing", "title": "A mock returning a new object per call plus a dependent effect makes Jest hang forever", "stacks": ["jest", "react"], "severity": "high", "symptom": "'Maximum update depth exceeded', then a worker that never exits.", "root_cause": "`jest.mock(..., () => ({ useX: () => ({ ... }) }))` returns a fresh object on every render. An effect that depends on it re-runs indefinitely.", "resolution": "Hoist mock return values to constants so they are referentially stable.", "prevention": "To tell a hang from an out-of-memory failure, watch CPU time (`ps -o time=`).", "detection": [{"kind": "regex", "recipe": "jest\\.mock\\([^)]*,\\s*\\(\\)\\s*=>\\s*\\(\\{\\s*use\\w+:\\s*\\(\\)\\s*=>\\s*\\(\\{"}], "evidence": ["tasks/lessons.md (2026-08-25)"], "sources": []}, {"id": "FT-002", "category": "frontend-testing", "title": "Order-keyed `mockResolvedValueOnce` chains shift when a page adds a fetch", "stacks": ["jest"], "severity": "low", "symptom": "Unrelated tests broke when a component gained one extra request.", "root_cause": "The mock answered by call order, not by URL.", "resolution": "Route mocks by exact URL.", "prevention": "Prefer URL-keyed mock routers, and match exactly, not with `includes`.", "detection": [{"kind": "regex", "recipe": "(\\.mockResolvedValueOnce\\([^)]*\\)\\s*){3,}"}], "evidence": ["3d6b2935d"], "sources": []}, {"id": "FT-003", "category": "frontend-testing", "title": "A hook mock missing a helper looks like a component bug", "stacks": ["jest"], "severity": "low", "symptom": "A TypeError in the component under test, caused by the mock.", "root_cause": "The hand-written auth mock lacked a function that the component calls.", "resolution": "A single shared, complete mock factory.", "prevention": "Build mocks from the real module's export list.", "detection": [{"kind": "review", "recipe": "per-test partial mocks of shared hooks"}], "evidence": ["3d6b2935d"], "sources": []}, {"id": "FT-004", "category": "frontend-testing", "title": "Tests with hardcoded dates expire", "stacks": ["jest", "time"], "severity": "low", "symptom": "A test started failing on 2026-09-02 with no code change.", "root_cause": "Fixture dates compared against logic that uses 'today'.", "resolution": "Use fake timers or relative dates.", "prevention": "Flag literal ISO dates in fixtures used with date-relative logic.", "detection": [{"kind": "regex", "recipe": "['\\\"]20\\d{2}-\\d{2}-\\d{2}['\\\"] in test files that do not call useFakeTimers/setSystemTime"}], "evidence": ["3d6b2935d"], "sources": []}, {"id": "FT-005", "category": "frontend-testing", "title": "The suite passes on a dirty `node_modules`", "stacks": ["jest", "ci"], "severity": "medium", "symptom": "`jest.mock('@removed/pkg')` passed locally after the package had been removed from package.json.", "root_cause": "The package was still installed on the developer machine.", "resolution": "Run CI from a clean install.", "prevention": "Every `jest.mock('<pkg>')` must name a declared dependency.", "detection": [{"kind": "shell", "recipe": "for each jest.mock('<pkg>') literal, assert <pkg> is in package.json deps/devDeps"}], "evidence": ["dee235489 (2026-08-25)"], "sources": []}, {"id": "FT-006", "category": "frontend-testing", "title": "Jest configuration breakage fails silently", "stacks": ["jest"], "severity": "medium", "symptom": "The `jest` block was dropped from package.json with no error. Tests outside the frontend folder could not resolve `react/jsx-runtime`. `--testPathPattern` began exiting 1.", "root_cause": "`moduleDirectories` takes directory names; `modulePaths` is the correct option for absolute paths. Jest 30 renamed `--testPathPattern` to `--testPathPatterns`, removed alias matchers (`toBeCalled`, `toThrowError`), and jsdom 26 forbids reassigning `window.location`.", "resolution": "Fixed the configuration and updated the docs and scripts.", "prevention": "In CI, assert that `jest --listTests` returns a non-zero count that does not drop.", "detection": [{"kind": "regex", "recipe": "--testPathPattern\\b|toBeCalled\\b|toThrowError|window\\.location\\s*="}, {"kind": "ci", "recipe": "jest --listTests | wc -l >= baseline"}], "evidence": ["b1c90c60e, 07359040c (2026-07-06)", "e9bf6a8d3"], "sources": ["https://jestjs.io/docs/upgrading-to-jest30"]}, {"id": "FT-007", "category": "frontend-testing", "title": "A patch bump exposes a latent async race in RTL tests", "stacks": ["rtl", "react19"], "severity": "low", "symptom": "Tests asserted content set in an effect synchronously after a `waitFor`, and began failing after a React patch release.", "root_cause": "Timing changed, and a duplicate text node made `getByText` throw.", "resolution": "Use `findBy*`, or assert inside `waitFor`.", "prevention": "Flag `getBy*` assertions on effect-populated content outside `waitFor`.", "detection": [{"kind": "review", "recipe": "getBy* for effect-set content outside waitFor/findBy"}], "evidence": ["bffcd63ac (2026-08-10)"], "sources": []}, {"id": "FT-008", "category": "frontend-testing", "title": "Radix UI and Recharts need specific handling in jsdom", "stacks": ["rtl", "radix", "shadcn", "recharts"], "severity": "low", "symptom": "Tab clicks did nothing, dialogs never appeared, tooltips threw, and charts rendered empty.", "root_cause": "Radix `TabsTrigger` activates on `mousedown`. Dialogs portal and animate. `Tooltip` requires a provider. `ResponsiveContainer` measures -1 in jsdom.", "resolution": "Use `fireEvent.mouseDown` for tabs, `waitFor` dialog content, wrap renders in `TooltipProvider`, and pass `minWidth`/`minHeight` to charts.", "prevention": "Keep these in a shared test-render helper.", "detection": [{"kind": "ast", "recipe": "fireEvent.click on an element located by role 'tab'"}, {"kind": "review", "recipe": "Tooltip import in a component under test with no provider in the wrapper"}], "evidence": ["frontend/CLAUDE.md", "4b60f76c1"], "sources": []}, {"id": "FT-009", "category": "frontend-testing", "title": "`not.toThrow()` passes on broken error-reporting code", "stacks": ["jest", "jsdom"], "severity": "low", "symptom": "A test of the global error reporter passed while the reporter was broken.", "root_cause": "jsdom swallows exceptions thrown inside event listeners. Also, `String(x)` is not total: `Object.create(null)` throws.", "resolution": "Assert that the report arrived, not that nothing threw.", "prevention": "Assert outcomes, not the absence of exceptions.", "detection": [{"kind": "regex", "recipe": "expect\\([^)]*\\)\\.not\\.toThrow\\(\\)"}], "evidence": ["a19c7ae97 (2026-09-07)"], "sources": []}, {"id": "FT-010", "category": "frontend-testing", "title": "Text-oracle tests break on harmless refactors", "stacks": ["jest"], "severity": "low", "symptom": "`includes(key)` matched `status` inside `status-finance-routes`. A regex required a prop to come first. A `.tsx` path was truncated to `.ts`.", "root_cause": "Source-scan tests used substring and regex matching on JSX source.", "resolution": "Parse with a real parser, or match tokens exactly.", "prevention": "Prefer AST checks for source-scan tests.", "detection": [{"kind": "review", "recipe": "tests that read source files and use .includes()/regex on JSX"}], "evidence": ["e71c7f1bf", "3d6b2935d (2026-09-03)"], "sources": []}, {"id": "FT-011", "category": "frontend-testing", "title": "Playwright pitfalls: missing APIs, redirects, drifting locators, rate limits, committed auth state", "stacks": ["playwright"], "severity": "medium", "symptom": "A spec called `request.options()`, which does not exist. Trailing-slash paths returned 307 instead of the expected status. `xpath=ancestor::div[1]` locators drifted silently. Parallel workers tripped login rate limits.", "root_cause": "Assumptions about the API and the app that nobody verified; shared credentials across workers. A spec that has never been executed is not a test.", "resolution": "Use `request.fetch({method:'OPTIONS'})`, canonical paths, role/test-id locators, and log in once in global setup (per-worker users where needed). Gitignore `playwright/.auth`, because storageState files hold live cookies.", "prevention": "Prefer web-first assertions over `waitForTimeout`.", "detection": [{"kind": "regex", "recipe": "xpath=ancestor::|request\\.options\\(|waitForTimeout|page\\.\\$\\("}], "evidence": ["e7f266b39", "7c8e9c45a (2026-06-28)"], "sources": ["https://playwright.dev/docs/best-practices", "https://playwright.dev/docs/auth"]}, {"id": "FT-012", "category": "frontend-testing", "title": "Playwright is installed in a different package from the app", "stacks": ["playwright", "npm", "yarn"], "severity": "medium", "symptom": "`npx playwright test --list` failed with a missing module on a freshly provisioned machine.", "root_cause": "Playwright lives in the root `package.json` (npm), while the app lives in `frontend/` (yarn). The installer only ran `yarn install` in `frontend/`. Bumping Playwright without `playwright install` leaves a browser-build mismatch and a silent hang.", "resolution": "Documented `npm ci && npx playwright install chromium` at the root.", "prevention": "Provisioning scripts install every package root, and run a post-install `playwright install`.", "detection": [{"kind": "shell", "recipe": "every directory with a package.json that CI uses is installed by the provisioning script"}], "evidence": ["CLAUDE.md §E2E"], "sources": []}, {"id": "JD-001", "category": "js-dependencies", "title": "Yarn 1 `yarn upgrade <pkg>` is a silent no-op for transitive dependencies", "stacks": ["yarn"], "severity": "medium", "symptom": "A security alert 'fix' exited successfully and changed nothing.", "root_cause": "Yarn 1 only upgrades direct dependencies with that command.", "resolution": "Delete the stale `yarn.lock` entries and run `yarn install` to re-resolve within the existing ranges.", "prevention": "After an alert fix, verify with `yarn why <pkg>` and check that `yarn.lock` actually changed.", "detection": [{"kind": "ci", "recipe": "after a dependency-fix PR, assert the named package's lock entry changed"}], "evidence": ["55b6da2e5 (2026-09-11)", "5e64313af (2026-09-07)"], "sources": []}, {"id": "JD-002", "category": "js-dependencies", "title": "Stale or contradictory `resolutions` / `overrides` pins", "stacks": ["yarn", "npm"], "severity": "low", "symptom": "Nine packages were pinned twice (yarn and npm). One pin contradicted its parent's declared range and was silently ignored.", "root_cause": "Pins accumulate and are never re-derived.", "resolution": "Removed the redundant and contradicted pins.", "prevention": "For each pin, compare against every parent's declared range: flag 'already satisfied' and 'unsatisfiable'.", "detection": [{"kind": "shell", "recipe": "for each resolutions/overrides entry: npm ls / yarn why and compare against parent ranges"}], "evidence": ["f7768c14c (2026-08-25)"], "sources": []}, {"id": "JD-003", "category": "js-dependencies", "title": "Two lockfiles for one manifest, or a hand-edited package.json, break frozen installs", "stacks": ["yarn", "npm"], "severity": "medium", "symptom": "`--frozen-lockfile` failed in CI.", "root_cause": "`package.json` was edited without re-locking, and both `package-lock.json` and `yarn.lock` existed.", "resolution": "Keep one package manager and one lockfile per directory.", "prevention": "CI runs `yarn install --frozen-lockfile` / `npm ci`.", "detection": [{"kind": "shell", "recipe": "a directory containing both package-lock.json and yarn.lock"}], "evidence": ["0bfa6dc1a", "b90606cd6 (2026-09-01)"], "sources": []}, {"id": "JD-004", "category": "js-dependencies", "title": "Dependabot groups by directory, so a dependency declared in two manifests drifts", "stacks": ["dependabot", "mermaid"], "severity": "medium", "symptom": "The diagram-lint gate parsed with mermaid v11 while the browser rendered with v12, so the gate went green on syntax the browser could not draw.", "root_cause": "The root manifest (lint) and `frontend/` (runtime) each declared mermaid, and Dependabot bumped only one.", "resolution": "Bump both copies in one commit, with a warning comment in the workflow.", "prevention": "Fail CI when a package appears in several manifests with different major versions, and especially when a gate's version differs from the runtime's.", "detection": [{"kind": "shell", "recipe": "jq -r '.dependencies.mermaid // .devDependencies.mermaid' package.json frontend/package.json | cut -d. -f1 | sort -u | wc -l  # must be 1"}], "evidence": ["8257196fe", "8e7449186 (2026-09-15)"], "sources": []}, {"id": "JD-005", "category": "js-dependencies", "title": "Minor bumps break types that mocked tests cannot see", "stacks": ["recharts", "nextjs", "typescript"], "severity": "high", "symptom": "A recharts minor release widened the Tooltip formatter's types (`ValueType`), and Next 16.3 needed a newer `@swc/helpers`. Jest mocked recharts, so only `tsc` / `next build` caught it.", "root_cause": "A caret range accepted a breaking minor release.", "resolution": "Used exact pins to hold versions back, and added a Dependabot `ignore` entry that states its reason. Introduced a shared `ChartValue` type.", "prevention": "Every dependency PR must pass a clean `next build`.", "detection": [{"kind": "ast", "recipe": "formatter callbacks annotated (v: number) passed to recharts formatter"}], "evidence": ["3ca08b858", "f8209b7e6 (2026-08-18)", "7d6a0e283 (2026-09-01)"], "sources": []}, {"id": "JD-006", "category": "js-dependencies", "title": "Major bumps silently change defaults, attributes and module paths", "stacks": ["react-pdf", "mermaid", "react-resizable-panels"], "severity": "medium", "symptom": "react-pdf 11 enabled Suspense by default. mermaid 12 switched layout engine and look. react-resizable-panels 4 renamed exports and dropped `data-panel-group-direction`, so Tailwind `data-[…]` selectors became dead. A copy script with a hardcoded nested `node_modules/x/node_modules/y` path broke when the dependency was hoisted.", "root_cause": "Defaults and DOM attributes are part of a library's contract but are not type-checked.", "resolution": "Read the changelog and resolve paths with `require.resolve(dep, {paths:[require.resolve(consumer + '/package.json')]})`.", "prevention": "Grep for hardcoded nested `node_modules` paths, and for `data-[attr]` selectors whose attribute is no longer emitted.", "detection": [{"kind": "regex", "recipe": "node_modules/[^/]+/node_modules/"}, {"kind": "regex", "recipe": "data-\\[[a-z-]+"}], "evidence": ["8e7449186", "7d6a0e283"], "sources": []}, {"id": "JD-007", "category": "js-dependencies", "title": "Removing a UI library: runtime-generated classes, same-named components, duplicate majors", "stacks": ["tailwind", "tremor", "shadcn"], "severity": "low", "symptom": "A 475-line CSS block hand-declared the removed library's classes. Tremor `TableHead` is `<thead>` while shadcn `TableHead` is `<th>`. A prefix replace produced `<TableHeadererCell`. The old library had pulled in two majors of a charting library.", "root_cause": "Library ports assume same names mean the same semantics. Naive codemods replace the shorter name first.", "resolution": "Mapped each component explicitly, replacing the longest names first.", "prevention": "Check the lockfile for duplicate majors of one package.", "detection": [{"kind": "shell", "recipe": "yarn why <pkg> | grep -c 'Found' (more than one major)"}], "evidence": ["8d7419b26", "0bfa6dc1a", "bb66813f6 (2026-06-28)"], "sources": []}, {"id": "JD-008", "category": "js-dependencies", "title": "npm resolves a peer conflict by overriding it, and only warns", "stacks": ["npm", "node", "dependencies"], "severity": "high", "symptom": "`npm install pkg@latest` printed 15 `npm warn ERESOLVE overriding peer dependency` lines and exited 0. The tree then held a second, older copy of the conflicting package nested under the offender, and `npm ls --all` reported it `invalid:`.", "root_cause": "npm 7+ installs peer dependencies automatically and, when they conflict, prefers completing the install to failing. The result is a tree that does not match what the manifests ask for, announced only in a warning that scrolls past in CI output.", "resolution": "Treat an ERESOLVE warning as a failure. Either hold the dependency at the last version that resolves cleanly, or wait for the upstream package to widen its peer range — the block is usually one transitive package pinning an old major.", "prevention": "Gate on a clean resolve, not on the exit code. `--legacy-peer-deps` and an `overrides` block silence the same problem without fixing it.", "detection": [{"kind": "shell", "recipe": "rm -rf node_modules && npm ci 2>&1 | grep -c 'ERESOLVE\\|Conflicting peer'"}, {"kind": "shell", "recipe": "npm ls --all 2>&1 | grep -c 'invalid:'"}, {"kind": "regex", "recipe": "legacy-peer-deps|\\\"overrides\\\"\\s*:"}], "evidence": ["retirement_calculator_au (2026-09-20 audit): @babel/core 8 held at 7 because babel-preset-current-node-syntax still depends on @babel/plugin-syntax-*@^7"], "sources": ["https://docs.npmjs.com/cli/v10/commands/npm-install"]}, {"id": "AX-001", "category": "accessibility", "title": "A composite ARIA role without the children the role requires", "stacks": ["accessibility", "html"], "severity": "high", "symptom": "`role=\"tablist\"` on a div whose children were plain buttons with no `role=\"tab\"`, `aria-selected` or `aria-controls`, and whose panels were plain divs. A screen reader announced a tab list containing no tabs and never said which view was showing. On another page the same role wrapped a single button that controlled nothing.", "root_cause": "The container role was added for the name it gives, without the owned elements it requires. `tablist`, `radiogroup`, `menu`, `listbox`, `tree` and `grid` all require specific child roles; a container without them is worse than no role, because it replaces a working generic with a broken widget.", "resolution": "Apply the roles, ids and selected state from one function at init so the buttons, the panels and the selection cannot drift apart, and add a roving tabindex with arrow/Home/End keys. Where the thing is not really a tab list, delete the role: a set of toggles is `role=\"group\"` with `aria-pressed`, and a single label is a heading.", "prevention": "A container role is a promise about its children. Adding one means adding all of them.", "detection": [{"kind": "regex", "recipe": "role=[\\\"'](tablist|radiogroup|menu|menubar|listbox|tree|grid)[\\\"']"}, {"kind": "review", "recipe": "per container role, confirm every required child role is present and kept in sync"}], "evidence": ["retirement_calculator_au (2026-09-20 audit): two pages carried role=tablist with no role=tab children; WCAG 2.0 SC 4.1.2"], "sources": ["https://www.w3.org/WAI/ARIA/apg/patterns/tabs/"]}, {"id": "AX-002", "category": "accessibility", "title": "A toast injected into the DOM with no live region", "stacks": ["accessibility", "javascript"], "severity": "high", "symptom": "A shared notification helper created a styled div, appended it to `body` and removed it after five seconds. Screen-reader users got no feedback for any action on any page — including \"this action could not be completed\".", "root_cause": "The banner conveys its meaning through position and colour. Nothing tells assistive technology that the DOM changed, and the element is never focused, so nothing reads it.", "resolution": "`role=\"alert\"` with `aria-live=\"assertive\"` for errors and warnings, `role=\"status\"` with `aria-live=\"polite\"` for confirmations, plus `aria-atomic=\"true\"`. Put it in the shared helper, not at the call sites — one edit then covers every page.", "prevention": "Anything that appears in response to an action, and does not take focus, needs a live role. An error conveyed only visually also fails SC 3.3.1.", "detection": [{"kind": "regex", "recipe": "className\\s*=\\s*[`'\\\"]?(notification|toast|snackbar|flash)"}, {"kind": "review", "recipe": "per element appended in response to an action, a live role or a focus move"}], "evidence": ["retirement_calculator_au (2026-09-20 audit): utils.js showNotification appended an unannounced div for success and error alike"], "sources": ["https://www.w3.org/WAI/WCAG21/Understanding/error-identification.html"]}, {"id": "AX-003", "category": "accessibility", "title": "Contrast verified in the default theme only", "stacks": ["accessibility", "css"], "severity": "medium", "symptom": "A text token used for small labels measured 4.12:1 in the light theme (large text only) and 2.81:1 in the dark theme — below even the 3:1 large-text floor. A third theme failed at 3.34:1. The shipped default theme passed, so nobody saw any of it.", "root_cause": "Contrast was checked once, against one background. A token redefined in each theme block needs checking against THAT theme's background, and against the card surface as well as the page background, which are rarely the same colour.", "resolution": "Compute the ratio for every (token, theme background) pair and require 4.5:1 for normal text and 3:1 for large. Darken or lighten the token in place — one value per theme fixes every usage at once.", "prevention": "Make the check a test beside the tokens, and fail when a new theme block introduces a token the table does not cover, so a theme cannot be added without being measured.", "detection": [{"kind": "regex", "recipe": "\\[data-theme=|\\[data-contrast=|prefers-color-scheme"}, {"kind": "test", "recipe": "relative-luminance ratio per token against each theme background and surface"}], "evidence": ["retirement_calculator_au (2026-09-20 audit): --ink-4 failed AA in 2 of 5 themes; WCAG 2.0 SC 1.4.3"], "sources": ["https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum.html"]}, {"id": "AX-004", "category": "accessibility", "title": "A long form with no skip link, no landmark, and headings buried inside buttons", "stacks": ["accessibility", "html"], "severity": "medium", "symptom": "A twenty-section form where every keyboard user tabbed through the whole top bar and tier selector on each load. No `<main>`, no skip link, and each accordion's `<h3>` sat INSIDE its `<button>` rather than wrapping it, so the heading list screen-reader users navigate by did not reliably offer the sections.", "root_cause": "The controls were keyboard-operable, so the page passed a shallow check. Operability is not navigability: on a page this long, the cost is reaching the part you want.", "resolution": "A skip link as the first focusable element, one `<main>` as its target, and the APG accordion shape — heading wraps button, button carries `aria-expanded` and `aria-controls`. Assign the ids from the init function rather than by hand across twenty sections.", "prevention": "Tab through the page before shipping. The first Tab should offer to skip to the content.", "detection": [{"kind": "regex", "recipe": "<button[^>]*>(\\s*<[^/][^>]*>)*\\s*<h[1-6]"}, {"kind": "test", "recipe": "assert a skip link precedes every other focusable element and its href resolves to a landmark"}], "evidence": ["retirement_calculator_au (2026-09-20 audit): retirement.html and reverse.html; WCAG 2.0 SC 2.4.1"], "sources": ["https://www.w3.org/WAI/ARIA/apg/patterns/accordion/"]}, {"id": "FA-001", "category": "fastapi-pydantic", "title": "`extra='ignore'` and `response_model` silently drop undeclared fields", "stacks": ["pydantic", "fastapi"], "severity": "high", "symptom": "A field was always empty in the API although the handler built it: a jointly owned record showed one owner, and an edit form rendered blank first- and last-name inputs.", "root_cause": "FastAPI serialises every return value through `response_model` (or the return annotation). Any key the model does not declare is discarded with no warning, and on request models a mistyped client field is dropped the same way.", "resolution": "Declared the missing fields. A derived test asserts `SELECT aliases ∩ Model.model_fields ⊆ handler dict keys`, so the next column added to the query fails the test instead of rendering blank.", "prevention": "When a field is always empty, read the response model before debugging the handler or the component. Use `extra='forbid'` on request models, where a 422 is better than silent loss.", "detection": [{"kind": "test", "recipe": "set(handler_payload) - set(ResponseModel.model_fields) == set()"}, {"kind": "ast", "recipe": "request models (…Create/…Update/…Request) without model_config extra='forbid'"}], "evidence": ["71c59b20f (2026-09-06)", "tests/backend/test_users_pg_projection_completeness.py"], "sources": ["https://fastapi.tiangolo.com/tutorial/response-model/", "https://docs.pydantic.dev/latest/api/config/#pydantic.config.ConfigDict.extra"]}, {"id": "FA-002", "category": "fastapi-pydantic", "title": "Every hand-written allow-list between the query and the model is another silent filter", "stacks": ["python", "fastapi"], "severity": "high", "symptom": "An edit returned 200 and was stored nowhere. A SELECTed column never reached the response.", "root_cause": "A field passes through several dict-building stages (`SELECT … AS alias` → an intermediate dict → a normaliser → the response model), and each stage that enumerates keys drops anything it does not list.", "resolution": "Derived the expected key set from the SQL aliases and the model fields instead of enumerating it a second time.", "prevention": "When adding a column, grep every dict and allow-list between the query and the response.", "detection": [{"kind": "ast", "recipe": "module-level *_ALLOWED*/*_KEYS* constants used in {k: v … if k in X}, cross-checked against model fields"}], "evidence": ["CLAUDE.md §Silent Failures (GET /users, 2026-09-06)"], "sources": []}, {"id": "FA-003", "category": "fastapi-pydantic", "title": "A response model stricter than the data returns 500 or silently empties a list", "stacks": ["pydantic", "fastapi", "mongodb"], "severity": "high", "symptom": "A list endpoint returned `ResponseValidationError` 500s. Another returned 0 of 14 rows for two tenants and 87 of 87 for the main one.", "root_cause": "Models declared `created_at: str` while some writers stored BSON datetimes, and a required `id` was missing on some rows. Pydantic v2 does not coerce datetime to str, and FastAPI reports an outbound validation failure as a 500, not a 422. A per-row `try/except ValidationError: continue` then emptied a whole tenant.", "resolution": "Added a reusable `IsoTimestamp` annotated type that coerces on read and still rejects non-temporal values. Bad rows are logged and counted, not silently skipped.", "prevention": "Validate a sample of live documents per tenant against every response model, and test with the smallest tenant, not only the largest.", "detection": [{"kind": "ast", "recipe": "response_model fields typed str whose name matches *_at|*date*"}, {"kind": "ast", "recipe": "Model(**doc) in a loop inside except ValidationError: continue"}], "evidence": ["b988d095f (2026-08-26)", "e09bf4861", "1631e4c5d", "0701dbcf4 (2026-09-04)"], "sources": []}, {"id": "FA-004", "category": "fastapi-pydantic", "title": "A response model that inherits the request model's input bounds", "stacks": ["pydantic", "fastapi"], "severity": "medium", "symptom": "One over-long legacy row caused a 500 on the whole list endpoint.", "root_cause": "`class XResponse(XCreate)` inherited `max_length`/`le`/`pattern`, and FastAPI validates outbound data too.", "resolution": "Re-declared the bounded fields unconstrained on the response model, with parity tests.", "prevention": "Input validation belongs to input models only.", "detection": [{"kind": "ast", "recipe": "a response_model class subclassing a class whose fields carry Field(max_length|le|ge|pattern)"}], "evidence": ["f7efbbd23 (2026-08-26)"], "sources": []}, {"id": "FA-005", "category": "fastapi-pydantic", "title": "Two Pydantic classes sharing a name rename a shipped OpenAPI schema", "stacks": ["fastapi", "pydantic", "openapi"], "severity": "medium", "symptom": "`#/components/schemas/BuildingAssetResponse` became `models__building__BuildingAssetResponse` on four operations that were already shipped. Generated clients broke while the spec stayed valid.", "root_cause": "FastAPI module-qualifies colliding model names. The repo held 21 such pairs.", "resolution": "Renamed the new model.", "prevention": "Gate on a committed-spec diff that renames or removes schema keys.", "detection": [{"kind": "shell", "recipe": "grep -o '\"[a-z_]*__[A-Za-z]*\"' openapi.json   # module-qualified schema names"}, {"kind": "ast", "recipe": "group `class X(BaseModel)` by name across modules; flag duplicates reachable from app.openapi()"}], "evidence": ["fd045a533 (2026-09-08)"], "sources": []}, {"id": "FA-006", "category": "fastapi-pydantic", "title": "`Optional[X]` no longer implies a default in Pydantic v2; other v2 behaviour changes", "stacks": ["pydantic"], "severity": "high", "symptom": "Migrated models started rejecting payloads that omitted a field, and response models skipped rows.", "root_cause": "In v2, `x: Optional[int]` is required but nullable; only `= None` makes it optional. Other v2 changes: `.dict()`/`.parse_obj`/`Config`/`@validator` were renamed, `model_dump()` keeps Decimals and datetimes as Python objects (use `mode='json'`), assignment is not validated without `validate_assignment`, and coercion is lax by default (`'123'`→int, floats into Decimal).", "resolution": "Write `x: int | None = None` explicitly. Use `strict=True` and `Decimal` or `AwareDatetime` on money and timestamps.", "prevention": "Run with `-W error::pydantic.warnings.PydanticDeprecatedSince20`.", "detection": [{"kind": "regex", "recipe": ":\\s*(Optional\\[[^\\]]+\\]|[\\w\\[\\]]+\\s*\\|\\s*None)\\s*$  (in models, no default)"}, {"kind": "regex", "recipe": "\\.dict\\(\\)|\\.parse_obj\\(|@validator\\(|@root_validator|class Config:"}, {"kind": "regex", "recipe": ":\\s*float\\b on amount|price|cents|balance fields"}], "evidence": [], "sources": ["https://docs.pydantic.dev/latest/migration/", "https://docs.pydantic.dev/latest/concepts/conversion_table/"]}, {"id": "FA-007", "category": "fastapi-pydantic", "title": "A helper inserted between a route decorator and its function hijacks the route", "stacks": ["fastapi", "python-decorators"], "severity": "critical", "symptom": "`/auth/register` was served by a private address helper, and later `/intelligence/levy-model` by another helper whose positional arguments became query parameters. Nothing raised.", "root_cause": "A decorator applies to the next `def`. With stacked decorators the gap is invisible, and tooling that inserted code at `rindex('@router.get(')` landed between two stacked decorators.", "resolution": "Moved the helpers above the decorator stack. The `test_no_private_endpoint_registered_as_route` gate was originally scoped to a literal list of 4 of 145 router files and is now derived from the tree.", "prevention": "Never place code between a decorator and its function. Registered endpoints must have public names.", "detection": [{"kind": "ast", "recipe": "FunctionDef whose name starts with _ and carries @<router>.(get|post|put|patch|delete|api_route)"}, {"kind": "test", "recipe": "iterate app.openapi() operations; flag endpoint functions whose __name__ starts with _"}], "evidence": ["24590c614 (2026-09-06)", "270f61ffa (2026-09-05)", "tests/backend/test_router_registration_hygiene.py"], "sources": []}, {"id": "FA-008", "category": "fastapi-pydantic", "title": "Wrapping router imports in `try/except ImportError` turns a broken module into 404s", "stacks": ["fastapi", "imports"], "severity": "critical", "symptom": "Every route in a module silently 404'd. One router whose import-guard handler used a `logger` defined 15 lines later would have taken out every auth route.", "root_cause": "`server.py` guarded each `from routers.x import router` with `except ImportError: logger.warning(...)`, so a syntax error or NameError merely skips registration.", "resolution": "Defined `logger` before the guards, ran pyflakes, and added wiring tests that assert paths exist in `app.openapi()['paths']`.", "prevention": "Optional routers should fail loudly in CI. Check the logs first whenever an endpoint 404s after an edit.", "detection": [{"kind": "ast", "recipe": "Try whose body imports routers.* and whose handler does not re-raise"}, {"kind": "lint", "recipe": "pyflakes/ruff F821 at module scope"}], "evidence": ["c71feadb8 (2026-08-20)", "rules/post-compact-critical.md footgun #1"], "sources": []}, {"id": "FA-009", "category": "fastapi-pydantic", "title": "Unregistered duplicate routers: tests pass against code that never runs", "stacks": ["fastapi", "testing"], "severity": "high", "symptom": "A settings card called four routes that returned 404 for months while their unit tests passed. A security test exercised a BOLA guard that existed only in a dead duplicate, while the live inline handler had none.", "root_cause": "Router files that are never imported still define handlers, and tests import those handlers by module path.", "resolution": "Assert wiring via `app.openapi()`, port guards to the live handler, and point tests at the live handler. Registering a dead router exposed 4 tests that had only ever asserted the dead copy's behaviour.", "prevention": "Tests reach handlers through the registered app. A duplicate-drift audit compares guard shapes between same-named handlers.", "detection": [{"kind": "shell", "recipe": "diff routers/*.py against `from routers.X import` in the app entrypoint"}, {"kind": "ast", "recipe": "same-named async def handlers in two modules where only one is registered; tests importing from unwired modules"}], "evidence": ["CLAUDE.md §The duplicate that RUNS is not the duplicate that is TESTED", "tests/backend/test_unwired_router_drift.py"], "sources": []}, {"id": "FA-010", "category": "fastapi-pydantic", "title": "`except Exception` in a handler turns deliberate `HTTPException`s into 500s", "stacks": ["fastapi", "error-handling"], "severity": "high", "symptom": "An invalid page key returned 500 'store failed' and logged 'unexpected failure' instead of a 422.", "root_cause": "Validators that raise `HTTPException(401/422)` ran inside the try, and the error mapper converted every non-domain exception to 500.", "resolution": "The mapper's first branch is `if isinstance(exc, HTTPException): return exc`.", "prevention": "Always put `except HTTPException: raise` before `except Exception`.", "detection": [{"kind": "ast", "recipe": "route function with try/except Exception raising HTTPException(500) and no preceding except HTTPException"}], "evidence": ["24e140378 (2026-09-03)"], "sources": ["https://fastapi.tiangolo.com/tutorial/handling-errors/"]}, {"id": "FA-011", "category": "fastapi-pydantic", "title": "Calling a route function directly bypasses dependency injection", "stacks": ["fastapi", "pytest"], "severity": "medium", "symptom": "Tests behaved oddly: parameters arrived as `Header(None)` objects instead of values.", "root_cause": "Invoked as a plain coroutine, `Depends()`/`Header()`/`Query()` defaults are FieldInfo objects. Rate-limited routes also need a real Starlette `Request`.", "resolution": "Pass explicit keyword arguments, or use `TestClient`/`httpx.AsyncClient`.", "prevention": "Prefer HTTP-level tests for route behaviour.", "detection": [{"kind": "ast", "recipe": "direct await <router_fn>(...) in tests omitting kwargs whose defaults are Header(|Query(|Depends("}], "evidence": ["9dd281371 (2026-08-07)"], "sources": []}, {"id": "FA-012", "category": "fastapi-pydantic", "title": "`app.routes` is lazily wrapped in newer FastAPI, so negative route assertions pass vacuously", "stacks": ["fastapi", "pytest"], "severity": "high", "symptom": "`assert path not in {r.path for r in app.routes}` passed trivially: `app.routes` had length 1 while OpenAPI listed 1,176 paths.", "root_cause": "FastAPI 0.141 keeps an `_IncludedRouter` wrapper in `app.routes` instead of flattening the included routes.", "resolution": "Assert on `app.openapi()['paths']`, or flatten recursively.", "prevention": "Every negative or count assertion needs a non-empty precondition.", "detection": [{"kind": "regex", "recipe": "for r in app\\.routes"}, {"kind": "review", "recipe": "negative membership assertions over a collection never asserted non-empty"}], "evidence": ["4655b0ae6 (2026-09-03)", "379cec1d9"], "sources": []}, {"id": "FA-013", "category": "fastapi-pydantic", "title": "A permission check reads a claim that no dependency populates", "stacks": ["fastapi", "authorization"], "severity": "high", "symptom": "The guard never fired in production. Its unit test passed because the test set the claim directly.", "root_cause": "`current_user['governance_offices']` was only hydrated by a dependency that this route did not use.", "resolution": "Introduced a single resolver function that every route calls.", "prevention": "Before gating on a claim, confirm which request path populates it.", "detection": [{"kind": "ast", "recipe": "current_user['k'] / .get('k') in a route with no Depends chain that assigns k"}], "evidence": ["969c600", "CLAUDE.md §A permission that reads a claim nobody populated"], "sources": []}, {"id": "FA-014", "category": "fastapi-pydantic", "title": "`List[str]` receiving a null element, where an empty list means 'all'", "stacks": ["pydantic", "api-design"], "severity": "medium", "symptom": "An approval looked like a silent no-op, and the client got 'Input should be a valid string'.", "root_cause": "The UI sent `[null]` for unlinked records. Filtering the nulls out would have turned 'remove one' into 'remove all', because the endpoint treated an empty list as 'all'.", "resolution": "`List[Optional[str]]` with a validator that raises a named error.", "prevention": "Never let an empty collection mean 'everything' on a destructive endpoint.", "detection": [{"kind": "ast", "recipe": "handlers where `if not ids:` selects all rows before a destructive write"}], "evidence": ["6982aaedc (2026-08-30)"], "sources": []}, {"id": "FA-015", "category": "fastapi-pydantic", "title": "A blocking call inside `async def` stalls every request on the worker", "stacks": ["fastapi", "asyncio", "uvicorn"], "severity": "critical", "symptom": "One slow SMTP relay or scraper froze every request on a worker for 180–300 s. An 8.2 s CPU-bound build ran inside a handler.", "root_cause": "`async def` endpoints run on the event loop, so `smtplib`, `subprocess.run`, `requests`, `time.sleep`, sync DB drivers, bcrypt and heavy CPU work block it. (`def` endpoints run in the threadpool.)", "resolution": "`asyncio.to_thread(...)` / `run_in_threadpool`, plus a per-process cache for CPU-bound results (deep-copied on read). The gate test has positive and negative controls so it does not flag its own fix.", "prevention": "Enable ruff's ASYNC rules. Mind threadpool exhaustion (40 AnyIO tokens by default).", "detection": [{"kind": "ast", "recipe": "inside AsyncFunctionDef: smtplib.*, subprocess.run/call/check_output, requests.*, time.sleep, urllib.request, bcrypt.hashpw not wrapped in to_thread/run_in_executor"}, {"kind": "lint", "recipe": "ruff ASYNC210/ASYNC230/ASYNC251"}], "evidence": ["9eb844ac4 (2026-09-04)", "602f2f62d", "e7d78309f", "6c1e24dd3 (2026-09-13)", "tests/backend/test_no_blocking_io_in_async_handlers.py"], "sources": ["https://fastapi.tiangolo.com/async/#path-operation-functions", "https://docs.astral.sh/ruff/rules/#flake8-async-async"]}, {"id": "FA-016", "category": "fastapi-pydantic", "title": "`Depends` results are cached per request; `yield`-dependency teardown timing changed", "stacks": ["fastapi"], "severity": "medium", "symptom": "A dependency that mutates state ran once when it was expected twice. A DB session was already closed when a background task used it.", "root_cause": "`use_cache=True` is the default. Since 0.106 the exit code of a `yield` dependency runs before the response is sent.", "resolution": "`Depends(fn, use_cache=False)` where re-evaluation is needed. Background tasks open their own session.", "prevention": "Never pass request-scoped sessions to background work.", "detection": [{"kind": "regex", "recipe": "background_tasks\\.add_task\\([^)]*(session|db)"}], "evidence": [], "sources": ["https://fastapi.tiangolo.com/tutorial/dependencies/sub-dependencies/", "https://fastapi.tiangolo.com/release-notes/"]}, {"id": "FA-017", "category": "fastapi-pydantic", "title": "BackgroundTasks are not a job queue; `on_event` and `lifespan` don't mix", "stacks": ["fastapi", "starlette"], "severity": "medium", "symptom": "Work queued with `add_task` was lost on a restart. Startup hooks silently did not run.", "root_cause": "Background tasks run in-process after the response, with no persistence or retry. Once `lifespan=` is passed, `@app.on_event` handlers are not called.", "resolution": "Use a durable queue (ARQ/Temporal/transactional outbox) for must-happen work, and migrate every startup hook to one lifespan.", "prevention": "Grep for add_task on email, payment or ledger work.", "detection": [{"kind": "regex", "recipe": "on_event\\(\\\"(startup|shutdown)\\\"\\)"}, {"kind": "regex", "recipe": "background_tasks\\.add_task\\(.*(email|payment|ledger|send)"}], "evidence": [], "sources": ["https://fastapi.tiangolo.com/tutorial/background-tasks/#caveat", "https://fastapi.tiangolo.com/advanced/events/"]}, {"id": "FA-018", "category": "fastapi-pydantic", "title": "CORS with credentials cannot use a wildcard, and echoing any Origin is worse", "stacks": ["fastapi", "security"], "severity": "high", "symptom": "Browsers rejected credentialed requests, and the 'fix' (a `.*` origin regex) enabled credentialed cross-origin reads.", "root_cause": "The spec forbids `*` together with credentials. Echoing the request's Origin grants every site access.", "resolution": "An explicit origin list, with any regex anchored.", "prevention": "Review CORS configuration on every deploy target.", "detection": [{"kind": "regex", "recipe": "allow_origins=\\[\\\"\\*\\\"\\]|allow_origin_regex=['\\\"]\\.\\*"}], "evidence": [], "sources": ["https://fastapi.tiangolo.com/tutorial/cors/", "https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS#credentialed_requests_and_wildcards"]}, {"id": "FA-019", "category": "fastapi-pydantic", "title": "uvicorn behind a proxy: the client IP comes from the proxy, and state is per-process", "stacks": ["uvicorn", "security"], "severity": "medium", "symptom": "Rate limits and audit logs saw the proxy's IP, or trusted a spoofed `X-Forwarded-For`.", "root_cause": "Without `--proxy-headers` and a narrow `--forwarded-allow-ips`, the client host is the proxy's address, and `*` trusts anyone. `--workers N` gives each process its own in-memory limiter.", "resolution": "Trust only the reverse proxy's IP (`TRUSTED_PROXY_CIDRS`), and keep shared state in Redis.", "prevention": "Check the systemd `ExecStart` flags.", "detection": [{"kind": "regex", "recipe": "--forwarded-allow-ips[= ]['\\\"]?\\*"}], "evidence": [], "sources": ["https://www.uvicorn.org/deployment/#proxies-and-forwarded-headers"]}, {"id": "FA-020", "category": "fastapi-pydantic", "title": "Letting the caller set a flag that hides rows is an authorisation decision", "stacks": ["fastapi", "security"], "severity": "high", "symptom": "Any unauthenticated caller could erase their own login attempts from the security dashboard and the risk baseline.", "root_cause": "The login audit took `is_test_data` from an `X-Test-Data` request header, and every read of the audit log excluded test rows.", "resolution": "The flag is derived server-side (running under pytest, or the header outside production only).", "prevention": "Audit request models that expose internal flags (mass assignment).", "detection": [{"kind": "regex", "recipe": "request\\.headers\\.get\\(['\\\"]X-Test"}, {"kind": "regex", "recipe": "\\$set\\\":\\s*(body|payload|data)\\b|\\*\\*(body|payload)\\.(dict|model_dump)\\(\\)"}], "evidence": ["CLAUDE.md §Never let the caller set the flag"], "sources": ["https://cheatsheetseries.owasp.org/cheatsheets/Mass_Assignment_Cheat_Sheet.html"]}, {"id": "PY-001", "category": "python-core", "title": "`d.get(k, default)` does not default a key that is present but None", "stacks": ["python"], "severity": "high", "symptom": "Several live 500s: `None >= 85`, `None.capitalize()`, `float(None)`. A sweep found 42 more sites.", "root_cause": "The default applies only when the key is absent, and both stores wrote explicit nulls.", "resolution": "`(d.get(k) or default)` or an explicit `is None` branch. Where 0 is a real value, return 'unmeasured' rather than 0.", "prevention": "Treat 'missing' and 'null' identically at read boundaries.", "detection": [{"kind": "ast", "recipe": "Call .get(k, default) whose result flows into a comparison, arithmetic, float()/int() or attribute access"}], "evidence": ["39efc770f (2026-08-27)", "122efca38", "f66300c24", "7f303a6bb (2026-09-09)", "footgun #14"], "sources": []}, {"id": "PY-002", "category": "python-core", "title": "Unparenthesised `x.get(k) or {}.get(...)` binds the wrong way", "stacks": ["python"], "severity": "medium", "symptom": "The wrong object was returned silently.", "root_cause": "It parses as `x.get(k) or ({}.get(...))`.", "resolution": "Parenthesise it: `(x.get(k) or {}).get(...)`.", "prevention": "Lint rule.", "detection": [{"kind": "ast", "recipe": "BoolOp(Or) whose last operand is a Call on a Dict/List literal attribute"}], "evidence": ["122efca38"], "sources": []}, {"id": "PY-003", "category": "python-core", "title": "`getattr(obj, name, default)` does not protect against a property that does I/O", "stacks": ["python", "pymongo"], "severity": "medium", "symptom": "A fully green CI run failed at session teardown with `ServerSelectionTimeoutError`.", "root_cause": "`MongoClient.address` and `.nodes` are properties that perform server selection, and `getattr`'s default catches only `AttributeError`.", "resolution": "Decide from configuration (URI/env) rather than probing the client, and wrap the access in try/except.", "prevention": "Be suspicious of `getattr` defaults on client objects, especially in finalisers.", "detection": [{"kind": "regex", "recipe": "getattr\\(\\w*client\\w*,\\s*['\\\"](address|nodes|primary|secondaries)['\\\"]"}], "evidence": ["357fe32c8 (2026-09-08)"], "sources": []}, {"id": "PY-004", "category": "python-core", "title": "Undefined names in an `except (A, B)` tuple only fail when the try raises", "stacks": ["python", "error-handling"], "severity": "high", "symptom": "A mirror failure became a `NameError` 500 after the primary write had already committed.", "root_cause": "The except clause named exception classes the module never imported, and the clause is evaluated only when an exception occurs.", "resolution": "Removed the wrong names.", "prevention": "Make ruff F821 a blocking check.", "detection": [{"kind": "lint", "recipe": "ruff/pyflakes F821 (covers ExceptHandler.type names)"}], "evidence": ["2d6d50fab (2026-09-04)"], "sources": []}, {"id": "PY-005", "category": "python-core", "title": "`''` passed where `None` means 'absent'", "stacks": ["python", "validation"], "severity": "high", "symptom": "An email ingest recorded zero messages, because every message raised 422.", "root_cause": "The caller passed `actor_user_id=''`. The validator rejected it, and `CAST('' AS UUID)` fails too.", "resolution": "An `_optional_uuid` helper treats None as absent, while `''` or malformed input stays an error. The caller now passes None.", "prevention": "Normalise empty strings at the boundary, or reject them explicitly.", "detection": [{"kind": "ast", "recipe": "call sites passing '' to params annotated Optional[UUID|str]"}], "evidence": ["4d46680a3 (2026-09-08)"], "sources": []}, {"id": "PY-006", "category": "python-core", "title": "A try/except that logs and continues turns a hard failure into a permanent silent one", "stacks": ["python", "error-handling"], "severity": "critical", "symptom": "No registration ever created a user row (the FK violation was swallowed). Status changes were written to only one store (a cast to a nonexistent enum was swallowed). A fresh deploy had no administrator. A cron kill switch vanished under `except ImportError: pass`.", "root_cause": "Broad excepts around writes, labelled 'non-fatal'.", "resolution": "Narrowed the excepts, asserted post-conditions in tests, and made safety switches fail closed.", "prevention": "If a write must happen, assert that it happened. 'No exception' is not 'changed a row'.", "detection": [{"kind": "ast", "recipe": "except Exception/bare except whose body is only pass/logger.*/return None|False|[] and whose try contains insert|update|execute|commit|send"}, {"kind": "regex", "recipe": "except ImportError:\\s*pass"}], "evidence": ["e5a5aa7c8 (2026-09-07)", "c66066596", "footguns #16, #23"], "sources": []}, {"id": "PY-007", "category": "python-core", "title": "A function-local import of a nonexistent module hidden by `except: return []`", "stacks": ["python", "imports"], "severity": "high", "symptom": "A route returned `[]` forever. The module it imported does not exist in the repo.", "root_cause": "Linters and top-level AST audits pass because the import is lazy and wrapped.", "resolution": "A test resolves every first-party import at any depth with `importlib.util.find_spec`, and checks that the walk actually reached files.", "prevention": "Never wrap imports in catch-alls.", "detection": [{"kind": "test", "recipe": "every Import/ImportFrom at any depth with a first-party root resolves via find_spec"}], "evidence": ["cb5a8cc24 (2026-09-03)", "tests/backend/test_no_phantom_first_party_imports.py"], "sources": []}, {"id": "PY-008", "category": "python-core", "title": "A bare except around `exec_module` leaves a half-loaded module", "stacks": ["python", "imports"], "severity": "medium", "symptom": "A measurement script published 13/12/128 when the truth was 25/25/103.", "root_cause": "The module raised part-way through (a `@dataclass` needed a `sys.modules` registration). Definitions above the failure point existed, and `getattr(m, X, {})` masked the missing ones.", "resolution": "Let analysis scripts crash on import failure, and register the module in `sys.modules` before `exec_module`.", "prevention": "Corroborate headline numbers a second way.", "detection": [{"kind": "ast", "recipe": "spec.loader.exec_module( inside try with a swallowing handler, or without a prior sys.modules[name] = m"}], "evidence": ["memory: pattern_my_own_measuring_script_swallowed_its_import"], "sources": []}, {"id": "PY-009", "category": "python-core", "title": "An exception classifier must not treat programming errors as outages", "stacks": ["python", "error-handling", "postgresql"], "severity": "high", "symptom": "An SQL bug (undefined column) made a route serve the fallback store permanently. Conversely, a settings read fell back only when the result was EMPTY and raised on an outage.", "root_cause": "`except Exception: return None`, where None means 'unavailable, fall back'.", "resolution": "`classify_pg_exception` by SQLSTATE: programming errors surface as 500s with no fallback, while infrastructure errors fall back. 'Raised' and 'empty' are separate branches, each logged with its exception type.", "prevention": "A fallback may never enable a protected flag.", "detection": [{"kind": "ast", "recipe": "except Exception: return None in functions whose None return triggers a fallback in callers"}], "evidence": ["8c5962318", "875a886d2 (2026-08-10)", "33d67f1e7 (2026-09-06)"], "sources": []}, {"id": "PY-010", "category": "python-core", "title": "A try scope narrower than its 'must never fail' claim", "stacks": ["python", "error-handling"], "severity": "medium", "symptom": "A notification that 'must never fail a registration' guarded 1 line of a 60-line block, so an unreachable DB would 500 a registration that had already committed.", "root_cause": "The try block did not cover all of the best-effort side effects.", "resolution": "Wrapped the whole best-effort block, with an AST test asserting every side-effect call sits inside the try.", "prevention": "State the invariant in a test, not a comment.", "detection": [{"kind": "test", "recipe": "AST: the try body contains every side-effect call of the best-effort block"}], "evidence": ["5282da9bb (2026-09-05)"], "sources": []}, {"id": "PY-011", "category": "python-core", "title": "`__main__` or `close()` indented inside a loop: valid syntax, wrong behaviour", "stacks": ["python"], "severity": "high", "symptom": "A cron script did nothing when run. A connection closed after the first iteration.", "root_cause": "An indentation slip moved `if __name__ == '__main__':` or `client.close()` into a block.", "resolution": "Fixed the indentation.", "prevention": "Lint for these shapes.", "detection": [{"kind": "ast", "recipe": "If testing __name__ == '__main__' not at module top level; .close() on a client inside a For body"}], "evidence": ["284afca11 (2026-08-07)"], "sources": []}, {"id": "PY-012", "category": "python-core", "title": "`python path/to/script.py` puts the script's own directory on `sys.path[0]`", "stacks": ["python", "imports", "packaging"], "severity": "high", "symptom": "Combined with `except ImportError: pass`, a kill switch silently vanished. Tests passed because the harness had added `.` to the path.", "root_cause": "Direct script execution changes the import root.", "resolution": "An explicit path bootstrap plus an unguarded module-level import, and a test that runs the script exactly the way cron does. Watch `Path(__file__).parents[N]` off-by-ones: `parents[1]` cut a scan from 282 files to 28 while still reporting OK.", "prevention": "Run scripts with `python -m`, or bootstrap explicitly.", "detection": [{"kind": "ast", "recipe": "files under scripts/ or cron/ importing first-party packages with no sys.path bootstrap"}], "evidence": ["c66066596", "bcd86bed0", "f1bef0ac5"], "sources": []}, {"id": "PY-013", "category": "python-core", "title": "Eager heavy imports cost seconds at startup; `ImportError` is not the only failure", "stacks": ["python", "imports", "performance"], "severity": "medium", "symptom": "`import server` took 8.9 s, 2.86 s of it weasyprint, matplotlib and openpyxl.", "root_cause": "Heavy optional libraries were imported at module level for use in a few functions. Native libraries fail with `OSError`, not `ImportError`.", "resolution": "Import lazily, use `importlib.util.find_spec` for availability flags, call `matplotlib.use('Agg')` before importing pyplot, and catch `(ImportError, OSError)`.", "prevention": "Profile with `python -X importtime`.", "detection": [{"kind": "regex", "recipe": "^import (weasyprint|matplotlib\\.pyplot|openpyxl|pandas)"}, {"kind": "regex", "recipe": "except ImportError:\\s*\\n\\s*HAS_"}], "evidence": ["9f5f63285 (2026-09-04)"], "sources": []}, {"id": "PY-014", "category": "python-core", "title": "Dependency pins: exact-pin conflicts, editable installs masking pins, undeclared imports", "stacks": ["python", "packaging", "pip"], "severity": "medium", "symptom": "A clean `pip install` failed on an unresolvable conflict between pydantic and pydantic_core. `pip install -e` of a sibling repo masked a stale pin. A package was present in the venv but absent from requirements.", "root_cause": "The long-lived venv diverged from requirements.txt.", "resolution": "Test requirements in a throwaway venv.", "prevention": "In CI: `pip install --dry-run -r requirements.txt` in a fresh venv, `pip check`, and an import-vs-requirements diff (e.g. deptry).", "detection": [{"kind": "ci", "recipe": "fresh-venv install + pip check + deptry"}], "evidence": ["adbb863f7 (2026-08-18)", "083d3d71a", "ea70287fe"], "sources": []}, {"id": "PY-015", "category": "python-core", "title": "A 'one-line' suggested edit introduces a missing import", "stacks": ["python", "imports"], "severity": "high", "symptom": "A NameError on a live approval path: `datetime.now(timezone.utc)` without importing `timezone`.", "root_cause": "Code-review suggestions are applied without a compile or lint step.", "resolution": "Added the import.", "prevention": "Run ruff F821 as a blocking check on changed files.", "detection": [{"kind": "lint", "recipe": "ruff F821 blocking in CI"}], "evidence": ["tasks/lessons.md"], "sources": []}, {"id": "PY-016", "category": "python-core", "title": "A context-dependent helper silently misbehaves outside a request", "stacks": ["python", "contextvars"], "severity": "high", "symptom": "'Missing tenant context' was swallowed by a blanket except, so every script took the fallback path.", "root_cause": "A tenant contextvar is set by request middleware only, and scripts and workers never set it.", "resolution": "Set and restore the context around calls in scripts and workers.", "prevention": "Make a missing context raise, and never catch that error.", "detection": [{"kind": "ast", "recipe": "functions under scripts/, workers/ or cron/ calling a tenant-scoped wrapper without set_ctx_* on the path"}], "evidence": ["0052603c9 (2026-08-20)"], "sources": []}, {"id": "PY-017", "category": "python-core", "title": "Float money plus `round()` (banker's rounding) produces different cents", "stacks": ["python", "decimal"], "severity": "critical", "symptom": "Two functions named `dollars_to_cents` shipped side by side and returned different money: `'10.005'` was rejected by one and returned 1001 from the other. `8.115*100 == 811.4999…`.", "root_cause": "`int(round(float(v)*100))` combines binary float error with round-half-even.", "resolution": "One owner module using `Decimal(str(x)).quantize(Decimal('0.01'), ROUND_HALF_UP)`, integer cents everywhere, and a float ban in the domain package enforced by a regex+AST test.", "prevention": "Convert once, at the ingestion boundary.", "detection": [{"kind": "regex", "recipe": "round\\(\\s*float\\(|round\\(.*\\*\\s*100\\)|Decimal\\([^'\\\"]\\w*\\)"}, {"kind": "ast", "recipe": "float( calls or float literals inside the domain package"}], "evidence": ["f9e4d34c8 (2026-09-02)", "9ee666c1b", "tests/backend/test_no_floats_in_domain.py"], "sources": ["https://docs.python.org/3/library/functions.html#round", "https://docs.python.org/3/library/decimal.html#decimal.Decimal.quantize"]}, {"id": "PY-018", "category": "python-core", "title": "Naive datetimes: `utcnow()` is deprecated, and `date.today()` is local time", "stacks": ["python", "time"], "severity": "high", "symptom": "A dedupe compared UTC ISO prefixes against local `date.today()`, so on a UTC+10 host it failed open and re-notified everyone. Comparing naive and aware datetimes raised TypeError.", "root_cause": "`datetime.utcnow()` returns a naive datetime, and `date.today()` uses the host's time zone.", "resolution": "Use `datetime.now(timezone.utc)` and `zoneinfo` for business dates, and pass `today` in explicitly so it can be tested.", "prevention": "Enable ruff's DTZ rules.", "detection": [{"kind": "regex", "recipe": "utcnow\\(\\)|utcfromtimestamp\\(|datetime\\.now\\(\\)|date\\.today\\(\\)"}, {"kind": "lint", "recipe": "ruff DTZ001-DTZ012"}], "evidence": ["b40e914e4 (2026-09-12)", "22b8ae335"], "sources": ["https://docs.python.org/3/library/datetime.html#datetime.datetime.utcnow"]}, {"id": "PY-019", "category": "python-core", "title": "A function returning a numeric string is compared numerically", "stacks": ["python"], "severity": "medium", "symptom": "Wrong year comparisons: a helper returned `'2026'`, a str.", "root_cause": "String-typed return values were compared against ints.", "resolution": "Cast at the boundary: `int(str(y).split('-')[0])`.", "prevention": "Annotate return types and run a type checker.", "detection": [{"kind": "review", "recipe": "comparisons between str-returning helpers and int literals (mypy/pyright)"}], "evidence": ["footgun #3"], "sources": []}, {"id": "PY-020", "category": "python-core", "title": "Secrets in exception text end up in logs and databases", "stacks": ["python", "logging", "security"], "severity": "high", "symptom": "Driver errors echoed the full connection URI, password included, into log files and a stored job document.", "root_cause": "pymongo and asyncpg include the DSN in some exception messages, and child-process stderr was persisted verbatim.", "resolution": "A `redact_secrets()` helper handles the URI pattern and the values of secret-named env vars.", "prevention": "Redact before persisting any `str(exc)` or stderr.", "detection": [{"kind": "ast", "recipe": "str(exc) or stderr written to DB fields or logger.* without a redaction call"}], "evidence": ["602f2f62d (2026-08-20)"], "sources": []}, {"id": "PY-021", "category": "python-core", "title": "A redaction filter on the root logger misses uvicorn's loggers", "stacks": ["python", "logging", "uvicorn"], "severity": "high", "symptom": "`?email=` query strings were not redacted in the access logs.", "root_cause": "uvicorn's loggers set `propagate=False`, so root-level filters never see their records.", "resolution": "Attach the filter to every handler in `logging.Logger.manager.loggerDict`, idempotently.", "prevention": "Test redaction against the real access-log record.", "detection": [{"kind": "regex", "recipe": "logging\\.getLogger\\(\\)\\.addFilter"}], "evidence": ["2f630e40a (2026-08-30)"], "sources": []}, {"id": "PY-022", "category": "python-core", "title": "Fire-and-forget `Popen` with no liveness check; a zombie passes `os.kill(pid, 0)`", "stacks": ["python", "subprocess"], "severity": "medium", "symptom": "A job sat at 'starting' forever.", "root_cause": "The child died instantly, and the zombie still answered `kill(pid, 0)`. The script path was resolved relative to the wrong `__file__`.", "resolution": "Read `/proc/<pid>/stat` for the process state, and preflight the script path.", "prevention": "Keep and poll `Popen` handles.", "detection": [{"kind": "regex", "recipe": "os\\.kill\\(\\w+,\\s*0\\)"}, {"kind": "ast", "recipe": "Popen( whose handle is discarded (no wait/poll)"}], "evidence": ["memory: pattern_silent_popen_and_playwright_browser_drift"], "sources": []}, {"id": "PY-023", "category": "python-core", "title": "A subprocess argument list still lets a value starting with `-` become an option", "stacks": ["python", "security"], "severity": "medium", "symptom": "A user-supplied value could be read as a command-line flag.", "root_cause": "Validation allowed a leading `-`.", "resolution": "Require a leading alphanumeric character, or insert `--`.", "prevention": "Review argv built from user input.", "detection": [{"kind": "review", "recipe": "user-controlled values in subprocess argv validated by a regex that allows a leading -"}], "evidence": ["e7d78309f (2026-09-11)"], "sources": []}, {"id": "PY-024", "category": "python-core", "title": "User input interpolated into a regex", "stacks": ["python", "mongodb", "security"], "severity": "high", "symptom": "`.` matched another user's address, and `+` made plus-addressed mail fail to match itself, so an email suppression list leaked.", "root_cause": "`{'$regex': f'...{var}...'}` without escaping.", "resolution": "Use `re.escape`.", "prevention": "Also applies to `re.compile` with user input.", "detection": [{"kind": "regex", "recipe": "\\$regex['\\\"]?\\s*:\\s*f['\\\"]|\\$regex['\\\"]?\\s*:\\s*\\w+\\b(?!.*re\\.escape)"}], "evidence": ["c66066596 (2026-08-21)"], "sources": []}, {"id": "AS-001", "category": "asyncio", "title": "A fire-and-forget `create_task` can be garbage-collected mid-flight", "stacks": ["asyncio"], "severity": "high", "symptom": "Background work vanished, and 'Task exception was never retrieved' appeared at GC time.", "root_cause": "The event loop keeps only weak references to tasks.", "resolution": "Keep tasks in a set with `add_done_callback(set.discard)`, or use a `TaskGroup`.", "prevention": "Enable ruff RUF006.", "detection": [{"kind": "regex", "recipe": "^\\s*asyncio\\.create_task\\("}, {"kind": "lint", "recipe": "ruff RUF006"}], "evidence": [], "sources": ["https://docs.python.org/3/library/asyncio-task.html#asyncio.create_task"]}, {"id": "AS-002", "category": "asyncio", "title": "`gather` does not cancel siblings, and `return_exceptions=True` results go unchecked", "stacks": ["asyncio"], "severity": "medium", "symptom": "Failures were unobserved while sibling tasks kept running. An unbounded `gather` over 87 items exhausted a pool of 15 connections.", "root_cause": "The first exception propagates while the others continue. Exceptions returned in the result list are easy to ignore.", "resolution": "Use `TaskGroup` (3.11+), or check `isinstance(r, BaseException)`. Bound the fan-out with a semaphore, or better, with a set-based query.", "prevention": "Lint for gather over a comprehension that opens DB sessions.", "detection": [{"kind": "regex", "recipe": "gather\\(\\*\\[.*for .* in"}, {"kind": "regex", "recipe": "return_exceptions=True"}], "evidence": ["306682c57", "2ebcabe4d"], "sources": ["https://docs.python.org/3/library/asyncio-task.html#task-groups"]}, {"id": "AS-003", "category": "asyncio", "title": "Swallowing `CancelledError` breaks shutdown and timeouts", "stacks": ["asyncio"], "severity": "medium", "symptom": "Shutdown hung, and `asyncio.timeout()` stopped working.", "root_cause": "`except BaseException` or a bare `except:` in a coroutine catches cancellation (a `BaseException` since 3.8).", "resolution": "Catch `Exception`, and add a regression test that cancellation propagates.", "prevention": "Enable ruff BLE001 and E722.", "detection": [{"kind": "regex", "recipe": "except\\s*:|except BaseException"}], "evidence": ["4013acac6 (2026-08-06)", "ef545cce8"], "sources": []}, {"id": "AS-004", "category": "asyncio", "title": "Two `asyncio.run()` calls in one script: 'Future attached to a different loop'", "stacks": ["asyncio"], "severity": "high", "symptom": "The dry run always worked, and the first real `--apply` crashed.", "root_cause": "A lazily created connection pool was bound to the first loop, which was already closed.", "resolution": "Use a single `asyncio.run(main())`.", "prevention": "Lint for more than one `asyncio.run` in a module's main path.", "detection": [{"kind": "regex", "recipe": "asyncio\\.run\\(  (count > 1 per module)"}], "evidence": ["tasks/lessons.md LESSON 35 (2026-08-06)"], "sources": []}, {"id": "AS-005", "category": "asyncio", "title": "`asyncio.get_event_loop()` under Python 3.12 is order-dependent in tests", "stacks": ["asyncio", "pytest"], "severity": "medium", "symptom": "77 tests passed in isolation and 38 failed in the full run.", "root_cause": "Since 3.12, no loop is created implicitly in a thread that has none.", "resolution": "Use `asyncio.run()`.", "prevention": "Grep for `get_event_loop().run_until_complete`.", "detection": [{"kind": "regex", "recipe": "get_event_loop\\(\\)\\.run_until_complete"}], "evidence": ["1bb7e78c8 (2026-09-04)"], "sources": []}, {"id": "AS-006", "category": "asyncio", "title": "Module-level async client singletons are bound to one loop", "stacks": ["asyncio", "pytest", "motor", "sqlalchemy"], "severity": "medium", "symptom": "'Task attached to a different loop' within a single test module.", "root_cause": "TestClient allocates a loop per instance, and `from database import db` holds a direct reference, so rebinding `database.db` never reaches the modules that imported it.", "resolution": "Use function-scoped reset fixtures and mutate the singleton in place. Give loop-bound clients the same scope as their loop (pytest-asyncio `loop_scope`).", "prevention": "Set `asyncio_mode`, `asyncio_default_fixture_loop_scope` and the test loop scope explicitly in pytest.ini.", "detection": [{"kind": "config", "recipe": "pytest.ini defines asyncio_mode and asyncio_default_fixture_loop_scope"}, {"kind": "regex", "recipe": "def event_loop\\("}], "evidence": ["379cec1d9 (2026-08-10)", "e7b172268"], "sources": ["https://pytest-asyncio.readthedocs.io/en/stable/reference/configuration.html"]}, {"id": "AS-007", "category": "asyncio", "title": "`asyncio.gather` added without `import asyncio`, with the NameError swallowed", "stacks": ["asyncio", "imports"], "severity": "high", "symptom": "A feature silently did nothing.", "root_cause": "The call site sat inside `try/except Exception`.", "resolution": "Added the import.", "prevention": "Make ruff F821 blocking.", "detection": [{"kind": "shell", "recipe": "grep -rln 'asyncio\\.\\(gather\\|wait\\)' | xargs grep -L '^import asyncio'"}], "evidence": ["ef545cce8 (2026-06-05)"], "sources": []}, {"id": "PT-001", "category": "python-testing", "title": "A module-level `load_dotenv(override=True)` in one test file redirected the whole suite to production", "stacks": ["pytest", "config"], "severity": "critical", "symptom": "11,700 tests wrote to the production database for four days, but only in full runs. A single-file run was fine, which inverted the symptom.", "root_cause": "pytest imports every module at collection time, so one module's `override=True` re-pointed `DB_NAME`, and an autouse fixture re-read `os.environ` for every test.", "resolution": "Capture the database choice once. A guard test pins it, and a subprocess-import test checks that sentinel env vars survive. The conftest refuses the production DB by name.", "prevention": "Ban `override=True` under tests/. Assert the active DB name at the end of the session.", "detection": [{"kind": "regex", "recipe": "load_dotenv\\([^)]*override=True  (in tests/ or modules tests import)"}], "evidence": ["b68a4ed68 (2026-08-31)", "8ea663861 (2026-09-05)", "tests/backend/test_session_db_pinning.py"], "sources": []}, {"id": "PT-002", "category": "python-testing", "title": "`patch(..., create=True)` on a nonexistent attribute lets the real path run", "stacks": ["pytest", "unittest.mock"], "severity": "critical", "symptom": "The suite relayed 2,753 real SMTP sends while the email kill switch was on, exhausted the provider's quota and bounced real notices.", "root_cause": "The patch targeted a function that did not exist, `create=True` hid that, and the send sat inside `try/except: pass`.", "resolution": "A transport-level gate that tests cannot mock away (it refuses under pytest), plus a session-autouse fixture that makes `smtplib.SMTP`/`SMTP_SSL` raise.", "prevention": "Verify by counting the sent-log rows before and after a suite run.", "detection": [{"kind": "regex", "recipe": "patch\\([^)]*create=True"}, {"kind": "regex", "recipe": "except[^:]*:\\s*pass  (in test files)"}], "evidence": ["b58a5a4d3 (2026-09-01)", "f7805a246"], "sources": []}, {"id": "PT-003", "category": "python-testing", "title": "Patch where a name is USED, not where it is defined", "stacks": ["pytest", "unittest.mock"], "severity": "high", "symptom": "A test failed with `'mongo' == 'postgres'`, or passed by accident on a machine with a live database. Tests read and wrote real stores through unpatched modules.", "root_cause": "`from x import f` binds at import time. Extracting a query into a new store module, which has its own `from database import db`, silently unmocked every caller's `patch('routers.x.db')`.", "resolution": "Patch the importing module's name, plus a structural test that parses the calls a function makes and asserts each store call is mocked.", "prevention": "After any extraction refactor, grep the tests that patch the old location.", "detection": [{"kind": "review", "recipe": "for each patch('a.b.f'), find modules doing `from a.b import f` that the target calls"}], "evidence": ["d5c15ffe1 (2026-09-06)", "6aefd9233", "e7b172268"], "sources": ["https://docs.python.org/3/library/unittest.mock.html#where-to-patch"]}, {"id": "PT-004", "category": "python-testing", "title": "Unconstrained mocks on both sides of a boundary: a 100% inert feature with 44 green tests", "stacks": ["pytest", "unittest.mock"], "severity": "critical", "symptom": "A shipped feature did nothing in production while its whole test suite, CI and an AI review were green.", "root_cause": "An `AsyncMock()` without a spec accepts any arguments, so the failing call's wrong signature was never exercised.", "resolution": "Patch only the unit of work, let real argument validation run, and use `autospec=True` / `create_autospec`.", "prevention": "Require at least one test per boundary that calls the real callee.", "detection": [{"kind": "regex", "recipe": "(Async|Magic)Mock\\(\\)(?!.*spec)"}], "evidence": ["831c9285a", "4d46680a3 (2026-09-08)"], "sources": ["https://docs.python.org/3/library/unittest.mock.html#autospeccing"]}, {"id": "PT-005", "category": "python-testing", "title": "A MagicMock where an AsyncMock is needed", "stacks": ["pytest", "asyncio"], "severity": "high", "symptom": "`TypeError: object MagicMock can't be used in 'await'`, often swallowed. Adding one awaited DB call broke tests in six files.", "root_cause": "The production code awaits the method, and a plain MagicMock returns a non-awaitable. Inside `gather` the error can be hidden.", "resolution": "Use `AsyncMock` for awaited methods, and shared DB-mock factories.", "prevention": "After adding an awaited call on a shared path, grep for its mocks.", "detection": [{"kind": "regex", "recipe": "mock_db\\.\\w+\\.\\w+\\s*=\\s*MagicMock\\("}], "evidence": ["9dd281371", "backend/CLAUDE.md"], "sources": []}, {"id": "PT-006", "category": "python-testing", "title": "Patching `asyncio.create_task` or a module's `asyncio` with a bare MagicMock hangs at teardown", "stacks": ["pytest", "asyncio", "sqlalchemy"], "severity": "high", "symptom": "50 tests PASSED, then the process hung forever at loop finalisation.", "root_cause": "`AsyncSession.__aexit__` uses `asyncio.shield(create_task(...))`, so the connection never returned to the pool. Patching the whole module turned `gather` into a MagicMock too.", "resolution": "A replacement that closes the coroutine and returns a resolved Future, with `gather` passed through.", "prevention": "Diagnose hangs with `PYTHONFAULTHANDLER=1 timeout -s ABRT 45 pytest ...`.", "detection": [{"kind": "regex", "recipe": "patch\\(['\\\"](asyncio\\.create_task|[\\w.]+\\.asyncio)['\\\"]\\)(?!.*side_effect)"}], "evidence": ["26536b233 (2026-09-06)", "5282da9bb"], "sources": []}, {"id": "PT-007", "category": "python-testing", "title": "Mock data that copies the code's own wrong field names confirms the bug", "stacks": ["pytest"], "severity": "high", "symptom": "13 tests mocked `site_settings.find_one({'id':'main'})`, a document that has never existed, so the real API returned $0 for every tenant while the tests passed.", "root_cause": "The mocks were written from the code, not from the data.", "resolution": "Built fixtures from real documents or schemas.", "prevention": "Flag mocks of collections or keys that no writer ever writes.", "detection": [{"kind": "review", "recipe": "cross-check mock keys/collections against grep of inserts/updates"}], "evidence": ["memory: pattern_a_test_that_mirrors_the_bug_confirms_it"], "sources": []}, {"id": "PT-008", "category": "python-testing", "title": "Session-scoped autouse fixtures with side effects, and raising finalisers", "stacks": ["pytest"], "severity": "high", "symptom": "Every test run raised the production login rate limit from 10 to 200 per minute. A raising finaliser failed a fully green run.", "root_cause": "Fixing a broken shared client re-armed an autouse fixture that wrote to it, and the finaliser had no guard.", "resolution": "Removed the write, and wrapped the finalisers.", "prevention": "When you repair a shared handle, grep its callers and ask which of them WRITE.", "detection": [{"kind": "regex", "recipe": "scope=['\\\"]session['\\\"],\\s*autouse=True"}], "evidence": ["62cb84d47", "357fe32c8 (2026-09-08)"], "sources": []}, {"id": "PT-009", "category": "python-testing", "title": "An undeclared test plugin: `asyncio_mode=auto` without pytest-asyncio installed", "stacks": ["pytest", "packaging"], "severity": "medium", "symptom": "Tests errored in CI with 'async fixture with no plugin' while passing locally.", "root_cause": "pytest-asyncio was installed in the dev venv but absent from the CI requirements.", "resolution": "Declared the plugin.", "prevention": "If pytest.ini sets `asyncio_mode`, require pytest-asyncio in the requirements file.", "detection": [{"kind": "config", "recipe": "pytest.ini asyncio_mode set ⇒ pytest-asyncio in requirements"}], "evidence": ["9426f7de5", "61cd1c392 (2026-09-02)"], "sources": []}, {"id": "PT-010", "category": "python-testing", "title": "Wall-clock-dependent tests fail for only part of each period", "stacks": ["pytest", "time"], "severity": "high", "symptom": "Tests passed for most of a quarter and failed for the 14 days after each due date. They were written off as flaky.", "root_cause": "The logic read the real clock, and the grace-window branch existed only inside that window.", "resolution": "Pass `today` explicitly and pin `_now` in tests.", "prevention": "A defect that appears for 14 days a quarter reads as flaky, so treat intermittent date failures as real.", "detection": [{"kind": "review", "recipe": "date.today()/datetime.now() inside functions that take a date param; date-relative asserts without a clock fixture"}], "evidence": ["4e3a38b28 (2026-09-01)", "b40e914e4", "3a834630d"], "sources": []}, {"id": "PT-011", "category": "python-testing", "title": "A filtered, partial or wrong-cwd test run supports no '0 failures' claim", "stacks": ["pytest"], "severity": "medium", "symptom": "`pytest -k capital` skipped `TestCapitalShock` (6 failures). A path argument skipped a whole test root (133 tests). Running from the wrong directory produced FileNotFoundErrors that were mistaken for pre-existing failures.", "root_cause": "`-k` is case-sensitive, and path arguments override `testpaths`.", "resolution": "One canonical command (`venv/bin/python -m pytest -q` from the repo root), run in full after any fix.", "prevention": "Record the tree SHA in the run header, and don't edit files during a background run.", "detection": [{"kind": "ci", "recipe": "forbid -k/path-filtered pytest as a merge gate"}], "evidence": ["memory: pattern_filtered_test_runs_hide_failures", "CLAUDE.md §Backend"], "sources": []}, {"id": "PT-012", "category": "python-testing", "title": "Source-text oracles in tests match prose, docstrings and proximity", "stacks": ["pytest", "ast"], "severity": "medium", "symptom": "A `getsource` search for `date.today()` failed on its own docstring. A proximity check (`except Exception` within 1,200 characters) passed broken code and failed correct code.", "root_cause": "Substring and regex tests do not know the difference between code and comments.", "resolution": "Use `tokenize` to drop COMMENT and STRING tokens, `__code__.co_names`, or AST walks.", "prevention": "A change that should move a number and doesn't means the oracle is broken.", "detection": [{"kind": "regex", "recipe": "inspect\\.getsource\\(.*\\)\\s*(in|\\.find|re\\.search)"}], "evidence": ["b40e914e4", "5282da9bb"], "sources": []}, {"id": "PT-013", "category": "python-testing", "title": "`inspect.getsource` against a file edited during the run", "stacks": ["python", "pytest"], "severity": "low", "symptom": "A source-scanning test failed in a background run, then passed on its own and looked flaky.", "root_cause": "Line numbers are recorded at import time and the file is read at call time, so a +3-line edit shifted the slice.", "resolution": "Don't edit files during a test run, and run CI on an immutable checkout.", "prevention": "Prefer AST checks over `getsource` slicing.", "detection": [{"kind": "review", "recipe": "tests slicing getsource by line numbers"}], "evidence": ["memory: pattern_editing_source_during_a_background_test_run"], "sources": []}, {"id": "PT-014", "category": "python-testing", "title": "`SimpleNamespace` stand-ins for request models break when a model gains a field", "stacks": ["pytest", "pydantic"], "severity": "medium", "symptom": "One commit produced 51 failing tests with `AttributeError`.", "root_cause": "The tests duck-typed request models.", "resolution": "Construct real models (`Model(...)` or `model_construct`).", "prevention": "Grep for SimpleNamespace passed as a model.", "detection": [{"kind": "regex", "recipe": "SimpleNamespace\\(  (in tests passed to BaseModel-annotated params)"}], "evidence": ["0d0145d2e (2026-08-19)"], "sources": []}, {"id": "PT-015", "category": "python-testing", "title": "A stale `__pycache__` corrupts a bisect; `py_compile` writes a `.pyc`", "stacks": ["python"], "severity": "low", "symptom": "A confident, reproducible, completely wrong first-bad commit. A permission error (EACCES) reported as a syntax error.", "root_cause": "Bytecode from another checkout was reused, and `py_compile` writes its output next to the source.", "resolution": "Set `PYTHONDONTWRITEBYTECODE` or clear the caches between checkouts, and check syntax with `compile(src, path, 'exec')`.", "prevention": "Clean caches in any bisect script.", "detection": [{"kind": "shell", "recipe": "git bisect run sh -c 'find . -name __pycache__ -exec rm -rf {} +; pytest ...'"}], "evidence": ["26536b233", "f876d1ab7 (2026-08-28)"], "sources": []}, {"id": "PT-016", "category": "python-testing", "title": "Tests without a DB isolation layer write to the real database through default sessions", "stacks": ["pytest", "postgresql"], "severity": "high", "symptom": "Tests silently wrote real rows. Best-effort telemetry writers hid the errors.", "root_cause": "Services fell back to a default session built from `DATABASE_URL` when `pg_session_factory=None`, and `_safe_*` writers never raise.", "resolution": "Autouse mocks for the default factories, plus a 'production unreachable under pytest' guard.", "prevention": "A dedicated test database, with the redirect in the ROOT conftest.", "detection": [{"kind": "regex", "recipe": "pg_session_factory=None  (in tests/)"}], "evidence": ["memory: pattern_test_no_db_override_writes_to_real_control_plane"], "sources": []}, {"id": "PG-001", "category": "postgres-rls", "title": "With no tenant context set, a read of a FORCE-RLS table returns 0 rows and no error", "stacks": ["postgresql", "rls"], "severity": "critical", "symptom": "A feature 'never fired' while every test was green. A lookup returned None for a tenant that exists. An outbox relay polled 0 of 15,000+ rows from its first day while logging 'worker started' on every boot.", "root_cause": "A policy such as `tenant_id = current_setting('app.tenant_id')::uuid` evaluates to NULL or false when the GUC is unset. RLS filters rows; it never raises.", "resolution": "Set the context (a bypass sentinel or the real tenant) before querying. The relay's table got an explicit policy clause for its worker role.", "prevention": "Keep at least one test that runs the real SQL against a populated database, not mocks. When a `count(*)` returns 0, suspect missing context before concluding the data is gone.", "detection": [{"kind": "sql", "recipe": "SELECT n.nspname, c.relname FROM pg_class c JOIN pg_namespace n ON n.oid=c.relnamespace WHERE c.relforcerowsecurity;"}, {"kind": "ast", "recipe": "functions executing text(...) SQL against a FORCE-RLS table with no set_config('app.tenant_id', …) in the same function (match executed SQL, not docstrings)"}], "evidence": ["36535efb4 (2026-08-29)", "856ff86b5", "footgun #8"], "sources": []}, {"id": "PG-002", "category": "postgres-rls", "title": "A bypass sentinel works only on tables whose policy has a bypass clause", "stacks": ["postgresql", "rls"], "severity": "high", "symptom": "A cleanup `DELETE FROM lots` run under the sentinel deleted 0 rows, and the next `DELETE FROM schemes` failed with an FK violation. A snapshot reported '0 overrides' while 13 existed.", "root_cause": "Policies are asymmetric: some tables add `OR current_setting(...) = '<sentinel>'`, others are strictly `tenant_id = current_tenant_id()`.", "resolution": "Switch tenant context per TABLE, not per script block.", "prevention": "List which policies carry the bypass. Lint repair scripts for DML on a no-bypass table issued after SETting the sentinel.", "detection": [{"kind": "sql", "recipe": "SELECT polrelid::regclass, pg_get_expr(polqual, polrelid) FROM pg_policy;  -- grep each for the sentinel literal"}], "evidence": ["7336d6fdb (2026-08-11)", "footguns #7, #8"], "sources": []}, {"id": "PG-003", "category": "postgres-rls", "title": "A join under a real tenant context silently drops rows that belong to another tenant", "stacks": ["postgresql", "rls"], "severity": "medium", "symptom": "A report showed '8 of 13 rows have set_by = NULL' for a column declared NOT NULL.", "root_cause": "RLS on the joined users table hid platform-tenant actors (super admins), so the LEFT JOIN produced NULLs.", "resolution": "Resolve actors in a separate lookup under the bypass context.", "prevention": "When a NOT NULL column shows NULLs through a join, suspect RLS.", "detection": [{"kind": "review", "recipe": "joins from a tenant-scoped table to a table holding rows of other tenancies"}], "evidence": ["9d85dd826", "footgun #11"], "sources": []}, {"id": "PG-004", "category": "postgres-rls", "title": "Owners are exempt from RLS unless FORCE is set; superusers and BYPASSRLS roles are always exempt", "stacks": ["postgresql", "rls", "security"], "severity": "critical", "symptom": "No symptom at all: tenant isolation switches off silently once the app role owns a table without FORCE, or connects as a superuser.", "root_cause": "PostgreSQL applies RLS to the table owner only under `FORCE ROW LEVEL SECURITY`, and never applies it to superusers or BYPASSRLS roles. RLS with no policies is default-deny. Unique constraints and FK checks bypass RLS, so they can leak whether a row exists.", "resolution": "The ownership-repair tool checks every RLS table for FORCE and refuses if any lacks it; all 220 carry it. DATABASE_URL never points at a superuser.", "prevention": "The app connects as a NOSUPERUSER, non-owner role. Test isolation with that role: a superuser test DB turns isolation tests green while they test nothing.", "detection": [{"kind": "sql", "recipe": "SELECT relname FROM pg_class WHERE relrowsecurity AND NOT relforcerowsecurity;"}, {"kind": "sql", "recipe": "SELECT rolname FROM pg_roles WHERE rolsuper OR rolbypassrls;  -- compare to the app login role"}], "evidence": ["0fb5bc0b8 (2026-09-05)", "footgun #29"], "sources": ["https://www.postgresql.org/docs/16/ddl-rowsecurity.html"]}, {"id": "PG-005", "category": "postgres-rls", "title": "A new tenant-data table created without ENABLE/FORCE RLS", "stacks": ["postgresql", "rls"], "severity": "high", "symptom": "One table had no isolation for months. It was harmless only because nothing wrote to it, and it was about to hold bank details.", "root_cause": "RLS was added by a one-off sweep migration, and tables created after it were not covered.", "resolution": "A migration added the policy in the same change that gave the table sensitive data.", "prevention": "A test asserts that every table with a `tenant_id` column has `relrowsecurity`, `relforcerowsecurity` and at least one policy. Run it against the DEPLOYED schema too (see PG-018).", "detection": [{"kind": "sql", "recipe": "SELECT c.oid::regclass FROM pg_class c JOIN pg_attribute a ON a.attrelid=c.oid AND a.attname='tenant_id' WHERE c.relkind='r' AND (NOT c.relrowsecurity OR NOT c.relforcerowsecurity OR NOT EXISTS (SELECT 1 FROM pg_policy p WHERE p.polrelid=c.oid));"}], "evidence": ["ae45be32e (2026-09-12)", "tests/backend/test_rls_coverage.py"], "sources": []}, {"id": "PG-006", "category": "postgres-rls", "title": "A view over RLS tables runs with the VIEW OWNER's rights", "stacks": ["postgresql", "views", "rls"], "severity": "high", "symptom": "A latent leak: correct today, broken the moment the view owner differs from the caller, FORCE is dropped, or the view reads a table without RLS.", "root_cause": "Before `security_invoker` (PG15+), views evaluate policies and privileges as their owner. Materialized views store the owner's view of the data and cannot enforce the caller's policies at all.", "resolution": "Every view is created `WITH (security_invoker = true)`, and a test asserts it.", "prevention": "Never expose a materialized view directly; reach it through a tenant-filtered, permission-checked interface.", "detection": [{"kind": "sql", "recipe": "SELECT c.oid::regclass FROM pg_class c WHERE c.relkind IN ('v','m') AND c.relnamespace::regnamespace::text NOT IN ('pg_catalog','information_schema') AND NOT coalesce(c.reloptions @> '{security_invoker=true}', false);"}, {"kind": "regex", "recipe": "CREATE (OR REPLACE )?VIEW(?![^;]*security_invoker)"}], "evidence": ["1e778d719 (2026-09-02)", "docs/architecture/database_views_viability_2026-09-02.md"], "sources": ["https://www.postgresql.org/docs/16/sql-createview.html"]}, {"id": "PG-007", "category": "postgres-rls", "title": "Setting the tenant GUC by string interpolation, and `SET` versus `SET LOCAL`", "stacks": ["postgresql", "asyncpg", "security", "pooling"], "severity": "critical", "symptom": "103 sites built `f\"SET app.tenant_id = '{tid}'\"`. `text('SET LOCAL app.tenant_id = :tid')` can never work.", "root_cause": "PostgreSQL cannot bind parameters in `SET`. A plain `SET` (or `set_config(..., false)`) is session-scoped, so a pooled connection carries the previous request's tenant into the next one. Under PgBouncer transaction pooling, a SET outside a transaction lands on an arbitrary server connection.", "resolution": "`SELECT set_config('app.tenant_id', $1, true)` inside the request transaction.", "prevention": "Reset the connection on checkout (`DISCARD ALL` / `reset_on_return`). `current_setting('app.tenant_id', true)` must yield NULL, never a default tenant.", "detection": [{"kind": "regex", "recipe": "SET( LOCAL)?\\s+app\\.\\w+\\s*=\\s*['\\\"]?\\{"}, {"kind": "regex", "recipe": "set_config\\('app\\.[^']+',\\s*[^,]+,\\s*false\\)"}], "evidence": ["8a420bb14", "fddd857b8", "74a6ac3f0 (2026-08-29/30)"], "sources": ["https://www.postgresql.org/docs/16/sql-set.html", "https://www.pgbouncer.org/features.html"]}, {"id": "PG-008", "category": "postgres-rls", "title": "A migration's backfill UPDATE under FORCE RLS matches 0 rows", "stacks": ["postgresql", "alembic", "rls"], "severity": "high", "symptom": "The backfill left the column NULL and the following `SET NOT NULL` failed. Another migration reported 'linked 0 accounts' when 27 were unlinked.", "root_cause": "Alembic's env never set a tenant context, so every RLS read inside the migration saw nothing.", "resolution": "Disable/re-enable RLS around the backfill (or iterate real tenants), and assert the post-condition inside the migration.", "prevention": "Every data migration ends with an assertion that counts the rows it was meant to fix.", "detection": [{"kind": "ast", "recipe": "UPDATE/DELETE on a FORCE-RLS table in upgrade() with no set_config or DISABLE/ENABLE and no trailing assertion"}], "evidence": ["fe332612e (0077, 2026-08-03)", "1e778d719 (0117)"], "sources": []}, {"id": "PG-009", "category": "postgres-rls", "title": "Neither `count(*)` nor `n_live_tup` alone tells you whether a table is empty", "stacks": ["postgresql", "statistics"], "severity": "medium", "symptom": "`n_live_tup` read 0 for tables holding 3,480, 2,272 and 322 rows (never analysed), and 101 against a real 87 for another. A table census came back 44 populated / 202 empty when the truth was 67/179.", "root_cause": "`count(*)` is filtered by RLS. `n_live_tup` and `reltuples` are statistics that are zero until ANALYZE runs, and `reltuples = -1` means never analysed. The census script looked up the real tenants BEFORE setting the sentinel, so that lookup also returned 0 rows.", "resolution": "Take the MAX of the sentinel `count(*)`, each real tenant's `count(*)`, and `n_live_tup`. Set the sentinel BEFORE listing tenants, and assert more than one context was used.", "prevention": "Run `ANALYZE` after bulk loads. Treat `n_live_tup = 0` with `last_analyze IS NULL` as 'unmeasured'.", "detection": [{"kind": "sql", "recipe": "SELECT relname, n_live_tup, last_analyze, last_autoanalyze FROM pg_stat_user_tables WHERE last_analyze IS NULL AND last_autoanalyze IS NULL;"}], "evidence": ["9a421f8ff (2026-09-03)", "1e778d719"], "sources": ["https://www.postgresql.org/docs/16/routine-vacuuming.html#VACUUM-FOR-STATISTICS"]}, {"id": "PG-010", "category": "postgres-rls", "title": "A tenant id derived by computation instead of looked up", "stacks": ["postgresql", "foreign-keys"], "severity": "high", "symptom": "No registration ever created a user row. The FK rejected every insert, and a try/except swallowed the error.", "root_cause": "The tenant id was computed as `uuid5(NAMESPACE_DNS, f'tenant-{id}')`, which is not the real tenant.", "resolution": "Resolve `tenant_id` from the tenants/schemes table. Never derive it.", "prevention": "If a write must happen, assert that it landed.", "detection": [{"kind": "regex", "recipe": "uuid5\\([^)]*\\)\\s*#?.*tenant|tenant_id\\s*=\\s*uuid\\.?uuid5\\("}], "evidence": ["footgun #16"], "sources": []}, {"id": "PG-011", "category": "postgres-schema", "title": "`information_schema.data_type` reports citext, domains and enums as `USER-DEFINED`", "stacks": ["postgresql"], "severity": "medium", "symptom": "A PII sweep filtering on `data_type IN ('text','character varying')` skipped `users.email` and reported 'clean'.", "root_cause": "Extension and domain types report as `USER-DEFINED`; the actual type name is in `udt_name`.", "resolution": "Match `udt_name` too, or use `format_type(atttypid, atttypmod)` from pg_attribute.", "prevention": "Never conclude 'nothing remains' from a type-filtered sweep.", "detection": [{"kind": "regex", "recipe": "data_type\\s+IN\\s*\\(\\s*'text'"}], "evidence": ["footgun #13"], "sources": []}, {"id": "PG-012", "category": "postgres-schema", "title": "A cast to an enum type that does not exist, inside a broad except", "stacks": ["postgresql", "enums"], "severity": "high", "symptom": "`CAST(:s AS core.user_status)` pointed at a type that does not exist (the real one was `core.record_status`). The UndefinedObject error was caught as 'non-fatal', so every status change for months was written to one store only.", "root_cause": "Catalog-level programming errors were swallowed as if they were transient.", "resolution": "Cast to the correct type, and let `UndefinedObject`, `UndefinedColumn` and `UndefinedTable` propagate.", "prevention": "Validate `::schema.type` and `CAST(... AS schema.type)` in SQL strings against `pg_type` in CI.", "detection": [{"kind": "regex", "recipe": "(::|AS\\s+)(\\w+)\\.(\\w+)  -- extract and verify against pg_type"}], "evidence": ["footgun #23"], "sources": []}, {"id": "PG-013", "category": "postgres-schema", "title": "A NULL in a composite UNIQUE key collides with nothing", "stacks": ["postgresql", "constraints"], "severity": "critical", "symptom": "Every per-record credit was doubled, exactly 2.000×. Rows with a NULL `fund_id` coexisted with rows carrying a value for the same key.", "root_cause": "NULLs are distinct in UNIQUE constraints by default, so adding a nullable dimension to a key makes existing rows invisible to the new rows' uniqueness check. A partial index `WHERE fund_id IS NULL` deduplicated only within its own cohort.", "resolution": "Fixed the rows and the key. Use `UNIQUE NULLS NOT DISTINCT` (PG15+) or a NOT NULL column with a sentinel value.", "prevention": "Every nullable column inside a unique key needs a written justification. `ON CONFLICT` targets must match the index exactly.", "detection": [{"kind": "sql", "recipe": "SELECT conrelid::regclass, conname FROM pg_constraint c WHERE contype='u' AND EXISTS (SELECT 1 FROM pg_attribute a WHERE a.attrelid=c.conrelid AND a.attnum = ANY(c.conkey) AND NOT a.attnotnull);"}], "evidence": ["memory: pattern_a_null_in_a_uniqueness_key_collides_with_nothing (2026-09-04)"], "sources": ["https://www.postgresql.org/docs/16/ddl-constraints.html#DDL-CONSTRAINTS-UNIQUE-CONSTRAINTS"]}, {"id": "PG-014", "category": "postgres-schema", "title": "A text discriminator column with no CHECK constraint splits silently", "stacks": ["postgresql", "constraints"], "severity": "medium", "symptom": "`party_type` held 'individual' ×351, 'person' ×32 and 'organisation' ×31, so `WHERE party_type = 'individual'` returned a plausible, wrong count.", "root_cause": "A backfill migration wrote a new spelling into a free-text column. Nothing fails when a discriminator drifts.", "resolution": "Normalised the values and added `CHECK (party_type IN (...))`, after auditing every writer, because the constraint would otherwise block a writer nobody had checked.", "prevention": "Use an enum, lookup table or CHECK from day one for any discriminator.", "detection": [{"kind": "sql", "recipe": "SELECT col, count(*) FROM t GROUP BY 1  -- for low-cardinality text columns with no CHECK/FK; flag near-synonyms"}], "evidence": ["c231bedab", "0120 migration"], "sources": []}, {"id": "PG-015", "category": "postgres-schema", "title": "The ORM naming convention rewrites explicit constraint names", "stacks": ["sqlalchemy", "alembic"], "severity": "low", "symptom": "`parties_party_type_check` became `ck_parties_parties_party_type_check`, so a later `DROP CONSTRAINT` by the literal name failed.", "root_cause": "The `MetaData(naming_convention=...)` template wraps the name you pass.", "resolution": "Pass the SHORT name and let the convention build it. Verify against `pg_constraint.conname` after upgrade.", "prevention": "Read the emitted DDL, not the Python.", "detection": [{"kind": "sql", "recipe": "SELECT conname FROM pg_constraint WHERE conname LIKE 'ck_%\\_%\\_%check%';"}], "evidence": ["e787a9c94"], "sources": []}, {"id": "PG-016", "category": "postgres-schema", "title": "A backfill with no change to the writers is not an invariant", "stacks": ["postgresql", "migrations"], "severity": "high", "symptom": "A migration linked 32 users to their parties, and the very next INSERT reopened the gap. Four other writers (seeds, a startup hook, genesis) still created unlinked rows, and `ON CONFLICT DO UPDATE` never touched the column, so the gap reappeared only on a FRESH deploy.", "root_cause": "The data was fixed while the code that creates it was not.", "resolution": "Fixed every writer in the same PR (deriving ids with uuid5 so re-runs are idempotent), and added a test that measures the writers, not only the rows.", "prevention": "Every data backfill is paired with a writer change and a row-invariant test.", "detection": [{"kind": "review", "recipe": "a migration with UPDATE … SET col and no code diff touching INSERTs into that table"}, {"kind": "test", "recipe": "count(*) WHERE col IS NULL = 0 after running every writer path"}], "evidence": ["b9a629afb", "9d85dd826 (2026-09-02)"], "sources": []}, {"id": "PG-017", "category": "postgres-schema", "title": "`ON CONFLICT … DO UPDATE … RETURNING id` returns the existing row's OLD id", "stacks": ["postgresql"], "severity": "medium", "symptom": "A fix to how ids are derived 'did nothing': existing rows kept their old ids.", "root_cause": "An upsert keeps the surviving row's primary key.", "resolution": "After changing a derivation, compare the new value against the live rows. Delete row by row, each inside its own savepoint.", "prevention": "Verify a fix against live data, not only against its derivation.", "detection": [{"kind": "review", "recipe": "id-derivation changes without a live-row comparison"}], "evidence": ["91611f343 (2026-09-07)"], "sources": []}, {"id": "PG-018", "category": "postgres-schema", "title": "Schema gates run against a freshly migrated database prove only that the migrations agree with themselves", "stacks": ["postgresql", "testing"], "severity": "medium", "symptom": "RLS-coverage tests stayed green while drift existed in production: a policy dropped by hand, a table created outside migrations.", "root_cause": "CI migrates a throwaway container from the committed files, so it can never see the deployed catalog.", "resolution": "The read-only gates also run against the DEPLOYED schema through a read-only DSN. They found a defect in the author's own migration within a minute.", "prevention": "Every catalog assertion runs twice: against a fresh migrate AND against the deployed database.", "detection": [{"kind": "ci", "recipe": "run catalog tests with DATABASE_URL pointing at a read-only replica of production"}], "evidence": ["aa5e321c3 (2026-09-06)"], "sources": []}, {"id": "PG-019", "category": "postgres-schema", "title": "A permission-losing default on a dark code path", "stacks": ["postgresql", "authorization"], "severity": "critical", "symptom": "`allowed_roles=None` meant 'no filter'. The moment the domain was promoted, the new store's read would have returned every document to anonymous callers.", "root_cause": "An optional permission parameter defaulted to 'no restriction', on a path no test exercised because it was not yet live.", "resolution": "Visibility became a REQUIRED keyword argument, a restrictive-by-default `is_public BOOLEAN NOT NULL DEFAULT FALSE` column was added, and the function raises `VisibilityNotExpressible` instead of approximating.", "prevention": "Authorisation parameters never default to None. A restricted caller with no expressible branch sees nothing, not everything.", "detection": [{"kind": "ast", "recipe": "repo/query functions whose role/visibility/permission parameter defaults to None"}], "evidence": ["2dd288921 (0105, 2026-08-29)"], "sources": []}, {"id": "PG-020", "category": "postgres-schema", "title": "Money and time types: `timestamp` versus `timestamptz`, `numeric` versus float/`money`", "stacks": ["postgresql", "types"], "severity": "high", "symptom": "Dates shifted around DST changes, and amounts drifted.", "root_cause": "`timestamp without time zone` stores wall-clock time with no zone. Floats are inexact, and the `money` type depends on the locale.", "resolution": "Use `timestamptz` for instants, `date` for business dates, and integer cents (`bigint`) or `numeric(p,s)` for money.", "prevention": "Audit the catalog for these types.", "detection": [{"kind": "sql", "recipe": "SELECT table_schema, table_name, column_name, data_type FROM information_schema.columns WHERE data_type IN ('timestamp without time zone','real','double precision','money');"}], "evidence": [], "sources": ["https://wiki.postgresql.org/wiki/Don%27t_Do_This"]}, {"id": "PG-021", "category": "postgres-schema", "title": "Bitemporal 'current' means both open intervals", "stacks": ["postgresql", "temporal"], "severity": "medium", "symptom": "Superseded (retracted) records were reported as live.", "root_cause": "A retracted row still has `valid_to IS NULL`. Only `recorded_to IS NULL` distinguishes it.", "resolution": "Use `valid_to IS NULL AND recorded_to IS NULL`, in one shared predicate.", "prevention": "Centralise temporal predicates in the repository layer.", "detection": [{"kind": "regex", "recipe": "valid_to IS NULL(?!.*recorded_to)"}], "evidence": ["CLAUDE.md §Ownership"], "sources": []}, {"id": "PG-022", "category": "postgres-schema", "title": "Sequences are not transactional; identity versus serial", "stacks": ["postgresql"], "severity": "low", "symptom": "Gaps in ids, and duplicate-key errors after a bulk load with explicit ids.", "root_cause": "Rolled-back inserts consume sequence values, and loading explicit ids does not advance the sequence.", "resolution": "Use `GENERATED ALWAYS AS IDENTITY`, and `setval` after bulk loads. A gapless legal number needs a counter table updated under a lock.", "prevention": "Never use a surrogate id as an invoice or receipt number.", "detection": [{"kind": "sql", "recipe": "compare max(id) with the sequence's last_value per table"}], "evidence": [], "sources": ["https://wiki.postgresql.org/wiki/Don%27t_Do_This#Don.27t_use_serial"]}, {"id": "PG-023", "category": "postgres-schema", "title": "`SECURITY DEFINER` functions without a pinned `search_path`", "stacks": ["postgresql", "security"], "severity": "high", "symptom": "A privilege-escalation risk: objects in a schema the caller controls can shadow the names the function uses.", "root_cause": "The function resolves names through the caller's `search_path`. Clusters upgraded from before PG15 keep `CREATE` on `public` for everyone.", "resolution": "`ALTER FUNCTION … SET search_path = pg_catalog, <schema>`; `REVOKE CREATE ON SCHEMA public FROM PUBLIC`.", "prevention": "Catalog check in CI.", "detection": [{"kind": "sql", "recipe": "SELECT proname FROM pg_proc WHERE prosecdef AND NOT coalesce(array_to_string(proconfig, ',') LIKE '%search_path%', false);"}], "evidence": [], "sources": ["https://www.postgresql.org/docs/16/sql-createfunction.html#SQL-CREATEFUNCTION-SECURITY"]}, {"id": "PG-024", "category": "postgres-schema", "title": "No `statement_timeout`, `lock_timeout` or `idle_in_transaction_session_timeout`", "stacks": ["postgresql", "operations"], "severity": "medium", "symptom": "A runaway query or an abandoned transaction held locks and blocked vacuum indefinitely.", "root_cause": "The defaults are unlimited.", "resolution": "Set them per role, and override per migration.", "prevention": "Check the role configuration.", "detection": [{"kind": "sql", "recipe": "SELECT rolname, rolconfig FROM pg_roles WHERE rolname = '<app_role>';"}], "evidence": [], "sources": ["https://www.postgresql.org/docs/16/runtime-config-client.html#GUC-STATEMENT-TIMEOUT"]}, {"id": "PG-025", "category": "postgres-schema", "title": "A GRANT is not ownership, and the gap fails one step at a time", "stacks": ["postgresql", "privileges", "deploy"], "severity": "high", "symptom": "The deploy died on `CREATE SCHEMA IF NOT EXISTS core` for a schema that had existed for months, right after preflight printed 'PostgreSQL: OK'. Granting ALL cleared that error, and the next migration failed with 'must be owner of table'.", "root_cause": "The database had been created by a personal superuser. PostgreSQL checks the CREATE privilege BEFORE evaluating `IF NOT EXISTS`, and `ALTER TABLE` requires ownership.", "resolution": "An idempotent `align-postgres-ownership` repair that walks one database's catalog. It refuses if any RLS table lacks FORCE, because handing ownership to the app role would otherwise switch isolation off.", "prevention": "Traps: `REASSIGN OWNED BY` also moves SHARED objects (other databases, template0/1). A sequence owned by an identity or serial column cannot be re-owned on its own; exclude pg_depend deptypes 'a', 'i' and 'e'.", "detection": [{"kind": "sql", "recipe": "SELECT c.oid::regclass FROM pg_class c JOIN pg_namespace n ON n.oid=c.relnamespace WHERE n.nspname = ANY(:app_schemas) AND c.relowner <> :app_role::regrole;"}, {"kind": "sql", "recipe": "SELECT has_database_privilege(current_user, current_database(), 'CREATE');"}], "evidence": ["0fb5bc0b8 (2026-09-05)", "footgun #29"], "sources": ["https://www.postgresql.org/docs/16/sql-reassign-owned.html"]}, {"id": "AL-001", "category": "alembic", "title": "A revision id longer than `alembic_version.version_num` (VARCHAR(32))", "stacks": ["alembic"], "severity": "medium", "symptom": "The migration's DDL ran, then the final `UPDATE alembic_version` raised StringDataRightTruncation and rolled the whole transaction back. Offline `--sql` mode does not catch it.", "root_cause": "Alembic's version table column is 32 characters, and descriptive, sentence-style revision ids exceed that.", "resolution": "Use `NNNN_few_words` ids, with the filename matching the revision.", "prevention": "A CI test asserts `len(revision) <= 32` for every file in `versions/`.", "detection": [{"kind": "regex", "recipe": "^revision\\s*=\\s*['\\\"][^'\\\"]{33,}['\\\"]"}], "evidence": ["e787a9c94", "284d4a095", "bf0975313", "footgun #9"], "sources": []}, {"id": "AL-002", "category": "alembic", "title": "A migration applied to production from an UNTRACKED file", "stacks": ["alembic", "process"], "severity": "high", "symptom": "Production reported head `0124_…` while that file showed `??` in `git status`. This happened twice.", "root_cause": "The CI head check migrates a throwaway container from the committed files, so it validates the repo against itself and can never see the live head.", "resolution": "Commit the migration in the same change as the code that needs it, then run `upgrade`.", "prevention": "Deploy preflight reads the live `alembic_version` and asserts that `git ls-files` contains that revision and that it is an ancestor of HEAD.", "detection": [{"kind": "shell", "recipe": "rev=$(psql -Atc 'select version_num from core.alembic_version'); git ls-files backend/alembic/versions | grep -q \"$rev\""}], "evidence": ["footgun #28", "memory: pattern_untracked_migration_applied_to_production"], "sources": []}, {"id": "AL-003", "category": "alembic", "title": "A migration applied from an unmerged branch breaks every deploy from main", "stacks": ["alembic"], "severity": "high", "symptom": "Every deploy failed with `Can't locate revision identified by '0127_…'` for a day before anyone noticed.", "root_cause": "Alembic walks backwards from the database's revision, and main does not have that file.", "resolution": "Fix forward by merging; do not downgrade. `deploy.sh` now prints `alembic current`, `alembic heads` and the versions listing on failure.", "prevention": "The same preflight as AL-002: the database head must exist in the deployed checkout.", "detection": [{"kind": "shell", "recipe": "alembic current vs alembic heads in the deployed checkout"}], "evidence": ["d3b21141c (2026-09-05)"], "sources": []}, {"id": "AL-004", "category": "alembic", "title": "Two branches each add a migration on the same parent: multiple heads, and git sees no conflict", "stacks": ["alembic"], "severity": "medium", "symptom": "`alembic upgrade head` failed with 'Multiple head revisions'.", "root_cause": "Both files declare the same `down_revision`, and they are different files, so git merges them cleanly.", "resolution": "Renumber and repoint `down_revision` (or `alembic merge heads`), and update every prose reference.", "prevention": "CI asserts exactly one head.", "detection": [{"kind": "ci", "recipe": "test $(alembic heads | wc -l) -eq 1"}], "evidence": ["d90743fbc (2026-09-03)"], "sources": []}, {"id": "AL-005", "category": "alembic", "title": "`connection.execute()` in env.py auto-begins an outer transaction that swallows the migration run", "stacks": ["alembic", "sqlalchemy"], "severity": "high", "symptom": "All 62 migrations 'ran', and afterwards `alembic_version` did not exist.", "root_cause": "SQLAlchemy 2.x autobegins. Alembic's transaction nested inside that outer one, which was rolled back when the connection closed.", "resolution": "Commit the pre-statement (`CREATE SCHEMA`) before `context.begin_transaction()`.", "prevention": "After any env.py change, assert `alembic current` against a fresh database.", "detection": [{"kind": "review", "recipe": "connection.execute(...) in env.py before context.begin_transaction() with no commit"}], "evidence": ["8a97f4f48", "1c755796a (2026-07-05)"], "sources": []}, {"id": "AL-006", "category": "alembic", "title": "Autogenerate blind spots: renames, enums, server defaults, policies", "stacks": ["alembic"], "severity": "high", "symptom": "A renamed column was generated as drop + add, which would have lost data. Enum value additions, views, RLS policies and triggers never appeared in the diff.", "root_cause": "Autogenerate compares ORM metadata only, and cannot infer intent.", "resolution": "Always read and edit the generated file. Use `op.alter_column(new_column_name=…)` for renames and `ALTER TYPE … ADD VALUE` in an autocommit block.", "prevention": "In review, flag drop_column + add_column on the same table.", "detection": [{"kind": "review", "recipe": "op.drop_column and op.add_column on the same table in one migration"}], "evidence": [], "sources": ["https://alembic.sqlalchemy.org/en/latest/autogenerate.html#what-does-autogenerate-detect-and-what-does-it-not-detect"]}, {"id": "AL-007", "category": "alembic", "title": "Lock-heavy DDL: `CREATE INDEX CONCURRENTLY`, volatile defaults, `SET NOT NULL`", "stacks": ["alembic", "postgresql", "performance"], "severity": "high", "symptom": "A 'fast' ALTER queued behind a long transaction and stalled every query on the table. `CREATE INDEX CONCURRENTLY` failed inside the migration transaction.", "root_cause": "Most ALTERs take ACCESS EXCLUSIVE, and every later query queues behind them. A volatile default forces a table rewrite. `SET NOT NULL` scans the whole table. CONCURRENTLY cannot run inside a transaction.", "resolution": "`SET lock_timeout` with retry; add FK and CHECK constraints `NOT VALID`, then `VALIDATE`; use `autocommit_block()` for concurrent index builds.", "prevention": "Also flag `ADD COLUMN … NOT NULL` with no DEFAULT.", "detection": [{"kind": "regex", "recipe": "server_default=.*(gen_random_uuid|clock_timestamp|random)"}, {"kind": "regex", "recipe": "postgresql_concurrently=True(?![\\s\\S]*autocommit_block)"}, {"kind": "sql", "recipe": "SELECT indexrelid::regclass FROM pg_index WHERE NOT indisvalid;"}], "evidence": ["7f2a35fa8 (2026-09-11)"], "sources": ["https://www.postgresql.org/docs/16/sql-altertable.html#SQL-ALTERTABLE-NOTES", "https://alembic.sqlalchemy.org/en/latest/api/runtime.html#alembic.runtime.migration.MigrationContext.autocommit_block"]}, {"id": "AL-008", "category": "alembic", "title": "Importing ORM models into a migration binds it to current code", "stacks": ["alembic"], "severity": "medium", "symptom": "An old migration broke after the models changed.", "root_cause": "A migration must reflect the schema at ITS point in history, not the current model classes.", "resolution": "Use `sa.table()`/`sa.column()` lightweight constructs or raw SQL inside migrations, and batch large backfills in a separate script.", "prevention": "Test downgrades.", "detection": [{"kind": "regex", "recipe": "^from (models|db_postgres\\.models)  (in alembic/versions)"}], "evidence": [], "sources": ["https://alembic.sqlalchemy.org/en/latest/cookbook.html"]}, {"id": "AL-009", "category": "alembic", "title": "Migration linters that read source text naively", "stacks": ["alembic", "tooling"], "severity": "low", "symptom": "An RLS lint missed tables created in a loop and misread combined ALTER statements.", "root_cause": "Loops over literal lists, file-order ENABLE/FORCE/NO FORCE, comma-joined actions and schema qualification all defeat line-by-line reading.", "resolution": "The lint now unrolls literal loops, follows statement order, splits actions, matches exact qualified names, and ignores print, log and comment text.", "prevention": "Test lints with adversarial fixtures.", "detection": [{"kind": "test", "recipe": "fixtures covering loops, combined ALTERs and comments"}], "evidence": ["7f2a35fa8 (2026-09-11)"], "sources": []}, {"id": "SA-001", "category": "sqlalchemy-asyncpg", "title": "`text()` truncates a bind parameter immediately followed by `::cast`", "stacks": ["sqlalchemy"], "severity": "medium", "symptom": "`syntax error at or near ':'`: `:meta::jsonb` bound a parameter named `met`.", "root_cause": "SQLAlchemy's bind-parameter regex stops before `::`.", "resolution": "Use `CAST(:x AS type)`.", "prevention": "Regex lint.", "detection": [{"kind": "regex", "recipe": ":[A-Za-z_]\\w*::"}], "evidence": ["memory: pattern_sqlalchemy_text_cast_truncates_bindparam"], "sources": []}, {"id": "SA-002", "category": "sqlalchemy-asyncpg", "title": "asyncpg cannot infer the type of `(:p IS NULL OR col = :p)`", "stacks": ["asyncpg"], "severity": "medium", "symptom": "`AmbiguousParameterError` at PREPARE on every call, which mocked tests never reached.", "root_cause": "asyncpg prepares statements server-side and PostgreSQL needs a type for every parameter.", "resolution": "`CAST(:p AS text)` (text rather than citext when an exact comparison is intended).", "prevention": "Regex lint.", "detection": [{"kind": "regex", "recipe": ":\\w+\\s+IS\\s+(NOT\\s+)?NULL(?!.*CAST)"}], "evidence": ["0a83a0db4 (2026-08-10)", "9b48824ea"], "sources": []}, {"id": "SA-003", "category": "sqlalchemy-asyncpg", "title": "asyncpg encodes a DATE parameter with `date.toordinal()`", "stacks": ["asyncpg", "time"], "severity": "medium", "symptom": "`'str' object has no attribute 'toordinal'`. `CAST(:d AS DATE)` did not help.", "root_cause": "asyncpg performs no implicit string-to-date, uuid or numeric conversion on parameters. `numeric` comes back as Decimal, and json/jsonb as str unless a codec is registered.", "resolution": "Pass `datetime.date` values, and register JSON codecs.", "prevention": "Grep for `.isoformat()` values fed into query parameters.", "detection": [{"kind": "regex", "recipe": "isoformat\\(\\)  (in values passed as query params)"}], "evidence": ["5b3651839 (2026-08-27)", "footgun #21"], "sources": ["https://magicstack.github.io/asyncpg/current/usage.html#type-conversion"]}, {"id": "SA-004", "category": "sqlalchemy-asyncpg", "title": "A try/except around a statement does not un-abort the PostgreSQL transaction", "stacks": ["postgresql", "sqlalchemy"], "severity": "high", "symptom": "An outbox insert that 'must not abort' the identity write poisoned the transaction: the commit failed and the user UPDATE was discarded, while the other store's write stood.", "root_cause": "After any error, PostgreSQL rejects every statement until ROLLBACK. Catching the error in Python does not reset that.", "resolution": "`async with session.begin_nested():` (a SAVEPOINT) around best-effort statements, and a savepoint per DELETE in sweeps.", "prevention": "Lint for a try around `session.execute` followed by further use of the session.", "detection": [{"kind": "ast", "recipe": "try: session.execute(...) except: (no rollback/begin_nested) followed by more session use"}], "evidence": ["1a065726e (2026-09-01)", "6bd711bf9", "d991e88b0"], "sources": []}, {"id": "SA-005", "category": "sqlalchemy-asyncpg", "title": "Connection pool exhaustion", "stacks": ["sqlalchemy", "postgresql", "performance"], "severity": "high", "symptom": "711 `pg_unavailable` errors in 15 minutes: 4 workers × 30 connections against about 89 available.", "root_cause": "Total connections = (pool_size + max_overflow) × workers × processes. An unbounded gather over N items each opened a session.", "resolution": "A configurable pool with a budget check that leaves 25% headroom below `max_connections`. Fan-out is bounded by a semaphore, or replaced by a set-based query.", "prevention": "An `AsyncSession` is never shared between concurrent tasks.", "detection": [{"kind": "regex", "recipe": "gather\\(\\*\\[.*session"}, {"kind": "review", "recipe": "pool_size × workers vs max_connections"}], "evidence": ["306682c57", "2ebcabe4d"], "sources": ["https://docs.sqlalchemy.org/en/20/orm/extensions/asyncio.html#using-asyncsession-with-concurrent-tasks"]}, {"id": "SA-006", "category": "sqlalchemy-asyncpg", "title": "An unguarded database read inside a shared dependency takes down every route", "stacks": ["fastapi", "postgresql", "availability"], "severity": "high", "symptom": "A database outage 500'd 283 routes, including routes for tenants served entirely by the other store.", "root_cause": "A feature-flag read inside `require_feature()` had no exception boundary.", "resolution": "Distinguish 'raised' from 'empty', log the exception type, and fall back. A fallback may never ENABLE a protected flag.", "prevention": "Run the suite with the database unreachable and CLASSIFY the failures.", "detection": [{"kind": "review", "recipe": "global dependencies calling the DB without an exception boundary"}], "evidence": ["memory: pattern_an_unguarded_pg_read_behind_283_routes (2026-09-06)", "33d67f1e7"], "sources": []}, {"id": "SA-007", "category": "sqlalchemy-asyncpg", "title": "Async ORM pitfalls: lazy loading raises `MissingGreenlet`; `expire_on_commit` expires everything", "stacks": ["sqlalchemy"], "severity": "high", "symptom": "Reading `obj.id` after `await session.commit()` raised `MissingGreenlet`.", "root_cause": "Implicit I/O is impossible under asyncio, and `expire_on_commit=True` forces a refresh on the next attribute access.", "resolution": "`async_sessionmaker(..., expire_on_commit=False)`, eager loading, and `lazy='raise'` on relationships.", "prevention": "Lint sessionmaker and relationship configuration.", "detection": [{"kind": "regex", "recipe": "async_sessionmaker\\((?![^)]*expire_on_commit=False)"}, {"kind": "regex", "recipe": "relationship\\((?![^)]*lazy=)"}], "evidence": [], "sources": ["https://docs.sqlalchemy.org/en/20/orm/extensions/asyncio.html#preventing-implicit-io-when-using-asyncsession"]}, {"id": "SA-008", "category": "sqlalchemy-asyncpg", "title": "asyncpg prepared-statement caches conflict with PgBouncer and live DDL", "stacks": ["asyncpg", "pgbouncer"], "severity": "high", "symptom": "`prepared statement \"__asyncpg_stmt_x\" already exists`, or 'cached statement plan is invalid' after a migration.", "root_cause": "Statements are cached per connection. PgBouncer transaction mode moves statements between server connections, and DDL invalidates cached plans.", "resolution": "For PgBouncer: `statement_cache_size=0`, a unique `prepared_statement_name_func` and `NullPool` (or PgBouncer ≥1.21 `max_prepared_statements`). Restart or retry after migrations.", "prevention": "Check the engine `connect_args` whenever DATABASE_URL targets a pooler.", "detection": [{"kind": "review", "recipe": "create_async_engine against a pooler port without statement-cache settings"}], "evidence": [], "sources": ["https://docs.sqlalchemy.org/en/20/dialects/postgresql.html#prepared-statement-cache"]}, {"id": "MG-001", "category": "mongodb", "title": "A global unique index on a tenant-scoped collection", "stacks": ["mongodb", "multi-tenant"], "severity": "high", "symptom": "`DuplicateKeyError index: year_1`: the SECOND tenant failed mid-import and was left with partial data.", "root_cause": "The index predated multi-tenancy. An unreferenced seed script would have recreated it.", "resolution": "Create the compound `(tenant_id, key)` unique index BEFORE dropping the old one. A deploy-preflight gate requires every unique index to lead with the tenant key or carry a written exemption, and stale exemptions fail too.", "prevention": "Exemptions must be justified from data, not from code: six 'the id is a uuid' exemptions were false because the seeds wrote slugs.", "detection": [{"kind": "shell", "recipe": "db.coll.getIndexes() — flag unique:true whose key lacks the tenant field"}, {"kind": "regex", "recipe": "create_index\\([^)]*unique=True(?![^)]*tenant_id)"}], "evidence": ["4c32d4072", "501aced42", "5405b80dc"], "sources": []}, {"id": "MG-002", "category": "mongodb", "title": "A tenant-scoping wrapper covers filters but not inserts or unique keys, and 'global' collections bypass it", "stacks": ["mongodb", "multi-tenant"], "severity": "high", "symptom": "An invitations list returned every tenant's invitations because the collection was on the GLOBAL list. A lookup by a natural key matched across tenants. Self-registered users were stored with `tenant_id: None`.", "root_cause": "The wrapper injects the tenant into query filters only.", "resolution": "Removed the collection from the global list, scoped the lookups, and required the tenant field on inserts.", "prevention": "Review the global-collection list like an allow-list of privileged operations.", "detection": [{"kind": "ast", "recipe": "queries on GLOBAL collections without the tenant field in the filter"}, {"kind": "test", "recipe": "inserts into scoped collections require the tenant field"}], "evidence": ["c231bedab", "12be5aecb (2026-09-04)"], "sources": []}, {"id": "MG-003", "category": "mongodb", "title": "Numeric strings sort lexicographically", "stacks": ["mongodb"], "severity": "low", "symptom": "'1', '10', '11', '2'.", "root_cause": "String fields sort by byte order.", "resolution": "Post-sort in application code with a natural key, or store a numeric sort key.", "prevention": "Mirror of TS-006.", "detection": [{"kind": "regex", "recipe": "\\.sort\\(['\\\"]\\w*(number|no|num)['\\\"]"}], "evidence": ["footgun #6"], "sources": []}, {"id": "MG-004", "category": "mongodb", "title": "Mixed BSON types in one field make range queries miss documents", "stacks": ["mongodb"], "severity": "medium", "symptom": "`year` was an int for one tenant and a string for another, so `{$gte: 2026}` silently skipped the strings.", "root_cause": "Different writers used different types, and BSON comparison is bracketed by type.", "resolution": "Normalise the type, and add `$jsonSchema` validators.", "prevention": "Profile field types per collection.", "detection": [{"kind": "shell", "recipe": "db.c.aggregate([{$group:{_id:{$type:'$field'}, n:{$sum:1}}}])  -- more than one type ⇒ finding"}], "evidence": ["memory: pattern_tenant_scoped_collection_with_global_unique_index"], "sources": []}, {"id": "MG-005", "category": "mongodb", "title": "`{field: false}` versus `{$ne: true}` for flags that may be absent", "stacks": ["mongodb"], "severity": "medium", "symptom": "`{is_archived: False}` silently excluded older documents that lacked the field.", "root_cause": "An equality match does not match a missing field, while `$ne` does. `{f: null}` matches both null and missing.", "resolution": "Use `{'$ne': True}` for 'not flagged'. Add `$exists` when you mean existence, and backfill hot-path fields so plain equality can be used.", "prevention": "Regex lint.", "detection": [{"kind": "regex", "recipe": "['\\\"](is_\\w+)['\\\"]:\\s*False"}], "evidence": ["deea81ccc"], "sources": ["https://www.mongodb.com/docs/manual/reference/operator/query/ne/"]}, {"id": "MG-006", "category": "mongodb", "title": "`to_list(N)` silently truncates", "stacks": ["mongodb", "motor", "pymongo"], "severity": "high", "symptom": "A 130-row plan showed 100 rows and lost the LAST years. A collection grew past `to_list(200)`.", "root_cause": "`to_list(length)` returns at most N documents with no warning.", "resolution": "Aggregate totals server-side (`$group`), use named limit constants, and paginate.", "prevention": "Log when a result length equals its cap.", "detection": [{"kind": "regex", "recipe": "to_list\\(\\d+\\)"}], "evidence": ["deea81ccc (2026-08-30)", "footgun #2"], "sources": []}, {"id": "MG-007", "category": "mongodb", "title": "An upsert or `find_one` on a key that should be unique but isn't enforced", "stacks": ["mongodb"], "severity": "high", "symptom": "Two cache documents existed for one tenant. `update_one` refreshed one of them, and `find_one` with no sort could serve the frozen $0 document.", "root_cause": "Concurrent upserts without a unique index create duplicates.", "resolution": "A deterministic sort, a dedupe script, then a unique index. Index creation logs rather than crashing startup while duplicates remain.", "prevention": "Every `upsert=True` filter must be covered by a unique index.", "detection": [{"kind": "review", "recipe": "each update_one(filter, upsert=True): a unique index covers the filter keys"}], "evidence": ["6152266d4 (2026-09-05)"], "sources": ["https://www.mongodb.com/docs/manual/reference/method/db.collection.updateOne/#upsert-with-duplicate-values"]}, {"id": "MG-008", "category": "mongodb", "title": "A check-then-insert seeder races concurrent startups", "stacks": ["mongodb", "concurrency"], "severity": "medium", "symptom": "17 system chat groups where 9 belonged; the duplicates were written 0.8 ms apart.", "root_cause": "A `find_one` followed by `insert_one` is not atomic.", "resolution": "A single upsert keyed on a partial unique index, treating `DuplicateKeyError` as success.", "prevention": "Lint for the check-then-insert shape.", "detection": [{"kind": "ast", "recipe": "count_documents/find_one followed by insert_one on the same collection in one function"}], "evidence": ["a67ceb9d4 (2026-09-02)"], "sources": []}, {"id": "MG-009", "category": "mongodb", "title": "`$setOnInsert` idempotency is permanent", "stacks": ["mongodb"], "severity": "medium", "symptom": "176 of 209 backfill rows matched archived documents and never revived them. An invalid vote permanently occupied its key. An idempotency key containing today's date duplicated on re-run.", "root_cause": "The upsert matched an existing document, even an invalid or archived one, and did nothing.", "resolution": "Validate BEFORE the upsert. Idempotency keys never contain wall-clock values.", "prevention": "Review every `$setOnInsert`.", "detection": [{"kind": "ast", "recipe": "update_one(..., {'$setOnInsert': …}, upsert=True) preceding a validation of the same value"}], "evidence": ["footgun #5"], "sources": []}, {"id": "MG-010", "category": "mongodb", "title": "`insert_one(doc)` mutates `doc` by adding an ObjectId `_id`", "stacks": ["mongodb", "pymongo", "fastapi"], "severity": "high", "symptom": "Every real HTTP call returned 500 (ObjectId is not JSON-serialisable) AFTER the write had committed, so clients retried and duplicated it. Unit tests that called the function directly passed.", "root_cause": "The driver adds `_id` to the dict you pass in. A `find_one` without a projection returns `_id` too. `uuid.UUID` values are not BSON-encodable without configuration.", "resolution": "Use `insert_one(dict(doc))` and a shared projection constant `{'_id': 0}`.", "prevention": "Test through the HTTP layer.", "detection": [{"kind": "ast", "recipe": "a variable passed to insert_one/insert_many that is later returned"}, {"kind": "ast", "recipe": "find/find_one without an _id-excluding projection whose result is returned"}], "evidence": ["128eed577", "381074c26", "db73f2c02"], "sources": []}, {"id": "MG-011", "category": "mongodb", "title": "Index creation conflicts and partial-filter limits", "stacks": ["mongodb"], "severity": "low", "symptom": "`IndexOptionsConflict` (code 85) aborted the rest of `ensure_indexes`. `$ne` was rejected inside `partialFilterExpression`.", "root_cause": "The same key spec under a different name conflicts, and partial filters support only a subset of operators.", "resolution": "Compare key specs before creating, and use `$eq`/`$exists`/`$gt`/`$in`. Manage indexes in migrations, not on every boot.", "prevention": "Dedupe BEFORE creating a unique index.", "detection": [{"kind": "regex", "recipe": "partialFilterExpression[^}]*\\$(ne|nin|not)"}], "evidence": ["tasks/lessons.md LESSONS 15-17"], "sources": []}, {"id": "MG-012", "category": "mongodb", "title": "Unsorted pagination, and `estimated_document_count` versus `count_documents`", "stacks": ["mongodb"], "severity": "medium", "symptom": "Pages duplicated and skipped documents. A census count drifted.", "root_cause": "Natural order is not insertion order. `estimated_document_count()` reads metadata and ignores filters.", "resolution": "Always sort on a unique tiebreaker and prefer range pagination. Use `count_documents(filter)` for truth.", "prevention": "Lint `.skip(` without `.sort(`.", "detection": [{"kind": "regex", "recipe": "\\.skip\\((?![\\s\\S]{0,80}\\.sort\\()"}, {"kind": "regex", "recipe": "estimated_document_count"}], "evidence": [], "sources": ["https://www.mongodb.com/docs/manual/reference/method/cursor.sort/#sort-consistency"]}, {"id": "MG-013", "category": "mongodb", "title": "BSON datetimes come back naive, and ObjectId and string ids don't match", "stacks": ["mongodb", "pymongo", "time"], "severity": "medium", "symptom": "Comparing with aware datetimes raised TypeError. `{_id: '65f…'}` matched nothing.", "root_cause": "Without `tz_aware=True` the driver returns naive datetimes. Strings and ObjectIds never compare equal.", "resolution": "`MongoClient(..., tz_aware=True, tzinfo=timezone.utc)`. Pick one id representation per collection and convert at the boundary.", "prevention": "Lint client construction.", "detection": [{"kind": "regex", "recipe": "(Async)?MongoClient\\((?![^)]*tz_aware)"}], "evidence": [], "sources": ["https://pymongo.readthedocs.io/en/stable/examples/datetimes.html"]}, {"id": "MG-014", "category": "mongodb", "title": "NoSQL operator injection from JSON bodies", "stacks": ["mongodb", "security"], "severity": "high", "symptom": "A login filter built from raw JSON would accept `{'$ne': null}`.", "root_cause": "Client-supplied dicts were passed straight into query filters.", "resolution": "Validate with strict Pydantic `str` types. Never pass raw request dicts into filters.", "prevention": "Grep for request JSON flowing into find calls.", "detection": [{"kind": "regex", "recipe": "find(_one)?\\(\\s*(await\\s+)?request\\.json|find(_one)?\\(\\s*body\\b"}], "evidence": [], "sources": ["https://owasp.org/www-project-web-security-testing-guide/latest/4-Web_Application_Security_Testing/07-Input_Validation_Testing/05.6-Testing_for_NoSQL_Injection"]}, {"id": "MG-015", "category": "mongodb", "title": "Motor is deprecated: migrate to PyMongo's native async client", "stacks": ["mongodb", "motor", "pymongo"], "severity": "medium", "symptom": "An end-of-life dependency sat on every data path.", "root_cause": "MongoDB deprecated Motor in favour of `pymongo.AsyncMongoClient`. The APIs differ in which calls are coroutines (`aggregate`, `watch`, `start_session`, `close`).", "resolution": "DONE here (2026-04-28): migrated to `pymongo.AsyncMongoClient`.", "prevention": "Never import both `motor` and `pymongo.asynchronous` in one module, and check awaits on the calls that changed.", "detection": [{"kind": "regex", "recipe": "^from motor|^import motor"}], "evidence": ["backend/database.py (GAP-INF-001)"], "sources": ["https://www.mongodb.com/docs/languages/python/pymongo-driver/current/reference/migration/"]}, {"id": "MG-016", "category": "mongodb", "title": "Transactions need a replica set", "stacks": ["mongodb"], "severity": "medium", "symptom": "`start_transaction()` failed on a standalone development server.", "root_cause": "Multi-document transactions require a replica set.", "resolution": "Run a single-node replica set in development as well, and use `with_transaction(callback)` for its retry loop.", "prevention": "Keep development topology identical to production.", "detection": [{"kind": "config", "recipe": "mongod started with --replSet in dev compose"}], "evidence": [], "sources": ["https://www.mongodb.com/docs/manual/core/transactions/"]}, {"id": "DS-001", "category": "dual-store", "title": "Two stores, no shared transaction: order the writes", "stacks": ["postgresql", "mongodb", "outbox"], "severity": "high", "symptom": "A mirror write issued inside an open PostgreSQL transaction survived that transaction's rollback. A careful restore updated one copy of a denormalised field and missed a second, leaving 68 of 87 records with the wrong name.", "root_cause": "There is no atomicity across stores.", "resolution": "Commit the primary, then replay the secondary writes. The durable form is a transactional outbox: a row in the same transaction, and a relay using `FOR UPDATE SKIP LOCKED`, backoff and a dead-letter queue. Backfills emit through the same path.", "prevention": "List every denormalised copy of a field before repairing it.", "detection": [{"kind": "ast", "recipe": "secondary-store write calls lexically inside `async with session.begin()` or before commit()"}], "evidence": ["5b3651839", "7fc20f822", "ca5df6dbe", "footgun #21"], "sources": []}, {"id": "DS-002", "category": "dual-store", "title": "The same entity has different ids in two stores, so an update matches 0 and 'succeeds'", "stacks": ["postgresql", "mongodb"], "severity": "high", "symptom": "Accounts archived in one store stayed active in the other. The mirror reported success.", "root_cause": "The mirror keyed on the primary store's id, and `update_one` returns success with `matched_count == 0`.", "resolution": "Match on id OR a shared natural key, resolve the secondary row's own id before touching child documents, and assert `matched_count`.", "prevention": "A zero match can be legitimate (a record that exists in one store only), so establish which case applies.", "detection": [{"kind": "ast", "recipe": "update_one/update_many/delete_many results whose matched/deleted counts are never read"}], "evidence": ["5b3651839", "91611f343", "footgun #24"], "sources": []}, {"id": "DS-003", "category": "dual-store", "title": "A write or sweep that swallows its failure and reports success", "stacks": ["postgresql", "mongodb"], "severity": "high", "symptom": "An emitter logged 'emitted 87' while the database rejected all 87, because a unit number was passed into a UUID column. A teardown `delete_many` aimed at the wrong database deleted 0 rows for six months while 947 rows accumulated.", "root_cause": "Errors were swallowed, and the counts reported were attempts, not results.", "resolution": "Return and count successes, re-count afterwards, and report what SURVIVED.", "prevention": "Every sweep compares its deleted count against a pre-count.", "detection": [{"kind": "regex", "recipe": "except Exception:\\s*(pass|logger\\.)"}, {"kind": "review", "recipe": "deleted_count never compared with a pre-count"}], "evidence": ["7fc20f822", "1a065726e", "memory: pattern_a_sweep_that_succeeds_at_nothing"], "sources": []}, {"id": "DS-004", "category": "dual-store", "title": "Shadow and parity comparisons must compare the same population", "stacks": ["dual-store", "migration"], "severity": "medium", "symptom": "260 'critical' diffs came from a one-unit payload compared against an 87-unit aggregate, plus test fixtures. A domain was promoted with no parity check at all. A route marked 'shadow passing' served a balance that was 100× wrong.", "root_cause": "Comparisons did not carry their population. A missing check read as 'passing'.", "resolution": "Population markers on every comparison (a mis-scoped one records nothing), a registry test that derives the expected set of checks from the source, and a live call against a known reference value.", "prevention": "Parity is a gate, not a reading: promotion refuses on measured divergence. Every field in a compared payload is classified (redacted, excluded or compared), or PII leaks into the diff table.", "detection": [{"kind": "test", "recipe": "derive the set of parity checks from the source tree; assert each is registered"}], "evidence": ["9b48824ea", "CLAUDE.md §Shadow Comparisons"], "sources": []}, {"id": "DS-005", "category": "dual-store", "title": "A routing control plane whose missing row defaults to the legacy store", "stacks": ["dual-store"], "severity": "medium", "symptom": "Emptying the routing table silently de-promoted every domain.", "root_cause": "Failing closed is correct, but the failure is invisible.", "resolution": "Restore a domain's routing row only together with the data it routes to, and alert when the number of promoted domains drops.", "prevention": "Emit a metric of routing decisions by reason.", "detection": [{"kind": "review", "recipe": "count of promoted domains monitored"}], "evidence": ["footgun #17"], "sources": []}, {"id": "DS-006", "category": "dual-store", "title": "A DR or parity figure written into a document is a reading, not a fact", "stacks": ["dual-store", "documentation"], "severity": "high", "symptom": "'87/87 matching, $0.00 gap' stood in six places for five days while the live figure was '39 diverged, $37,797.50'.", "root_cause": "The sentence read as settled, so nobody re-ran the check.", "resolution": "Documents give the re-run command next to any figure.", "prevention": "Never quote a DR figure: run the read-only check.", "detection": [{"kind": "review", "recipe": "numbers in docs without an as-of date and a re-derivation command"}], "evidence": ["memory: pattern_a_dr_figure_is_a_reading_not_a_fact", "footgun #27"], "sources": []}, {"id": "DS-007", "category": "dual-store", "title": "The repair-script pattern: dry-run, back up, re-verify each row, refuse on partial match, verify survivors", "stacks": ["operations", "data-repair"], "severity": "high", "symptom": "Scripts that trusted a pattern acted on rows that didn't fit it: 17 of 87 did not match the assumed shape. A post-check that re-ran the script's own blind query printed VERIFIED on a half-applied production write.", "root_cause": "The script and its verification shared the same defect.", "resolution": "Dry-run by default; back up the rows that will change; re-verify EVERY per-row condition; REFUSE on unresolvable or partial matches; `DELETE … RETURNING` with the count asserted; switch tenant context per table; build new indexes before dropping old ones; verify what SURVIVES with an independent query.", "prevention": "Never let a verification reuse the artefact it verifies.", "detection": [{"kind": "review", "recipe": "repair scripts lacking --apply gating, backups, refusal paths or an independent post-check"}], "evidence": ["3bf1d8231", "6152266d4", "9d85dd826"], "sources": []}, {"id": "DS-008", "category": "dual-store", "title": "Soft-retire with first-class columns, not a flag buried in JSON", "stacks": ["postgresql", "schema"], "severity": "low", "symptom": "Retired rows were indistinguishable in queries.", "root_cause": "The retirement flag lived inside a JSONB blob.", "resolution": "Added `retired_at` and `retired_reason` columns, so `WHERE retired_at IS NULL` is visible to reviewers.", "prevention": "Lifecycle state belongs in typed columns.", "detection": [{"kind": "review", "recipe": "status/lifecycle flags stored in JSONB"}], "evidence": ["3bf1d8231 (0098)"], "sources": []}, {"id": "SC-001", "category": "security-authz", "title": "BOLA/IDOR: an object fetched by id with no ownership or tenant check", "stacks": ["fastapi", "authorization", "owasp"], "severity": "critical", "symptom": "A detail endpoint returned any record by id. A todo endpoint accepted an arbitrary user id and echoed back that person's name.", "root_cause": "The list route filtered by permission and the detail route did not, or the guard existed only in an unregistered duplicate.", "resolution": "Every object read and write resolves the object THROUGH the caller's scope (`WHERE id = :id AND tenant_id = :t AND <visibility>`) and returns 404 on a miss. Database RLS is the second line.", "prevention": "For every `/{id}` route, test that user A cannot read, update or delete user B's object.", "detection": [{"kind": "ast", "recipe": "route handlers with a path id param whose DB lookup filter lacks the tenant/owner field"}, {"kind": "test", "recipe": "cross-tenant and cross-user access matrix per id route"}], "evidence": ["CLAUDE.md §The duplicate that RUNS", "memory: pattern_hiding_test_data_is_not_removing_it"], "sources": ["https://owasp.org/API-Security/editions/2023/en/0xa1-broken-object-level-authorization/"]}, {"id": "SC-002", "category": "security-authz", "title": "Mass assignment: request models that accept privileged fields", "stacks": ["fastapi", "pydantic", "owasp"], "severity": "high", "symptom": "Clients could set `is_test_data`, `role` or `is_approved` in a body or header.", "root_cause": "The request model reused the storage model, or the code did `**body` into an update.", "resolution": "Separate Create/Update models containing only client-settable fields, with `extra='forbid'`. Derive server-owned fields on the server.", "prevention": "Lint for `**payload.dict()` in `update_one`/`UPDATE` calls.", "detection": [{"kind": "regex", "recipe": "\\$set['\\\"]?\\s*:\\s*(body|payload|data)(\\.model_dump\\(\\)|\\.dict\\(\\))"}, {"kind": "ast", "recipe": "request models declaring role/is_admin/is_approved/tenant_id/is_test_data"}], "evidence": ["CLAUDE.md §Never let the caller set the flag"], "sources": ["https://owasp.org/API-Security/editions/2023/en/0xa3-broken-object-property-level-authorization/"]}, {"id": "SC-003", "category": "security-authz", "title": "Reading the raw role instead of the effective role", "stacks": ["authorization"], "severity": "high", "symptom": "Elevated users got silent 403s, or a guard that included an elevated role never matched.", "root_cause": "Elevation changes the effective role, not the stored role.", "resolution": "One `effective_role()` helper, used by every guard, and a test that bans raw `user['role']` in guards.", "prevention": "Use role constants, never string literals.", "detection": [{"kind": "regex", "recipe": "current_user\\[['\\\"]role['\\\"]\\]\\s*(in|==|not in)"}], "evidence": ["CLAUDE.md §Role Guard Rules"], "sources": []}, {"id": "SC-004", "category": "security-authz", "title": "A permission check that reads a claim nobody populated", "stacks": ["authorization"], "severity": "high", "symptom": "The guard never fired in production; the unit test passed because it supplied the claim itself.", "root_cause": "The claim was hydrated only on a different request path.", "resolution": "One resolver function computes entitlements, and the guard calls it.", "prevention": "Before gating on a claim, find the code that populates it on THIS request path.", "detection": [{"kind": "review", "recipe": "guards reading current_user[...] keys not set by the auth dependency"}], "evidence": ["commit 969c600"], "sources": []}, {"id": "SC-005", "category": "security-authz", "title": "Tri-state authorisation answers treated as booleans", "stacks": ["authorization"], "severity": "medium", "symptom": "'Unmeasured' (None) was treated as False, flagging every record in a tenant with no imported data.", "root_cause": "Truthiness collapses None and False.", "resolution": "Compare `is False` or `=== false` explicitly, and render 'unmeasured' distinctly.", "prevention": "Type tri-state values as `bool | None` and lint for truthiness checks on them.", "detection": [{"kind": "review", "recipe": "`if not x` where x is Optional[bool]"}], "evidence": ["CLAUDE.md §A Link Is Not a Roll"], "sources": []}, {"id": "SC-006", "category": "security-authz", "title": "Approval granted on the strength of a public fact", "stacks": ["authentication"], "severity": "critical", "symptom": "Matching a name against a public register minted an approved, active account on an unverified address. An unauthenticated call approved a DIFFERENT pending account.", "root_cause": "Knowledge of a public fact was treated as a credential.", "resolution": "Only a credential approves an account: a hashed, expiring, single-use invite token bound to its scope. Nothing else may be added to that expression.", "prevention": "Never let an unauthenticated request mutate another account, or write a credential onto an account it hasn't proven it owns.", "detection": [{"kind": "review", "recipe": "registration/approval logic whose inputs are not secrets issued by the system"}], "evidence": ["CLAUDE.md §Only a Credential Approves an Account"], "sources": []}, {"id": "SC-007", "category": "security-authz", "title": "403 versus 404, and error-code classification", "stacks": ["authorization", "api-contract"], "severity": "low", "symptom": "403s leaked that an object exists, and the UI rendered the same message for three different refusals.", "root_cause": "Status codes alone carry too little information.", "resolution": "Return 404 when the caller has no right to know the object exists, and a typed error code for 403s (feature disabled / scope / role), classified once on the client.", "prevention": "Never compare error-code literals in pages; use the classifier.", "detection": [{"kind": "regex", "recipe": "['\\\"]MANAGER_FUNCTION_SCOPE['\\\"]  -- outside the classifier"}], "evidence": ["CLAUDE.md §Never render a 403 from the status code alone"], "sources": []}, {"id": "SC-008", "category": "security-authz", "title": "Rate-limit and trusted-proxy configuration", "stacks": ["security", "fastapi"], "severity": "medium", "symptom": "Every client appeared to come from the proxy's IP, so a single rate-limit bucket covered everyone. A test run raised production's login limit from 10 to 200 per minute through a shared client.", "root_cause": "X-Forwarded-For was not trusted (or was trusted from anyone), and configuration was shared between test and production.", "resolution": "`--proxy-headers --forwarded-allow-ips=<proxy CIDRs>`, and isolated rate-limit configuration per environment.", "prevention": "Log the resolved client IP at login.", "detection": [{"kind": "config", "recipe": "uvicorn forwarded-allow-ips not '*'"}], "evidence": ["memory: pattern_a_repair_arms_everything_that_shared_the_break"], "sources": ["https://www.uvicorn.org/deployment/#proxies-and-forwarded-headers"]}, {"id": "GT-001", "category": "gates-ratchets", "title": "A check dies in exactly three ways: ABSENT, DEFANGED, or BLIND", "stacks": ["ci", "tooling"], "severity": "high", "symptom": "7 of 18 validation scripts could not fail anything: nothing ran them, or they ran without the flag that fails, or their subject list was a hardcoded literal that new files never joined.", "root_cause": "A check is only as good as its invocation, its exit code and how it discovers what to check.", "resolution": "Every check is wired into CI or preflight, with `--check`/`--strict` that exits non-zero, and DERIVES its subjects from the tree (glob or AST) rather than from a hand-maintained list.", "prevention": "When you add a check, prove it can fail: seed one violation and watch it go red.", "detection": [{"kind": "ci", "recipe": "for each scripts/validation/*.py: is it invoked in a workflow or preflight WITH its failing flag?"}, {"kind": "regex", "recipe": "(FILES|TARGETS|PATHS)\\s*=\\s*\\(\\s*['\\\"]  -- a literal subject list inside a gate"}], "evidence": ["d58013530", "6de6b9824", "ba0d9f562", "aa696899a"], "sources": []}, {"id": "GT-002", "category": "gates-ratchets", "title": "A text-search oracle counts prose as code", "stacks": ["tooling"], "severity": "medium", "symptom": "A router was 'on the seam' because a DOCSTRING mentioned the dispatch function. `\"_store\" in src` matched the word 'stored'. One comment made a unit read as both missing AND orphaned.", "root_cause": "A substring search can't tell identifiers from comments and strings.", "resolution": "Use AST-based oracles: imports, calls and names, with comments and strings excluded. `--list` prints each credit's chain so a wrong credit can be argued with.", "prevention": "If a number doesn't move when you move the code, the gate is broken.", "detection": [{"kind": "review", "recipe": "gates implemented as `in src` / grep over raw file text"}], "evidence": ["memory: pattern_a_text_search_oracle_counts_prose_as_code", "GAP-TOOL-004"], "sources": []}, {"id": "GT-003", "category": "gates-ratchets", "title": "A transitive import oracle credits everything", "stacks": ["tooling"], "severity": "medium", "symptom": "An 'imports a PostgreSQL module (transitively)' rule credited all 66 routers, because the shared auth helper reaches PostgreSQL.", "root_cause": "Everything transitively reaches everything through shared utilities.", "resolution": "Bound the walk: two hops at most, never through another router, a registered shared owner, or the control plane.", "prevention": "Sanity-check any oracle by counting how many subjects it credits. 100% means it measures nothing.", "detection": [{"kind": "review", "recipe": "transitive-closure oracles without a boundary"}], "evidence": ["38268548b"], "sources": []}, {"id": "GT-004", "category": "gates-ratchets", "title": "A gate whose oracle is the working tree is non-deterministic", "stacks": ["ci", "tooling"], "severity": "medium", "symptom": "It passed locally and failed CI on the same commit. A marker path 'dangled' until `git add`.", "root_cause": "`Path.exists()` sees untracked files locally, and generated files that CI doesn't have.", "resolution": "The oracle is `git ls-files` (tracked content), never the filesystem.", "prevention": "Run gates in a clean clone or worktree before trusting a local green.", "detection": [{"kind": "regex", "recipe": "Path\\([^)]*\\)\\.exists\\(\\)  -- inside a validation/gate script"}], "evidence": ["memory: pattern_a_gate_whose_oracle_is_the_filesystem", "memory: pattern_featuretrace_path_oracle_is_git"], "sources": []}, {"id": "GT-005", "category": "gates-ratchets", "title": "An environment-dependent gate passes in one place and fails in another", "stacks": ["ci"], "severity": "medium", "symptom": "Gates behaved differently depending on whether a venv, a database, or a particular tool version was present.", "root_cause": "Their output depended on the environment, not on the tree.", "resolution": "Make generators deterministic: a DB-free mode for the committed artefact, and an explicit skip-and-report when a dependency is missing.", "prevention": "State what a gate needs in its header.", "detection": [{"kind": "review", "recipe": "gate output that changes with installed deps or DB reachability"}], "evidence": ["859424898", "5aa684bc6", "e604f78fe"], "sources": []}, {"id": "GT-006", "category": "gates-ratchets", "title": "A detector encodes ONE spelling of the defect", "stacks": ["tooling", "frontend"], "severity": "medium", "symptom": "A currency gate caught `$${x}` template literals and missed the JSX `<td>${fmt(x)}<\/td>` form that shipped.", "root_cause": "The defect has more than one syntactic form.", "resolution": "Enumerate the syntaxes, and add a fixture for each one.", "prevention": "Write the detector's test with the SHIPPED bad code as a fixture.", "detection": [{"kind": "test", "recipe": "one fixture per syntactic form of the defect"}], "evidence": ["8ecf8458b"], "sources": []}, {"id": "GT-007", "category": "gates-ratchets", "title": "A gate that blesses a helper it cannot see being used", "stacks": ["tooling"], "severity": "medium", "symptom": "A gate accepted 'sanctioned indirection' by reading its own docstring. The blessed helper had 0 callers, and the guard was hand-rolled elsewhere.", "root_cause": "The allow-path was never verified.", "resolution": "Verify that sanctioned helpers are actually called (AST), and re-derive allowlists from data.", "prevention": "Stale exemptions fail the gate too.", "detection": [{"kind": "test", "recipe": "every allowlist entry still matches something"}], "evidence": ["5405b80dc", "memory: pattern_a_sanctioned_indirection_the_gate_cannot_see"], "sources": []}, {"id": "GT-008", "category": "gates-ratchets", "title": "Report-only audits exit 0 with findings", "stacks": ["ci", "tooling"], "severity": "medium", "symptom": "`audit_*.py` scripts printed hundreds of findings and exited 0, so nobody read them.", "root_cause": "The scripts were built to report, and not everything was wired to fail.", "resolution": "Run them ALL before committing, grep the output for YOUR files, and convert any audit that people rely on into a ratchet.", "prevention": "'Exit 0' does not mean 'clean'.", "detection": [{"kind": "shell", "recipe": "for f in scripts/validation/audit_*.py; do python3 $f | grep -F \"$(git diff --name-only)\"; done"}], "evidence": ["09607eddc", "memory: feedback_run_every_validation_audit_script"], "sources": []}, {"id": "GT-009", "category": "gates-ratchets", "title": "Ratchet, don't wall: a baseline that can only fall, and fails on an un-baselined improvement", "stacks": ["ci"], "severity": "high", "symptom": "A day-one `--strict` gate failed every PR on debt nobody in that PR had created, and was about to be deleted.", "root_cause": "A hard gate on legacy debt blocks work that is unrelated to the debt.", "resolution": "A baseline JSON of existing debt: new violations fail, and so does an improvement that isn't locked into the baseline. The correct merge of two baselines is the LOWER value.", "prevention": "Parse the counts robustly: a ratchet that misparses reads as 0 and passes.", "detection": [{"kind": "ci", "recipe": "gate compares current counts to a committed baseline; both directions fail"}], "evidence": ["f1585fd66", "8dbff8273", "d9a65f0e2"], "sources": []}, {"id": "GT-010", "category": "gates-ratchets", "title": "A clean summary printed on a schema error", "stacks": ["tooling"], "severity": "medium", "symptom": "A validator hit a schema/parse error, skipped the item, and printed '0 findings'.", "root_cause": "The error path fell through to the success summary.", "resolution": "Parse and schema errors are findings (non-zero exit), never skips.", "prevention": "Test the gate with a malformed input.", "detection": [{"kind": "test", "recipe": "malformed input ⇒ non-zero exit"}], "evidence": ["4ff782878"], "sources": []}, {"id": "GT-011", "category": "gates-ratchets", "title": "`--check` that validates format but not freshness", "stacks": ["tooling"], "severity": "medium", "symptom": "A committed generated file was well-formed and stale.", "root_cause": "The check verified its shape, not that it equals a regeneration.", "resolution": "`--check` regenerates in memory and diffs against the committed file (or compares content hashes).", "prevention": "Every generated artefact gets a regenerate-and-compare gate.", "detection": [{"kind": "ci", "recipe": "regenerate → git diff --exit-code"}], "evidence": ["b18b53d73"], "sources": []}, {"id": "GT-012", "category": "gates-ratchets", "title": "A single CI job running sequential gates names only the FIRST failure", "stacks": ["ci"], "severity": "medium", "symptom": "Fixing the reported gate revealed the next one, then another: three rounds of CI.", "root_cause": "Ten gates in one job, `set -e`.", "resolution": "Run every gate and aggregate the results (`|| fail=1`), or use a matrix. Locally, run all ten before pushing.", "prevention": "Print a summary table of pass/fail per gate.", "detection": [{"kind": "review", "recipe": "multi-gate CI step using set -e without aggregation"}], "evidence": ["memory: pattern_repo_hygiene_stops_at_first_gate"], "sources": []}, {"id": "GT-013", "category": "gates-ratchets", "title": "Green CI with no backend tests run", "stacks": ["ci", "testing"], "severity": "high", "symptom": "No PR job ran the main backend suite; only the security tests ran. The frontend Jest job had been dropped. Green meant nothing.", "root_cause": "Workflows drifted and nobody asserted WHICH suites ran.", "resolution": "A job that runs the full suite with the right services, and a test that asserts the workflow files invoke each suite.", "prevention": "Read the job logs for the test count, not the checkmark.", "detection": [{"kind": "ci", "recipe": "assert test count > threshold in each suite job"}, {"kind": "review", "recipe": "workflows vs the list of suites on disk"}], "evidence": ["83b80c83a", "81e72a0a0", "memory: pattern_green_ci_does_not_mean_backend_tests_ran"], "sources": []}, {"id": "GT-014", "category": "gates-ratchets", "title": "An unprotected main branch makes every gate advisory", "stacks": ["github", "ci"], "severity": "high", "symptom": "Merges landed with red checks.", "root_cause": "Branch protection or required checks were not configured.", "resolution": "Configure required status checks and block force-pushes.", "prevention": "Audit the repository settings periodically (`gh api repos/:o/:r/branches/main/protection`).", "detection": [{"kind": "shell", "recipe": "gh api repos/{owner}/{repo}/branches/main/protection"}], "evidence": ["GAP-TOOL-012"], "sources": []}, {"id": "GT-015", "category": "gates-ratchets", "title": "A CI job wrongly declared as 'needs no database'", "stacks": ["ci"], "severity": "medium", "symptom": "Tests skipped silently or failed confusingly in CI.", "root_cause": "The job had no service container, and the tests guarded with skips.", "resolution": "Declare the services a job needs, and make a skipped test count visible (fail if skips exceed N).", "prevention": "`-rs` in pytest output, and a skip budget.", "detection": [{"kind": "ci", "recipe": "pytest -rs; fail on unexpected skip count"}], "evidence": ["f3459e2db", "edc7f2af3"], "sources": []}, {"id": "GT-016", "category": "gates-ratchets", "title": "A filtered test run cannot support a 'zero failures' claim", "stacks": ["testing"], "severity": "medium", "symptom": "`pytest -k capital` skipped `TestCapitalShock` (`-k` is case-sensitive) and hid 6 failures. Passing `tests/backend` explicitly skipped 133 tests collected by `testpaths`.", "root_cause": "The filter semantics were not what the author assumed.", "resolution": "Claim 'all green' only from the unfiltered command, and report the collected count.", "prevention": "Put the collected-test count in commit messages or PRs.", "detection": [{"kind": "review", "recipe": "claims of 'all tests pass' from a -k or path-filtered run"}], "evidence": ["memory: pattern_filtered_test_runs_hide_failures"], "sources": []}, {"id": "GT-017", "category": "gates-ratchets", "title": "Duplicate implementations are invisible to call graphs: keep a capability index", "stacks": ["architecture", "tooling"], "severity": "high", "symptom": "Lot-to-unit resolution was rebuilt five times. Two `dollars_to_cents` functions returned DIFFERENT money for '10.005'.", "root_cause": "A re-implementation creates no edge to the original, so every map renders it as healthy new code.", "resolution": "A YAML capability index (concept → owner module → consumers → detector), enforced in CI. Search by behaviour ('which functions read these stores') before writing a helper.", "prevention": "A detector must separate correct use from incorrect use, or it gets allowlisted into uselessness (`detect: null` is acceptable).", "detection": [{"kind": "ci", "recipe": "canonical-owner registry --check"}, {"kind": "shell", "recipe": "find_similar_functions.py --like '<purpose>' --touches <stores>"}], "evidence": ["memory: pattern_capability_index_one_concept_one_owner"], "sources": []}, {"id": "GA-001", "category": "generated-artefacts-git", "title": "A three-way text merge of a generated file is always wrong", "stacks": ["git"], "severity": "high", "symptom": "A 'successful' merge produced an artefact describing neither branch, and it still validated.", "root_cause": "The correct result is whatever the generator produces from the MERGED tree.", "resolution": "A `.gitattributes` merge driver keeps one side without a conflict, and a post-merge/post-rewrite hook regenerates from the completed tree. CI `--check` is the backstop.", "prevention": "The driver must not regenerate: it runs mid-merge on a half-applied tree, and a version that did regenerate was measured losing one branch's change.", "detection": [{"kind": "config", "recipe": ".gitattributes entries for every generated path; a test derives the set from the generators"}], "evidence": ["c374665c8", "fd795f5db"], "sources": []}, {"id": "GA-002", "category": "generated-artefacts-git", "title": "A rebase resolves generated artefacts with no conflict, and the result is wrong", "stacks": ["git"], "severity": "high", "symptom": "After a rebase the artefact matched neither tree, and hygiene failed later.", "root_cause": "The driver picks a side for each replayed commit, and nothing regenerates at the end.", "resolution": "Regenerate after every rebase (a post-rewrite hook) and before pushing.", "prevention": "Server-side merges (the GitHub button) run no client hooks, so regenerate after merging main.", "detection": [{"kind": "ci", "recipe": "regenerate-and-diff on every PR"}], "evidence": ["memory: pattern_a_rebase_resolves_generated_files_silently"], "sources": []}, {"id": "GA-003", "category": "generated-artefacts-git", "title": "Git config and hooks are not versioned", "stacks": ["git"], "severity": "low", "symptom": "`.gitattributes` named a driver that a fresh clone did not have.", "root_cause": "`git config` and `.git/hooks` are local to each clone.", "resolution": "An install script, run once per clone. Without it the fallback is a normal conflict, which degrades safely.", "prevention": "Document the installer in the README.", "detection": [{"kind": "review", "recipe": ".gitattributes merge= drivers without an installer"}], "evidence": [], "sources": []}, {"id": "GA-004", "category": "generated-artefacts-git", "title": "A wall clock (or hostname, username, absolute path) stamped into a committed artefact", "stacks": ["generated-artefacts"], "severity": "medium", "symptom": "Every regeneration dirtied the tree and every branch conflicted. Reviewers learned to skim the diff, and three API descriptions sat months stale behind a date line that changed daily.", "root_cause": "Non-deterministic metadata in the output.", "resolution": "Stamp a content hash, not a time. Omit fields such as `_postman_exported_at` rather than faking them.", "prevention": "Keep optional metadata out of the hash input.", "detection": [{"kind": "regex", "recipe": "datetime\\.now\\(|utcnow\\(|time\\.time\\(|socket\\.gethostname\\(|getpass\\.getuser\\(  -- in a generator writing a committed file"}], "evidence": ["be4bd74dd", "5babfe2ce"], "sources": []}, {"id": "GA-005", "category": "generated-artefacts-git", "title": "A generic filename routed to the wrong generator; skip versus fail", "stacks": ["generated-artefacts"], "severity": "low", "symptom": "'1 generator failed' on a healthy tree, because an artefact was mis-routed. A phantom failure looks identical to a real one.", "root_cause": "Routing was done by filename pattern.", "resolution": "Route by exact path. A generator that CANNOT run (no venv) is skipped and reported, never failed.", "prevention": "Run generators in dependency order.", "detection": [{"kind": "test", "recipe": "every generated path routes to exactly one generator"}], "evidence": ["34953e135"], "sources": []}, {"id": "GA-006", "category": "generated-artefacts-git", "title": "Regenerating in a shared working tree while other work is in progress", "stacks": ["git", "process"], "severity": "medium", "symptom": "A regeneration picked up another session's uncommitted edits, or clobbered them.", "root_cause": "Concurrent sessions or agents shared one checkout.", "resolution": "Regenerate in a dedicated worktree (`git worktree add`), and use one worktree per concurrent session.", "prevention": "Check `git status` for files you didn't touch before committing.", "detection": [{"kind": "review", "recipe": "commits containing unrelated generated diffs"}], "evidence": [], "sources": []}, {"id": "GA-007", "category": "generated-artefacts-git", "title": "Scripted edits (`sed -i`, `cat >`) corrupting or overwriting files", "stacks": ["shell", "process"], "severity": "high", "symptom": "A sed insert prefixed ALL 205 lines and still committed clean. `cat >` overwrote a 534-line existing module; git showed ` M`, not `??`.", "root_cause": "Bulk text edits without verification, and a write to a path assumed to be new.", "resolution": "Run `py_compile`/`tsc` after every scripted edit, check that a path doesn't exist before `>`, and read `git diff --stat` before committing.", "prevention": "Prefer structured edits over sed.", "detection": [{"kind": "shell", "recipe": "python3 -m py_compile $(git diff --name-only -- '*.py')"}, {"kind": "review", "recipe": "git status ' M' on a file you believed you created"}], "evidence": ["c402f0e9a", "7a3665a0d", "58bf6d07c"], "sources": []}, {"id": "GA-008", "category": "generated-artefacts-git", "title": "A path-rewrite checked with the same malformed list reports '0 leftover'", "stacks": ["shell"], "severity": "medium", "symptom": "97 references were stale and 35 were corrupted, and verification reported success.", "root_cause": "The check reused the defect in the input list.", "resolution": "Verify with an independent method, for example resolving every link target.", "prevention": "Never let a verification reuse the artefact it verifies.", "detection": [{"kind": "review", "recipe": "verification scripts sharing inputs with the change"}], "evidence": ["memory: pattern_a_path_rewrite_check_that_shares_its_own_defect"], "sources": []}, {"id": "GA-009", "category": "generated-artefacts-git", "title": "An annotated tag's SHA versus its commit's SHA; two-dot versus three-dot diff", "stacks": ["git"], "severity": "low", "symptom": "A comparison against a tag 'did not match' any commit. A diff showed main's changes as though they were the branch's.", "root_cause": "`git rev-parse tag` returns the tag object; `A..B` and `A...B` mean different things in `diff`.", "resolution": "Use `git rev-parse tag^{commit}`, and `git diff main...HEAD` for 'what this branch changed'.", "prevention": "Be deliberate about the diff range.", "detection": [{"kind": "regex", "recipe": "git rev-parse \\S+(?<!\\^\\{commit\\})\\s"}], "evidence": [], "sources": []}, {"id": "GH-001", "category": "github-deps", "title": "`gh pr edit` fails on old gh versions (Projects classic sunset), and the failure is partial", "stacks": ["github"], "severity": "low", "symptom": "Reads worked, edits failed, and `| tail` swallowed the exit code, so the PR kept its old body.", "root_cause": "gh 2.45 queries the removed `projectCards` field.", "resolution": "Use the REST API (`gh api -X PATCH repos/:o/:r/pulls/N`) and VERIFY that the body landed.", "prevention": "Never pipe a mutating command through `tail`/`head` without `set -o pipefail`.", "detection": [{"kind": "shell", "recipe": "gh --version < 2.46 and scripts using gh pr edit"}], "evidence": ["cfe9266a7"], "sources": []}, {"id": "GH-002", "category": "github-deps", "title": "A token without `workflow` scope cannot merge PRs that touch `.github/workflows`", "stacks": ["github"], "severity": "low", "symptom": "An API merge was refused, while an SSH push worked.", "root_cause": "GitHub requires the `workflow` scope for workflow-file changes.", "resolution": "Grant the scope, or merge through a push.", "prevention": "Check `gh auth status` scopes.", "detection": [{"kind": "shell", "recipe": "gh auth status 2>&1 | grep -q workflow"}], "evidence": ["memory: pattern_gh_token_cannot_merge_workflow_prs"], "sources": []}, {"id": "GH-003", "category": "github-deps", "title": "Dependabot groups by directory, so the root copy of a package goes stale", "stacks": ["dependencies", "github"], "severity": "medium", "symptom": "Frontend mermaid was bumped and the root copy was not. The CI parse gate used the root copy, so it passed syntax the browser couldn't draw.", "root_cause": "Each `package.json` directory is a separate Dependabot ecosystem entry.", "resolution": "List every manifest directory in `dependabot.yml`, and pin shared tools once.", "prevention": "A test asserts versions match across manifests for shared packages.", "detection": [{"kind": "test", "recipe": "same package version across all package.json files"}], "evidence": ["8e7449186", "memory: pattern_dependabot_groups_by_directory"], "sources": []}, {"id": "GH-004", "category": "github-deps", "title": "Dependency bumps that pass CI but change behaviour", "stacks": ["dependencies"], "severity": "medium", "symptom": "A minor bump changed runtime semantics (for example FastAPI's lazy `app.routes`, or Jest 30's flag rename).", "root_cause": "CI didn't cover the changed path.", "resolution": "Read the changelog for every bump. Batch low-risk bumps, and isolate framework bumps with a targeted smoke test.", "prevention": "Keep lockfiles committed, and use `npm ci`/`yarn --frozen-lockfile`.", "detection": [{"kind": "ci", "recipe": "frozen-lockfile installs"}], "evidence": ["94fe68df3"], "sources": []}, {"id": "GH-005", "category": "github-deps", "title": "AI review bots can fabricate findings", "stacks": ["github", "process"], "severity": "medium", "symptom": "3 of 4 bot findings on a PR cited code that existed nowhere.", "root_cause": "The model hallucinated the code under review.", "resolution": "Grep the cited symbol before acting on a bot finding.", "prevention": "Treat bot output as leads, never as facts.", "detection": [{"kind": "review", "recipe": "verify each cited file:line exists and says what is claimed"}], "evidence": ["memory: pattern_review_bot_cites_code_that_does_not_exist"], "sources": []}, {"id": "OP-001", "category": "deploy-ops", "title": "One job run by two schedulers", "stacks": ["operations", "cron", "systemd"], "severity": "high", "symptom": "Nine jobs ran from BOTH crontab and a systemd timer. A crontab-only deduplication pass could not see the other scheduler.", "root_cause": "No single source of truth for scheduling.", "resolution": "Pick ONE scheduler per job, and list each job's scheduler in one manifest.", "prevention": "Audit `crontab -l` and `systemctl list-timers` together.", "detection": [{"kind": "shell", "recipe": "crontab -l; systemctl list-timers --all  -- intersect job names"}], "evidence": ["cd25e84b9", "f7805a246", "memory: pattern_two_schedulers_one_job"], "sources": []}, {"id": "OP-002", "category": "deploy-ops", "title": "Units present in the repo but not installed on the host", "stacks": ["operations", "systemd"], "severity": "medium", "symptom": "Cron scripts that 'exist' never ran. Ten units in the repo were not installed.", "root_cause": "Nothing reconciled the repo with the host.", "resolution": "The installer installs every unit, and a preflight diffs `deploy/systemd/*` against `systemctl list-unit-files`.", "prevention": "Don't assume a script ran because it exists.", "detection": [{"kind": "shell", "recipe": "comm -23 <(ls deploy/systemd) <(systemctl list-unit-files --no-legend | awk '{print $1}')"}], "evidence": ["0f190a2d5", "d6602e03a"], "sources": []}, {"id": "OP-003", "category": "deploy-ops", "title": "systemd units run with a minimal PATH and environment", "stacks": ["systemd"], "severity": "medium", "symptom": "The command worked in a shell and failed under the unit with 'not found'.", "root_cause": "Units don't read shell profiles.", "resolution": "Use absolute paths (the venv's python), and `Environment=`/`EnvironmentFile=`.", "prevention": "Test with `systemd-run --user --wait` or `env -i`.", "detection": [{"kind": "regex", "recipe": "^ExecStart=(?!/)"}], "evidence": ["759c87434"], "sources": []}, {"id": "OP-004", "category": "deploy-ops", "title": "A hardcoded version used as an oracle ('is this ours?')", "stacks": ["deploy", "shell"], "severity": "high", "symptom": "A macOS deploy told the operator to move their own PostgreSQL 18 cluster off its port. A Linux preflight named a major version nothing identified as ours.", "root_cause": "It inferred ownership from a version literal, not from identity (data directory, port, owning role).", "resolution": "Identify ownership by what you configured (DSN host, port and data directory), never by version. pg_createcluster is Debian-only, and checksums are a per-cluster setting.", "prevention": "Grep scripts for version literals in conditionals.", "detection": [{"kind": "regex", "recipe": "if .*(==|-eq)\\s*['\\\"]?1[4-9]['\\\"]?  -- version literal in a deploy conditional"}], "evidence": ["985fda5a2", "b3ad4be56", "5fb847ef8", "e082c0b32", "1157c2508"], "sources": []}, {"id": "OP-005", "category": "deploy-ops", "title": "Preflight ordering and interactive sudo mid-deploy", "stacks": ["deploy"], "severity": "medium", "symptom": "A deploy half-ran, then stopped at a sudo prompt or a check that should have run first.", "root_cause": "Cheap checks ran after expensive mutations.", "resolution": "Order the deploy as read-only checks → build → migrate → restart. Request sudo up front (`sudo -v`) and stop and prompt the operator rather than storing credentials.", "prevention": "A preflight must be side-effect free.", "detection": [{"kind": "review", "recipe": "mutations before all read-only checks in deploy scripts"}], "evidence": ["a903afef2", "80429fd94", "fbc906b5c"], "sources": []}, {"id": "OP-006", "category": "deploy-ops", "title": "An install script that provisions only part of what the application needs", "stacks": ["deploy"], "severity": "medium", "symptom": "A provisioned machine had no Playwright (it lives in the root package.json), and no DB role ownership.", "root_cause": "The installer covered only one package manager or directory.", "resolution": "The installer installs every manifest, and a smoke test runs each toolchain's `--version`/`--list`.", "prevention": "Provision from scratch in CI occasionally.", "detection": [{"kind": "ci", "recipe": "fresh-container install + smoke"}], "evidence": [], "sources": []}, {"id": "OP-007", "category": "deploy-ops", "title": "Never rebuild or delete `.next` in the directory production is serving from", "stacks": ["nextjs", "deploy"], "severity": "high", "symptom": "A mid-deploy build and `rm -rf .next` killed the operator's own build twice, and the live site errored.", "root_cause": "The running server reads `.next` lazily.", "resolution": "Build into a separate directory or release folder and switch with a symlink, or build while stopped.", "prevention": "Never run `next build` in the live tree during a session.", "detection": [{"kind": "review", "recipe": "scripts running next build/rm .next in the served dir"}], "evidence": ["memory: pattern_yarn1_upgrade_is_a_noop_for_transitive_deps"], "sources": []}, {"id": "SE-001", "category": "secrets-test-isolation", "title": "`.env.example` created by copying the live `.env`", "stacks": ["secrets"], "severity": "critical", "symptom": "51 of 58 production secrets were committed and pushed, and nine permission rules held database credentials in plaintext.", "root_cause": "A template was made by copying the real file.", "resolution": "Every value in an example file is a placeholder. Rotate everything that was committed, and remember that removing it from the tree does not clear history. An encryption key for data at rest needs re-encryption, not just a value swap.", "prevention": "Run a secret scanner (gitleaks/trufflehog) in pre-commit and CI.", "detection": [{"kind": "shell", "recipe": "gitleaks detect --redact"}, {"kind": "regex", "recipe": "(?i)(secret|password|api_key|token)\\s*=\\s*(?!<)[A-Za-z0-9/+_\\-]{12,}  -- in *.example"}], "evidence": ["CLAUDE.md §Secrets (2026-08-26)"], "sources": []}, {"id": "SE-002", "category": "secrets-test-isolation", "title": "The test suite reaching production", "stacks": ["testing", "secrets"], "severity": "critical", "symptom": "Test users (1,772 active super admins with a committed password) leaked into production. A module-level `load_dotenv(override=True)` executed at COLLECTION redirected the whole suite to production. Tests relayed live mail despite a kill switch, because the mocked switch fell through to real smtplib. Tests wrote fixtures into a production comparison table.", "root_cause": "Test and production shared credentials, and mocks covered some stores but not others.", "resolution": "A separate test database with separate credentials that production refuses; an autouse fixture asserting the DSN is a test DSN; no `override=True` in importable modules; an `under_pytest()` backstop that flags writes as test data; a sweep of every flagged table; and 'mock EVERY store a handler writes to'.", "prevention": "An auth gate refuses test-data accounts in production.", "detection": [{"kind": "regex", "recipe": "load_dotenv\\([^)]*override=True"}, {"kind": "test", "recipe": "session fixture asserts DB name/host is the test one"}], "evidence": ["8ea663861", "5145b880b", "memory: pattern_one_import_redirected_the_whole_suite_to_production", "memory: pattern_test_suite_relayed_live_mail_via_mocked_gate"], "sources": []}, {"id": "SE-003", "category": "secrets-test-isolation", "title": "Test-data flags live on rows; nothing flags a FILE or an unflagged row", "stacks": ["testing"], "severity": "medium", "symptom": "A fake PDF was left in real storage. Unflagged test rows became live credentials.", "root_cause": "The flag exists only where somebody set it.", "resolution": "An autouse fixture redirects file storage, and code under test ORs in `under_pytest()`. An orphan-file sweep refuses to run if any claim collection fails to read.", "prevention": "Never let the CALLER set the flag (a header or body field can hide audit rows).", "detection": [{"kind": "regex", "recipe": "is_test_data\\s*[:=]\\s*(request|body|payload|headers)"}], "evidence": ["memory: pattern_bytes_on_disk_have_no_row_to_flag", "memory: pattern_caller_settable_test_flag_suppresses_audit"], "sources": []}, {"id": "SE-004", "category": "secrets-test-isolation", "title": "Never probe a live mutation endpoint with real data", "stacks": ["operations", "security"], "severity": "high", "symptom": "A diagnostic registration took over two real accounts, and one real name was overwritten.", "root_cause": "An 'existing email' branch claimed the account instead of returning 409.", "resolution": "Read the branch logic first, or probe with an address that can't belong to anyone (`probe+x@example.invalid`).", "prevention": "Use RFC 2606 reserved domains for all probes.", "detection": [{"kind": "review", "recipe": "curl/probe commands against prod with real identifiers"}], "evidence": ["footgun #12"], "sources": []}, {"id": "SE-005", "category": "secrets-test-isolation", "title": "A static build publishes the server's own source, credentials included", "stacks": ["secrets", "deploy", "javascript", "nginx"], "severity": "critical", "symptom": "`curl https://site/contact/feedback_server.py` returned 200 with `ADMIN_PASSWORD = '...'` and the Flask `SECRET_KEY` in plain text. The PHP admin panel's source and a shell script that echoed the same password were served the same way.", "root_cause": "The bundler's copy step listed a directory of operator scripts into the output directory the web server serves as static files. Python and PHP are not executed there, so the request falls through to the static handler and returns the file verbatim. The admin panel had therefore never worked — getting its source back over HTTP is the proof.", "resolution": "Drop them from the copy patterns and run operator scripts outside the document root. Rotate every credential the file carried: deleting the file does not un-publish what was already fetched, and the value survives in git history. Read the access log to see who fetched those paths.", "prevention": "Assert the build output contains no server-side source, and deny those suffixes at the web server as defence in depth.", "detection": [{"kind": "regex", "recipe": "from:\\s*['\\\"][^'\\\"]*\\.(py|php|sh|env|ini|bak)['\\\"]"}, {"kind": "shell", "recipe": "find dist -regex '.*\\.\\(py\\|php\\|sh\\|env\\|ini\\|bak\\)$'"}, {"kind": "ci", "recipe": "fetch each server-source path from the deployed site and require 404"}], "evidence": ["retirement_calculator_au (2026-09-20 audit): four files served 200 from /contact/ before the copy patterns were removed"], "sources": []}, {"id": "DK-001", "category": "docs-knowledge", "title": "Docs are the one artefact no gate reads", "stacks": ["documentation"], "severity": "medium", "symptom": "A phantom schema, a live tenant described as deleted, five diagrams that never rendered, and a canonical file that contradicted itself.", "root_cause": "Nothing executes documentation.", "resolution": "Validators for doc code blocks and diagrams (parse mermaid in CI with the same version the browser uses), and link and path checking.", "prevention": "A citation that EXISTS is not a citation that is CORRECT: check that it says what you claim.", "detection": [{"kind": "ci", "recipe": "mermaid parse, link check, cited file:line existence"}, {"kind": "review", "recipe": "cited lines actually support the claim"}], "evidence": ["fe2a64474", "bff8d4f8b", "324046032", "memory: pattern_citation_exists_is_not_citation_is_correct"], "sources": []}, {"id": "DK-002", "category": "docs-knowledge", "title": "A number in a document goes stale; give its as-of date and the command that re-derives it", "stacks": ["documentation"], "severity": "medium", "symptom": "Counts were quoted for weeks after they changed, and partitions didn't sum to their population.", "root_cause": "Readings were written as facts.", "resolution": "Every figure carries a date and its re-run command, and partitions are checked to sum to the stated total.", "prevention": "Line numbers in long-lived docs are readings too; cite a grep instead.", "detection": [{"kind": "review", "recipe": "numbers without dates; partitions whose parts don't sum"}], "evidence": ["bf6393dc9"], "sources": []}, {"id": "DK-003", "category": "docs-knowledge", "title": "A resolved blocker left standing, or a reopened one recorded as closed", "stacks": ["documentation", "process"], "severity": "medium", "symptom": "Work continued against a gap that had already been closed, while a regression went unchecked because the doc said 'done'.", "root_cause": "No re-verification step.", "resolution": "Stale TO-DO lists cost the same as stale facts. Re-verify status claims before acting on them.", "prevention": "Periodic 'stale checks' passes.", "detection": [{"kind": "review", "recipe": "status sentences older than N days in canonical docs"}], "evidence": [], "sources": []}, {"id": "DK-004", "category": "docs-knowledge", "title": "Working with coding agents: delegate search, verify load-bearing results", "stacks": ["process", "ai-agents"], "severity": "medium", "symptom": "Context bloated with file dumps, and confident wrong answers from subagents were taken at face value.", "root_cause": "Reading is expensive and thinking is cheap; an agent's summary is only as good as its reading.", "resolution": "Delegate wide searches to subagents that return conclusions, and verify every number before it lands in a commit or a doc.", "prevention": "Deferred decisions noted by an agent are decisions unmade; re-evaluate them.", "detection": [{"kind": "review", "recipe": "agent-provided numbers cited without re-verification"}], "evidence": ["memory: reference_claude_code_usage_profile", "memory: pattern_a_deferral_repeated_is_a_decision_unmade"], "sources": []}]};
